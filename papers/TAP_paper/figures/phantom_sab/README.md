# Figure: phantom_sab

Caption preview: $\APD(\rr)$, frontal $\khat$.

This directory should hold:
  - `phantom_sab.py` — generation script
  - `phantom_sab.pdf` and `phantom_sab.png` — outputs
  - optional `phantom_sab.tex` for tikz figures
