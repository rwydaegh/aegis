# Secrets
.env

# Python
__pycache__/
src/aegis/_version.py
*.pyc
*.pyo
*.egg-info/
dist/
build/
*.egg
.pytest_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/

# Virtual environments
.venv
.venv/
.venv-nf/
venv/

# IDE
.vscode/
.zed/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
desktop.ini
*.stackdump

# Data (phantom meshes, tissue database) - tracked in data/
# Ignore stray STL/DB files outside data/ and test fixtures
*.stl
*.obj
*.db
*.sqlite
data/phantoms/*.fbx
!data/
!data/*.stl
!data/*.db
!data/*.csv
!data/antenna_patterns/index.sqlite.gz
data/antenna_patterns/msi_raw/
# AMASS-derived pose streams - not redistributable, see data/poses/README.md
data/poses/*.npz
# Raw AMASS dumps (license-restricted, do not commit)
data/poses/amass_smplx_g/
# Derived per-body PoseStream files for plaza_run; regenerable via scripts/ingest_amass.py
data/poses/plaza_run_walks/
# OSM cache for plaza_run preflight; regenerable from Overpass
data/scenes/brussels_grand_place/
!tests/fixtures/e2e_lab/e2e_icosahedron.stl

# MkDocs build
site/

# Jupyter
.ipynb_checkpoints/

# Logs
*.log

# Batch runner outputs
outputs/

# Replay-viewer mesh exports (regenerable via export_meshes.py)
JSAC/code/experiments/*/replay/meshes_*.bin
JSAC/code/experiments/*/replay/meshes_*.json

# QA screenshots
test_screenshots/

# Playwright CLI state
.playwright-cli/
.playwright-mcp/

# Hypothesis test database
.hypothesis/

# Mutmut run artifacts (local audit + minor-release CI only)
mutants/
mutmut_results_*.txt

# Pynguin test-generation artifacts
pynguin-report/

# Debug screenshots
*.png
!docs/assets/*.png
!docs/assets/diagrams/*.png
!aegis-web/public/*.png
!data/phantoms/renders/*.png
!JSAC/code/experiments/*/replay/*.png
# The figures PAPER_METHODS.md embeds, so the document is readable from a clone.
# The rest of that collection stays out: the Blender renders run to tens of MB.
!papers/city-exposure-study/semantic_twin/FIGURES/03_what_one_panorama_sees.png
!papers/city-exposure-study/semantic_twin/FIGURES/05_four_layers_deep.png
!papers/city-exposure-study/semantic_twin/FIGURES/14_exposure_cdf_korenmarkt.png
!papers/city-exposure-study/semantic_twin/FIGURES/15_crop_convergence.png
!papers/city-exposure-study/semantic_twin/FIGURES/16_eleven_cities_exposure.png
!papers/city-exposure-study/semantic_twin/FIGURES/17_evidence_ladder.png
!papers/city-exposure-study/semantic_twin/FIGURES/18_elevation_geometry.png
!papers/city-exposure-study/semantic_twin/FIGURES/19_deployment_box.png
!papers/city-exposure-study/semantic_twin/FIGURES/20_adjoint_idea.png
!papers/city-exposure-study/semantic_twin/FIGURES/21_one_ray.png
!papers/city-exposure-study/semantic_twin/FIGURES/22_evidence_reach.png
!papers/city-exposure-study/semantic_twin/FIGURES/25_bounce_budget.png
# Visual regression baselines — committed to git so drift is reviewable in PR diffs.
!aegis-web/tests/e2e/__snapshots__/**/*.png

# Binary presentation artifacts (keep local copies, not in git)
*.pptx
*.mp4
*.pdf

# MCP config (local)
.mcp.json

# Local theory artifacts (not part of the repo)
theory/*.pdf
theory/*.aux
theory/*.out
theory/*.toc
theory/*.log

# JSAC paper LaTeX build artifacts
JSAC/**/*.aux
JSAC/**/*.out
JSAC/**/*.toc
JSAC/**/*.log

# Playwright test results
test-results/

# Superpowers plans/specs/prompts (local tooling, not part of docs)
docs/superpowers/
.superpowers/

# Internal dev notes
docs/internal/

# Personal spinoff notes
spinoff/personal/
spinoff/*.xlsx
spinoff/small_random/*.xlsx
spinoff/publications/JSAC special issue DIGITAL TWIN clo.txt

# Heavy FCC data pulls (regenerable via eas_pull.py)
spinoff/nearfield_gtm/fcc_pull/*.csv

# Local task list
TODO.txt

# Pre-commit last run state
.last_run.json

# Frontend smoke test notes
tests/frontend_smoke.md

# Cloud dev machine state (local, auto-generated)
tools/.cloud-state.json
.worktrees/

# Blender add-ons (local installs, not part of AEGIS)
blosm/
tools/mpfb2/

# Roof visualization scripts (dev-only)
scripts/*
!scripts/build_phantoms.py
!scripts/fetch_smplx.py
!scripts/fuzz_api.sh
!scripts/ingest_amass.py
!scripts/migrate_channel_fp16.py
!scripts/run_mutmut.sh
!scripts/studio_precompute.py

# SMPL-X model archives (license-gated, not redistributable)
smplx_*.zip
models_smplx_*.zip

# ── GSD baseline (auto-generated) ──
*~
*.code-workspace
.env.*
!.env.example
node_modules/
.next/

# React frontend build outputs
aegis-web/dist/
src/aegis/viewer/static/
target/
vendor/
coverage/
.cache/
tmp/
tmux-cheatsheet.md
.rm_confirmed
.git_destructive_confirmed

# Presentation build artifacts
presentations/group/media/
presentations/group/slides/
presentations/group/preview*/
presentations/group/__pycache__/
presentations/group/talk.html
presentations/group/talk*.pdf
presentations/group/*.aux
presentations/group/*.nav
presentations/group/*.out
presentations/group/*.snm
presentations/group/*.toc
presentations/group/*.vrb
presentations/group/main.pdf
presentations/promotors/*.aux
presentations/promotors/*.nav
presentations/promotors/*.out
presentations/promotors/*.snm
presentations/promotors/*.toc
presentation_unzipped/
presentation.zip

# Allow presentation poster frames and figures (small PNGs)
!presentations/group/animations/*.png
!presentations/group/figures/*.png
!presentations/promotors/figures/*.png

# Agent local state (not shared)
agent_hq/local/.last_reviewed.json
agent_hq/local/logs/
.claude/scheduled_tasks.lock

# Validation cache + oversized FDTD references (regenerable / >500K)
validation/data/geometry_cache/
validation/sphere_apd_full.npz

# Editor / tool backups
*.bak

# Papers workspace - exclude external materials and regenerable outputs
# (PDFs and PNGs are already excluded globally above)
papers/key_literature/
papers/example_papers/
papers/extracted/
papers/drafts/tmp_*/
papers/**/*.npz
papers/**/*.aux
papers/**/*.bbl
papers/**/*.blg
papers/**/*.fls
papers/**/*.fdb_latexmk
papers/**/*.synctex.gz
papers/**/*.toc
papers/**/*.out

# Theory workspace - regenerable build artifacts (sources stay tracked)
theory/**/*.npz
theory/**/*.aux
theory/**/*.bbl
theory/**/*.blg
theory/**/*.fls
theory/**/*.fdb_latexmk
theory/**/*.synctex.gz
theory/**/*.toc
theory/**/*.out

# JSAC code experiments - regenerable computation outputs
JSAC/code/experiments/**/*.npz
JSAC/code/experiments/**/outputs/

# Additional JSAC LaTeX build artifacts (extends earlier *.aux/*.out/*.toc/*.log rules)
JSAC/**/*.bbl
JSAC/**/*.blg
JSAC/**/*.fls
JSAC/**/*.fdb_latexmk
JSAC/**/*.synctex.gz

# TAP paper dielectric bundle: large SQLite blob (regenerable from IT'IS)
papers/TAP_paper/dielectric_bundle/itis_v5.db

# ── Embedded paper repo: versioned in its own GitHub repo, never gitlink here ──
papers/coherent-exposure-operator/

# ── Downloaded / license-gated model weights (rclone-backed, regenerable) ──
data/vposer/
*.ckpt
*.h5

# ── Archives (keep local, never commit) ──
*.zip
*.rar
!tests/fixtures/test_patterns.zip

# ── Sim4Life EM-sim blobs ──
*.smash

# ── JSAC2 experimental workspace: regenerable compute + 3D + LaTeX junk ──
JSAC2/code/replay/
JSAC2/code/outputs/
JSAC2/**/*.npz
JSAC2/**/*.bin
JSAC2/**/*.glb
JSAC2/**/*.aux
JSAC2/**/*.out
JSAC2/**/*.toc
JSAC2/**/*.log

# ── Patent-deck LaTeX build artifacts ──
presentations/patent_concepts/*.aux
presentations/patent_concepts/*.out
presentations/patent_concepts/*.toc
presentations/patent_concepts/*.nav
presentations/patent_concepts/*.snm
presentations/patent_concepts/*.log
presentations/patent_concepts/_render/
presentations/patent_meeting/*.aux
presentations/patent_meeting/*.out
presentations/patent_meeting/*.toc
presentations/patent_meeting/*.nav
presentations/patent_meeting/*.snm
presentations/patent_meeting/*.log

# ── Raw scrape data (large, regenerable) ──
spinoff/nature career column ai/*.json
spinoff/nature career column ai/*.txt

# RunPod SSH keys (never commit private keys)
runpod_april26
runpod_april26.pub

# ── Study outputs (generated meshes / exposure maps, regenerable) ──
results/
studies/**/*.aux
studies/**/*.out

# ── Coherent Exposure Studio packs (generated by scripts/studio_precompute.py) ──
data/studio/

# unified report build artifacts
theory/unified_physics.pdf
theory/unified_physics.aux
theory/unified_physics.toc
theory/unified_physics.out
theory/unified_physics.fls
theory/unified_physics.fdb_latexmk
theory/unified_physics.log
theory/_harness_*

# Deep-research notes (local only)
anti-ai.md

# Replay artifact (26MB, regenerable via paper repo build_replay_artifact.py)
aegis-web/public/factory.json
papers/city-population-exposure/

# Blender/web scene build output
tools/blender-mcp/renders/
tools/rf-scene-web/dist/

# Vendored third-party Blender assets. Never commit these.
# City Generator: 565 MB blend, "Royalty Free" licence, redistribution unclear.
# Buildify (Pavel Oliva): licence forbids selling the graph or its derivatives,
# and AEGIS is proprietary, so it must not be shipped inside it.
tools/citygen/The_City_Generator_2.6/
tools/buildify/
The_City_Generator_2.6*.zip

# Local document build intermediates
*.aux
*.out
*.toc
*.nav
*.snm
*.fls
*.fdb_latexmk
*.synctex.gz
# Downloaded source document, retained locally
/papers/city-exposure-study/1-s2.0-S0160412025002910-mmc1.docx
