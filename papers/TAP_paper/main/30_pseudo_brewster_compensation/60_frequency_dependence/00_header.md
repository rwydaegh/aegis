% NEXT: The accuracy of the constant-$T_0$ approximation has a clean
% NEXT: frequency dependence. Define the sphere ratio $R(f)$
% NEXT: \begin{equation}\label{eq:R-of-f}
% NEXT:   R(f) \equiv T_0(f) / \Tbar(f),
% NEXT:   \qquad
% NEXT:   \Tbar(f) \equiv 2\int_0^1 \Tavg(\mu, f)\,\mu\,\diff\mu\,,
% NEXT: \end{equation}
% NEXT: where $\Tbar(f)$ is the flux-weighted Fresnel transmission. $R(f) =
% NEXT: 1$ when the constant-$T_0$ approximation is exact on a
% NEXT: direction-averaged quantity. $R < 1$ means $T_0$ underestimates
% NEXT: absorbed power. $R > 1$ means it overestimates. \Cref{fig:R-of-f}
% NEXT: shows that for skin the crossover is at 40.4~GHz, where the
% NEXT: compensation is exact. Below this
% NEXT: frequency the approximation underestimates absorbed power. Above, it
% NEXT: overestimates by at most $3.5\%$ at 100~GHz. The root-mean-square error
% NEXT: remains below $5\%$ across $0.3$--100~GHz. Frequency-resolved
% NEXT: Cole--Cole values for skin and the angle family
% NEXT: $\Tavg(\theta)\cos\theta$ at six representative frequencies are in
% NEXT: Table~\ref{tab:itis-fvs} and Fig.~\ref{fig:si-angle-family} of the SI.
\subsection{Frequency dependence}\label{subsec:pB-freq}

## reviews (subsection_header)

_(empty — run /review to populate)_
