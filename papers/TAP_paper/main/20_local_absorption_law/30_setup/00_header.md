% NEXT: \Cref{fig:configuration} shows the considered configuration. A
% NEXT: plane wave with intensity $\IPD$ and direction $\khat$ illuminates
% NEXT: the body, and we evaluate $\APD(\rr)$ at each visible surface point.
% NEXT: 
% NEXT: A harmonic plane wave with time-averaged Poynting vector
% NEXT: $\mathbf{S}_{\mathrm{inc}} = \IPD\,\khat$ (units W/m$^2$) illuminates
% NEXT: a body. Three working assumptions hold throughout. First, the
% NEXT: surface $\Sigma$ is locally flat on the wavelength scale. Second, the
% NEXT: skin depth at every frequency of interest is much smaller than any
% NEXT: body dimension, so all power transmitted through the surface is
% NEXT: absorbed within a thin surface layer. Third, coherent reflections
% NEXT: from internal tissue interfaces and from other body parts are
% NEXT: neglected at this stage and re-enter as bounded corrections in
% NEXT: \cref{sec:cauchy,subsec:corr-residuals}. At a surface point $\rr$ with
% NEXT: outward unit normal $\nhat(\rr)$, the incidence cosine is
% NEXT: $\mu(\rr) \equiv \nhat(\rr)\cdot(-\khat) = \cos\theta_i(\rr)$. A
% NEXT: front-facing point has $\mu > 0$; a point facing away from the
% NEXT: source has $\mu \le 0$.
\subsection{Configuration}

## reviews (subsection_header)

_(empty — run /review to populate)_
