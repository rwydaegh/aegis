% PREV: A closed-form method is proposed for the absorbed power density on
% PREV: biological tissue from $1$ to $100$~GHz. The whole-body absorbed
% PREV: power factors into a flux-weighted Fresnel transmission $\Tbar(f)$
% PREV: times a body shape factor $\Aab/A$.
% NEXT: Two extensions follow naturally. Coherent beamforming replaces
% NEXT: summed powers with summed amplitudes on short-range mmWave devices.
% NEXT: At mmWave the far-field distance shrinks to centimeters, so the
% NEXT: device antenna pattern maps directly onto the body surface
% NEXT: (\cref{subsec:disc-validity}). Pre-compliance for handheld uplink
% NEXT: follows in closed form.
The cost no longer depends on frequency. The $f^4$ FDTD scaling
becomes one matrix-vector multiply with positive-part gating,
differentiable in antenna position, orientation, and beam codebooks. A simulation campaign
that takes weeks of FDTD reduces to a tissue-property lookup and an
ambient-occlusion pass on the body mesh.

## reviews (paragraph)



_PaperMaker9000 sweep — all clear across 4 lens(es)._

- **sentence-craft** — pass (14 rules cleared).
- **voice-tells** — pass (22 rules cleared).
- **lexical-spotcheck** — pass (54 rules cleared).
- **latex-micro** — pass (41 rules cleared).
- **incremental (2026-05-22)** — pass (2 new/edited rules cleared).

