% NEXT: The calculation uses a fixed route, city mesh, material map, and roofline source
% NEXT: curve. Every route point uses 15~GHz and a crop with radius
% NEXT: $R_{\mathrm{crop}}=250$~m, which gives
% NEXT: $A_{\mathrm{crop}}=\pi R_{\mathrm{crop}}^2=196{,}349.54$~m$^2$. The receiver
% NEXT: position $\mathbf{x}$ is also the reciprocal ray origin and body reference
% NEXT: point. The phantom faces along the local direction of travel. Index $i$ denotes
% NEXT: a roofline segment, $r_i(\mathbf{x})$ is its range to the receiver, and
% NEXT: $\mathbf{r}$ is the position of a body surface element. These assumptions stay
% NEXT: fixed across replicas and sites.
\section{Fixed-Route Exposure Method}
\label{sec:method}

## reviews (section_header)

_(empty, run /review to populate)_
