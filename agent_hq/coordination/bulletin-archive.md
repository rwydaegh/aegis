## Recent findings

<!-- Newest on top. Format: [YYYY-MM-DD HH:MM] agent-name: finding -->

- [2026-04-02] code-reviewer: Focus area: compliance checks and limits. Found and fixed 1 bug: MIMO _user_stats compliance check (mimo.py:165) only passed sab_4cm2 to evaluate_compliance, omitting sinc_local, sab_1cm2, and sar_wb checks that the single-user compute route includes. This meant MIMO compliance could falsely show PASS when sinc_local limit was exceeded. Fixed by extracting sinc_averaged, sinc, sab_1cm2_averaged, and sar_wb from the result and passing to evaluate_compliance. Added 1 regression test (test_sinc_local_included_in_compliance). Updated 3 test files to set explicit sinc/sab_1cm2/sar_wb attributes on MagicMock results instead of relying on auto-creation. Reviewed full compliance module (__init__.py), result.py evaluate_compliance, compute.py _build_stats_response, analysis.py sweep/heatmap routes, and link_budget_compliance. All ICNIRP limit values verified correct. No other compliance bugs found. All 2190 tests pass.

- [2026-04-02] code-reviewer: Focus area: test coverage gaps and edge cases. Reviewed all recently changed files (compliance heatmap fix, MIMO stats, export endpoints, spatial kernel einsum optimization, Switzerland adapter, share link frontend, location routes). No new bugs found. Added 19 tests covering: (1) MIMO _user_stats at sub-6 GHz (compliance vacuously None with no applicable checks), (2) sab_averaged=None fallback to raw sab peak for ICNIRP compliance, (3) distribution stats without body mesh, (4) _build_scene invalid freq_hz/power_dbm types, negative/zero freq_hz, (5) orientation rotation applied to device offset, (6) MIMO summary compliance at exact budget boundary, (7) ComplianceCheck edge cases (zero limit, zero value, exact limit, exceeded), (8) evaluate_compliance sub-6 GHz paths (no sab check, SAR check, SAR fail), (9) summary_text with/without tx_power_dbm. Also reviewed base station adapters (Switzerland, Canada, New Zealand, Denmark, Austria, Belgium/Flanders) and frontend (shareLink, useVoxelLoader, Toolbar, SceneRoot) for bugs. No actionable issues found in recently changed code. All 2223 tests pass. PR #315.

- [2026-04-02] feature-agent: Added camera position and scene visibility to share links. Previously, shared links only preserved simulation parameters and display settings but not the camera angle or visibility toggles (body mesh, ground plane, grid, scene geometry). Now share links capture the exact camera position/target and all visibility states, so recipients see exactly what the sharer saw. Implementation: (1) New cameraState.ts module tracks camera position outside R3F without triggering re-renders. (2) CameraSyncer component writes camera position/target on every frame. (3) CameraInitializer applies camera override from share links on mount. (4) Added direct setters (setBodyMeshVisible, etc.) to scene store for programmatic visibility control. (5) Rebuilt frontend. All 2163 tests pass, TypeScript clean.

- [2026-04-02] code-reviewer: Focus area: error handling and input validation. Reviewed all viewer routes (compute.py, mimo.py, basestations.py, location.py, data.py), compliance module, export module, spatial kernel, and all recently changed frontend files. Found and fixed 3 bugs: (1) compliance_heatmap looked up S_ab limit only at the first frequency in the sweep, so cross-6-GHz ranges silently skipped S_ab checks for above-6-GHz frequencies (false compliance). Now checks per-frequency like sinc already did. (2) summary_text reported "FAIL" when no compliance checks existed (None is falsy), now correctly reports "N/A". (3) _build_stats_response in compute.py only caught ValueError from evaluate_compliance, but TypeError could also occur (MIMO route already catches both). Added 3 regression tests. All 2204 tests pass. PR #313.

- [2026-04-02] code-reviewer: Focus area: test coverage gaps and edge cases. Reviewed recently changed files (export endpoints, compliance sweeps, MIMO stats, basestations routes). Found and fixed 1 bug: test_viewer_mimo_routes.py::TestUserStats::test_with_result was broken (MagicMock for result.sab instead of np.array, causing TypeError on comparison). Added 17 tests covering: (1) dosimetry-json export endpoint (404, success, stats filtering, optional arrays absent), (2) dosimetry-npz export endpoint (404, success, optional arrays absent), (3) MIMO _user_stats with body (illuminated_area_cm2 path), all-zero sab edge case, distribution stats, (4) frequency_sweep crossing 6 GHz with mixed sinc+sab checks, single-point sweep, non-compliant sinc at high frequency. All 2201 tests pass. PR #312.

- [2026-04-02] feature-agent: Added JSON and NPZ dosimetry export endpoints. (1) GET /api/export/dosimetry-json: full per-triangle data (centroids, normals, areas, sab, sab_4cm2, sinc, etc.) plus compliance verdict and peak stats in a self-describing JSON file. (2) GET /api/export/dosimetry-npz: compressed NumPy archive preserving full float64 precision, much smaller than CSV for large meshes. Both endpoints reuse the cached result from the last compute. Frontend ExportPanel updated with JSON and NPZ download buttons. The old "Export stats (JSON)" button (which only exported the stats summary) is replaced by the full dosimetry JSON export. All 1699 tests pass.

- [2026-04-02] feature-agent: UX improvements: (1) Added WARN state to toolbar ComplianceBadge when any check is within 20% of ICNIRP limit (was only PASS/FAIL). (2) Fixed SmartDynamicRange only firing once - now recalculates on every new simulation result instead of only the first. (3) Added tooltip to ModePill explaining correction letter codes (F=Fresnel, P=Polarisation, C=Curvature, D=Diffraction). (4) Added compute_timeout_ms (60s) and power_input.max (100 dBm) to config DEFAULTS, wired into frontend. All 1699 tests pass, frontend builds clean.

- [2026-04-02] feature-agent: Fixed Sentry #303 and hardened error handling across the stack. (1) Fixed "No voxel data loaded" Sentry error (#303): useVoxelLoader now handles 404 gracefully instead of reporting to Sentry, and clears has_voxels capability to prevent re-triggers. SionnaSceneSelector.handleLoad also clears has_voxels when switching to scene mode. (2) Fixed location.py TOCTOU: eliminated double find_pipeline() call and moved tiles_dir read inside cache_lock. Added try/except around run_pipeline generator. (3) Added error handling to _run_dosimetry and _make_rt_response in compute.py: all 4 RT routes (DiffeRT, Sionna, Sionna-voxel, Sionna-env) now return JSON errors instead of raw 500s on engine failures. (4) Added try/except around engine.compute in basestations route. (5) Optimized spatial kernel curvature term with einsum to avoid two (M,N) intermediate allocations. (6) Fixed displayQuantity fallback in toggleQuantity: now falls back to first remaining enabled quantity instead of hardcoded 'sab'. All 1699 tests pass, frontend builds clean. Fixes #303.

- [2026-04-01] feature-agent: Made compliance analysis charts interactive. Click any point on the power sweep, frequency sweep, or 2D compliance heatmap to jump to that operating point (frequency/power). The dosimetry auto-recomputes after navigation. Heatmap tooltip now shows "(click to apply)" hint. Transforms the Analysis panel from read-only charts into an interactive design space explorer.

- [2026-04-01] feature-agent: Frontend crash resilience and UX fixes. (1) Fixed 3 unsafe JSON.parse() calls on X-Meta response headers in environment.ts (2 occurrences) and terrain.ts (1 occurrence) that would crash the UI on malformed headers. Replaced with parseJsonHeader() utility from client.ts which throws readable errors. (2) Added top-level AppErrorBoundary wrapping the entire app so crashes in Toolbar/HudOverlay/Sidebar show a recovery UI instead of white screen of death. (3) Fixed PeakIndicator ignoring displayQuantity: ring and label now follow the active visualization quantity (sab, sab_4cm2, sab_1cm2, sinc_local) instead of always showing sab peak. Ring radius adjusts for 1cm² vs 4cm² averaging area. (4) Added logging for silent channel preset loading failures in compute route. Closed issue #285 (already fixed by PR #286). All 1699 tests pass, frontend builds clean.

- [2026-04-01] code-reviewer: Focus area: type safety and API contracts. Reviewed all viewer routes (basestations.py, compute.py, environment.py, mimo.py, terrain.py, data.py), basestation modules (adapter.py, build.py, merge.py, parquet_io.py, provenance.py, classify.py, library.py, msi.py, antenna.py, utils.py), core modules (compliance/__init__.py, environment/terrain.py, geometry/averaging.py, geometry/device_offset.py, coherent/body_channel.py, coherent/fresnel_operator.py, coherent/ecbf.py, coherent/exposure_operator.py, mimo/compute.py, mimo/precoders.py). Found and fixed 2 bugs: (1) mimo.py:79-82 device_offset/device_position null values crashed np.array(None) before try/except block (unhandled TypeError -> 500). Changed to or-chain for None-aware fallback. (2) mimo.py:30-34 users_cfg accepted non-list types (e.g. string) that passed truthiness check but crashed on iteration. Added isinstance validation. Added 3 regression tests. All 2154 tests pass. PR #296.

- [2026-04-01] code-reviewer: Focus area: coherent MIMO and ECBF. Thorough review of all coherent pipeline code: body_channel.py (compute_body_channel, compute_body_channel_factored), fresnel_operator.py (te_tm_basis, compute_fresnel_operator, apply_fresnel_operator), exposure_operator.py (compute_exposure_operator, eigendecompose_Q, compute_rho), ecbf.py (solve_ecbf, bisection solver), field_channel.py, _accumulate.py, level7_coherent.py, level8_ecbf.py, mimo/compute.py (build_user_channels, compute_mimo_scene, compute_mimo_scene_with_bodies, compute_multistream_sab, compute_mrt_precoder, compute_user_sab, compute_total_exposure, _default_los_paths), mimo/precoders.py (mrt, zf, mmse, zf_exposure), mimo/array_paths.py (expand_paths_to_array), mimo/channel.py (dipole_effective_length, compute_channel_vector), mimo/array.py (AntennaArray, steering_matrix), mimo/scene.py, mimo/user.py, viewer/routes/mimo.py. Verified all coherent physics equations against monograph_v2.tex: G_tilde definition, depth coupling sqrt(sigma/(4*alpha)), Fresnel operator, exposure operator Q, ECBF QCQP, coherent_sinc. Numerically verified coherent-incoherent consistency at normal incidence (L7 matches L3 to <1%). Found and fixed 1 bug: mmse() precoder did not catch LinAlgError (unlike zf() which falls back to MRT). Fixed misleading comment in compute_rho (said h^T Q h* but code correctly computes h^H Q h). Added 1 regression test. All 1731 tests pass. PR #293.

- [2026-04-01] feature-agent: Added real SRTM terrain elevation to the viewer. The terrain route now downloads SRTM1 (1 arc-second, ~30 m resolution) elevation tiles from AWS open terrain data, caches them locally in ~/.cache/aegis/srtm/, and builds terrain meshes with real elevation. Falls back to flat terrain for ocean/polar areas. Frontend shows "SRTM elevation loaded (X m range)" or "Flat terrain" status. Also fixed 2 pre-existing test failures from the sub-6 GHz compliance extension (#284) where tests expected 1 GHz to be rejected but it is now valid. Added 7 new terrain tests. All 1695 tests pass, frontend builds clean.

- [2026-04-01] code-reviewer: Focus area: type safety and API contracts. Found and fixed 3 bugs: (1) basestations.py:203 KeyError when client sends `lat` without `lon` (missing paired key check). (2) cloudrf.py:69-70 TypeError when CloudRF API returns null gain fields (`None + 2.15`). (3) msi.py:133-145 IndexError on MSI files with >360 rows or truncated data (no bounds checks on pre-allocated arrays). Removed dead code reference on msi.py:118. Added 4 regression tests. Also reviewed adapter.py, merge.py, parquet_io.py, library.py, classify.py, build.py, provenance.py, analysis.py, compute.py, environment.py, terrain.py, mimo.py, body_channel.py, fresnel_operator.py, averaging.py, device_offset.py, cloudrf.py, viewer/config.py. No other actionable bugs found. All 1687 tests pass. PR #282.

- [2026-04-01] feature-agent: Added exposure distribution statistics and SAB histogram to the Analysis panel. Backend now returns distribution stats (mean, median, p95, p99, illuminated fraction/area, illuminated mean/median) in every compute response via `_build_stats_response`. Frontend Analysis panel has two new sections: (1) Exposure Distribution showing illumination coverage bar, illuminated area, and a statistics table (peak, p99, p95, mean, illuminated mean/median). (2) SAB Histogram computed client-side from the existing Float32Array with log-scale bins, color-coded above/below ICNIRP limit, and interactive tooltips. No new API calls needed for histogram. All 1684 tests pass, frontend builds clean.

- [2026-04-01] feature-agent: Performance optimizations across three hot paths: (1) Replaced Python list accumulation in `_precompute_numpy` (averaging.py) with pre-allocated NumPy arrays using CSR-format neighbor lists, eliminating O(M) list resizing for meshes without Numba. (2) Replaced broadcast-multiply-sum patterns with `einsum` in `apply_fresnel_operator` and `compute_body_channel_factored`, avoiding (M, N, 3) intermediate allocations in the coherent Fresnel path. (3) Fixed redundant full-array norm recomputation in `load_stl_binary` (mesh.py) to only recompute norms for degenerate triangles. All 1725 tests pass.

- [2026-04-01] feature-agent: Vectorized inner element loop in `compute_body_channel_factored()` with `np.add.at` (replaces Python for-loop over antenna elements). Added input validation to `/api/environment/coverage` route: validates required station fields (lat/lon/freq_mhz), bounds checks, and logs CloudRF API errors. #266 JAX fix was already merged by another agent (#270), but inner loop was still Python. All 1663 tests pass with JAX.

- [2026-04-01] feature-agent: Added compliance heatmap to Analysis panel. New `/api/compliance/heatmap` route exposes the existing `compliance_heatmap()` function (2D frequency x power compliance map). Frontend renders an interactive canvas-based heatmap with green (compliant) / red (exceeded) coloring, white compliance boundary line, crosshair showing current operating point, and hover tooltips with margin values. Legend shows color coding. Backend already had the function (tested in test_compliance.py), just needed the API route. All 1598 tests pass, frontend builds clean.

- [2026-04-01] feature-agent: Three improvements shipped: (1) Fixed race condition in environment store where rapid location changes caused stale fetch responses to overwrite fresh ones. Added AbortController to all environment fetch functions (fetchOSM, fetchGeoJSON, fetchTilesForRT, geocodeAndFetch). Previous fetch is aborted when a new one starts. (2) Added distance sweep to Analysis panel. Uses client-side 1/r^2 far-field approximation to show compliance margin vs antenna-body distance. Shows minimum compliant distance. No backend changes needed. (3) Fixed share link silent failure: corrupted or invalid share links now show a warning notification instead of silently returning empty state. Rebuilt frontend. All 1597+ tests pass.

- [2026-04-01] feature-agent: Fixed two Sentry bugs and shipped MIMO performance and UX improvements. (1) #251: geocode route crashed with KeyError on malformed Google API response. Added defensive validation and switched frontend geocodeAndFetch to fetchWithRetry. (2) #252: MIMO compute 502 for 6 users. Added compute_body_channel_factored that computes Fresnel for unique k_hat directions only (not all N*M_element pairs), giving ~4x speedup for 4x4 UPA. Applied to both build_user_channels and compute_mimo_scene_with_bodies. Increased Gunicorn timeout from 300s to 600s. (3) Added 120s frontend timeout for MIMO compute with actionable message ("Try reducing users or using MRT precoder"). (4) Rebuilt frontend bundle. All 1637 tests pass, golden tests pass. Tagged v0.8.12. PRs #253, #254.

- [2026-03-31] feature-agent: Four fixes shipped: (1) Fixed Sionna LOS detection bug where first filtered path was assumed to be shortest-delay (LOS). Now uses np.argmin(tau) to correctly identify the actual shortest-delay path per element. (2) Fixed DiffeRT degenerate path handling where zero-length paths (TX=RX coincidence or fully padded geometry) produced undefined k_hat and spurious high amplitude from division by ~0 distance. Now filters them out before amplitude computation. (3) Added parseJsonHeader() utility to frontend, replacing 3 unsafe JSON.parse(header) calls in client.ts, basestations.ts, and mimo.ts that would crash on malformed X-Stats headers. (4) Fixed thread-safety race condition on module-level _last_timings dict in engine.py, now protected by _timings_lock. Rebuilt frontend. All 1670 tests pass.

- [2026-03-31] code-reviewer: Focus area: compliance checks and limits. Reviewed compliance/__init__.py (ICNIRP 2020 limits, evaluate_compliance, power_sweep, frequency_sweep, compliance_heatmap, link_budget_compliance), compliance/__main__.py (CLI), result.py (compliant_sab, evaluate_compliance method), viewer/routes/compute.py (_build_stats_response), viewer/routes/analysis.py (compliance API routes), and CompliancePanel.tsx frontend. Verified all ICNIRP limit values (Tables 2, 5, 6). Found 1 bug: _build_stats_response passed sab_4cm2=None to evaluate_compliance when sab_averaged was None, silently omitting the most important compliance check. Now falls back to raw sab peak (conservative), matching DosimetryResult.evaluate_compliance(). Added regression test. All 1947 tests pass. PR #234.

- [2026-03-31] feature-agent: Four improvements shipped in one PR: (1) Cached body geometry hash on BodyMesh (computed once at construction, reused by engine averaging cache) to avoid O(M) SHA256 recomputation on every compute call. (2) Added 429 (rate limit) to frontend retry set alongside 502/503/504, using existing Retry-After header parsing. (3) Fixed CompliancePanel React anti-pattern where powerDbm was read via getState() inside an IIFE during render, causing stale "Max TX power" values. Now uses proper Zustand selector for reactive updates. (4) Added binary array bounds validation in API client to prevent silent Float32Array corruption from malformed server responses. Rebuilt frontend. All 1698 tests pass.

- [2026-03-31] code-reviewer: Focus area: tissue properties and Fresnel. Thorough review of all tissue/Fresnel code: fresnel.py, cole_cole.py, dielectric.py, database.py, _base.py (fresnel_weights), level3-6 kernels, coherent/fresnel_operator.py, coherent/body_channel.py, viewer/compute.py (resolve_skin_model), and analysis.py (tissue spectrum route). Verified all equations against monograph_v2.tex (Fresnel T_s/T_p, T0, amplitude t_s/t_p, depth coupling sqrt(sigma/4alpha), G_tilde definition, Cole-Cole model). All physics correct. Found one minor issue: analysis.py hardcoded eps_0 = 8.8541878128e-12 instead of importing EPS_0 from aegis.constants. Fixed in PR #230. All 1698 tests pass.

- [2026-03-31] code-reviewer: Focus area: test coverage gaps and edge cases. Reviewed all recently changed files (engine.py, result.py, coherent/_accumulate.py, compliance/, viewer routes, channel/generator.py). No bugs found. Added 29 tests covering: evaluate_compliance() fallback paths (sab_averaged=None, sinc_averaged=None), serialization edge cases (unknown keys, None arrays, complex eigenvalues), coherent_sinc edge cases (single path, zero precoder, multi-element), body cache key translation invariance, _build_result NaN/Inf rejection, _sanitize_for_json with NaN/Inf/nested structures, scale() with sab_1cm2_averaged, accumulate_by_element edge cases, field_channel element_index validation. All 1699 tests pass. PR #229.

- [2026-03-31] feature-agent: Four improvements shipped in one PR: (1) Thread-safe G-matrix cache in engine.py, added threading.Lock to protect the class-level OrderedDict LRU cache from concurrent Flask/precompute thread races. (2) Heatmap rendering optimization in BodyMeshInstance.tsx, replaced per-vertex setXYZ() calls with direct typed array writes (eliminates ~45K function calls for a 15K face mesh). (3) Fixed /api/system endpoint blocking 100ms per call by changing psutil.cpu_percent(interval=0.1) to interval=None (non-blocking). (4) Fixed sigma floor inconsistency in spatial kernel diffraction path (0.0 -> 1e-20, matching level6 kernel) to avoid sqrt(0) gradient issues with JAX autodiff. Rebuilt frontend. All 1670 tests pass.

- [2026-03-31] code-reviewer: Focus area: compliance checks and limits. Verified all ICNIRP 2020 limit values against monograph (all correct). Found and fixed 2 bugs: (1) DosimetryResult.compliant_sab property crashed with ValueError when freq_hz was outside ICNIRP range (e.g. 3.5 GHz sub-6 GHz), now returns None. (2) _build_stats_response in compute route skipped sinc_local compliance check when sinc_averaged was None but raw sinc existed, now falls back to raw sinc peak matching DosimetryResult.evaluate_compliance() behavior. Added 2 regression tests. All 1671 tests pass. PR #224.

- [2026-03-31] feature-agent: Fixed Sentry #217 (HTTP 502 in fetchOSM). Added `fetchWithRetry` utility to frontend API client that retries up to 2 times with exponential backoff on transient HTTP errors (502/503/504). Respects Retry-After header. Applied to all fetch calls across client.ts, mimo.ts, basestations.ts, environment store, terrain store. Also fixed inconsistent error extraction in basestations API (now reads backend JSON errors). Separately: optimized spatial averaging NumPy fallback by skipping unnecessary sqrt (squared distances give same argsort order), fixed Sidebar re-render cascade (was subscribing to entire UIStore), memoized FocusPointMarker ring geometry. Tagged v0.8.10. PRs #221, #222. All 1640 tests pass.

- [2026-03-31] code-reviewer: Focus area: error handling and input validation. Fixed 3 categories of missing validation: (1) power_dbm not validated as numeric in all 3 RT routes (DiffeRT, Sionna, voxel), (2) MIMO level param accepted incoherent levels 0-6 in mimo.py and basestations.py routes, (3) lat/lon not validated as numeric in from-voxels and geojson environment routes. Also fixed compute_sab test that failed with JAX installed (test asserted np.ndarray but function documents returning raw backend array). Added 4 new tests. All 1943 tests pass. PR #218.

- [2026-03-31] feature-agent: Three PRs shipped. (1) Vectorized CSV export from per-row Python loop to numpy.savetxt, 5-50x faster for large meshes (PR #214). (2) Extracted duplicated _accumulate_by_element functions from field_channel.py and body_channel.py into shared coherent/_accumulate.py module (PR #215). (3) Added PanelErrorBoundary component wrapping all 13 sidebar panels so one panel crash no longer takes down the entire sidebar; includes Sentry integration and retry button (PR #216). All 1640 tests pass.

- [2026-03-31] feature-agent: Added keyboard shortcut help modal (? key or toolbar button opens overlay showing all shortcuts: WASD camera, arrow nudge, Delete remove, Tab/1-9 MIMO user select). Added MIMO precoder fallback notification when auto-switching from ZF/MMSE to MRT (previously silent). Fixed averaging cache key to include centroid positions (previously only hashed areas, which could theoretically collide for different phantoms with identical area distributions). Rebuilt frontend bundle. All 1640 tests pass.

- [2026-03-31] feature-agent: Fixed silent compute timeout (60s abort showed no notification), stale sweep data persisting after parameter changes, silent screenshot export failures, and added NaN/Inf validation in engine._build_result. Also hardened JSON serialization across all X-Stats headers (inf/nan would crash JSON.parse in frontend). Added frequency validation and non-negative path power clamping in channel generator. All 1905 tests pass.

- [2026-04-03] feature-agent: Fixed Sentry #352 and 8 other bugs across backend and frontend. (1) Sentry #352: frontend fell through to computeSionnaEnvRT when no environment mesh was loaded, causing guaranteed 400. Added `has_env_mesh` capability flag to /api/config, guarded frontend with user-facing warning. Also re-fetch caps after cache clear. (2) Stale RT paths after cache clear: `_last_rt_paths` was not cleared in `_handle_clear_cache`, so tilt_power optimizer could use paths from previous scene. (3) power_dbm upper-bound validation: main compute route only checked minimum, RT routes had no range validation at all. Added 0-100 dBm bounds to all 5 compute endpoints. (4) tilt_power power clip at 90: was copy-pasted from tilt angle clip (0-90 degrees), should be 0-100 dBm. (5) tilt_power cos_inc recomputed 3x per step: normals and k_hat are constant across iterations, cached in setup() for ~3x speedup per step. (6) LSP heatmap resolution=0 accepted: added lower bound (max 1). (7) sar_wb stuck after frequency cross: was added below 6 GHz but never removed when going back above, producing misleading 0 values. (8) sab could be deselected in quantity toggle, breaking colormap fallback. (9) Optimize history unbounded: capped at 200 entries. All 2293 tests pass, TS compiles clean, frontend builds.

- [2026-04-03] code-reviewer: Focus area: kernels and physics correctness. Found and fixed compute_rho formula bug: was computing h^H Q h / (||h||^2 * lambda_max) instead of h^T Q h* / (||h||^2 * lambda_max) per monograph eq:rho-def. The h^T Q h* form gives the actual MRT absorbed power (x_MRT = sqrt(P) h*/||h||, so P_abs = x^H Q x = P h^T Q h* / ||h||^2). For complex h with complex Q, the old formula gave rho=1.0 for h=v_1 but the true MRT P_abs gives rho=0.845 -- a 15% error. Fixed compute_rho, updated two tests that used h=v_1 (should be h=conj(v_1)), added regression test verifying rho matches actual P_abs_MRT. All 2335 tests pass. Full kernel review (levels 0-8, spatial, body_channel, fresnel_operator, ECBF solver, exposure operator, Fresnel core, T0, depth coupling) found no other physics bugs.

- [2026-04-03] feature-agent: Fixed ECBF solver, exposure operator, MIMO compliance, and UI state bugs. (1) ECBF bisection: lambda bracket expansion >1e20 now returns fallback directly instead of passing bad bracket to _bisect (was silently falling through). (2) Bisection convergence: switched from absolute to relative tolerance for large lambda ranges. (3) eigendecompose_Q: replaced argsort[::-1] with flip() for JAX jit traceability ([::-1] creates non-traceable DynamicSlice). (4) MIMO summary compliance: used ICNIRP evaluate_compliance result when available, falls back to power-budget check only when compliance data unavailable (was always using ad-hoc power check, inconsistent with per-user stats). (5) Simulation store: setCurvature(false) now auto-disables diffraction (was leaving inconsistent curvature=false + diffraction=true state). All 2293 tests pass, TS compiles clean.

- [2026-04-03] code-reviewer: Focus area: compliance checks and limits. Found incomplete T0 fix: tilt_power.py had T0 parameter added but loop.py and optimize route never passed it through, so T0 always defaulted to 1.0 (no correction). Fixed loop.py to pass T0 from config, and optimize route to read T0 from cached dosimetry stats. Core compliance module (icnirp_limits, evaluate_compliance, sweeps, heatmap, link_budget) verified correct against ICNIRP 2020 Tables 2/5/6. All 2320 tests pass.

- [2026-04-03] feature-agent: Fixed GPU memory leaks and correctness bugs. (1) Added geometry/texture dispose effects to Antenna, SceneGeometry, HullMesh, LSPHeatmap (leaked GPU memory on every config/data change). (2) Added dispose on replacement in scene store setBodyGeometry and MIMO store setUserBodyGeometry. (3) Fixed bool("false") -> True in compute route correction flags with _parse_bool helper. (4) Added finally block to useOptimization so UI cannot freeze in "running" state on abnormal stream end. (5) Switched computeWithInlineMesh from raw fetch to fetchWithRetry. All 2292 tests pass, TS compiles clean.

- [2026-04-03] feature-agent: Fixed 4 remaining bugs. (1) Optimize route session collision: all sessionless clients shared a single "default" cancel key, so concurrent optimizations could cancel each other. Now generates UUID per session. (2) Compliance link_budget T0 fallback: bare except Exception silently swallowed corrupt DB or bad frequency errors. Now distinguishes ImportError from unexpected errors and logs warning with traceback. (3) Coverage store dropped techIndices: binary decoder parsed technology indices correctly but the store discarded them. Now stored as siteTechIndices. (4) usePoseExtract used deprecated boneTransform (renamed to applyBoneTransform in Three.js r137), masked by `as any` cast. All 2319 tests pass, TypeScript builds clean.

- [2026-04-03] feature-agent: Fixed 3 remaining issues in coverage globe and compute route. (1) Coverage API client used bare fetch() instead of fetchWithRetry() -- only endpoint without retry, causing permanent failure on transient 502/503/504. (2) Coverage store fetch permanently stuck after error (loaded||loading guard prevented re-fetch). Added retry() action and retry button in CoverageHud. (3) Inline mesh compute route accepted unbounded n_triangles from header, risking OOM. Added cap at 500,000. All 2319 tests pass, TS compiles clean.

- [2026-04-03] code-reviewer: Focus area: viewer backend routes and API. Fixed 3 bugs: (1) optimize.py tilt_power mode read `_last_dosimetry_result` from `cache` dict but compute route stores it in `app.config`, so tilt_power always failed with "No dosimetry result cached". (2) compute.py `generate_lsp_heatmap` imported non-existent `get_config` from config module, crashing `/api/lsp-heatmap` with ImportError. (3) CSP `connect-src`/`img-src` missing Cesium Ion domains, blocking the new CesiumGlobe from fetching tiles in production. Added regression test for tilt_power cache location. 2278 fast tests pass. PR #336.

- [2026-04-03] code-reviewer: Focus area: kernels and physics correctness. Fixed missing T0 Fresnel transmission factor in tilt_power optimizer `_evaluate` (was overestimating S_ab by ~2.3x for skin at 28 GHz, causing overly conservative power limits). Added regression test. Also noted: tilt_power mode unreachable via /api/optimize (DosimetryResult has no `_paths` attribute, so `getattr(last_result, "_paths", None)` always returns None) -- not fixed, appears to be unfinished feature integration. All kernels (levels 0-8), coherent body channel, exposure operator, ECBF solver, spatial kernel, tissue/Fresnel, channel generator, and other optim modules reviewed with no further physics bugs found. 2319 tests pass.

- [2026-04-13] code-reviewer: Focus area: geometry and mesh operations. Reviewed all 9 files in `src/aegis/geometry/` (mesh.py, averaging.py, occlusion.py, directivity.py, projected_area.py, skeleton.py, cauchy.py, parametric.py, device_offset.py). Verified: STL binary loader (vectorized reads, zero-norm fallback correct), BodyMesh construction (degenerate triangle fallback [0,0,1] correct, normalization correct), icosphere/cylinder winding orders (all outward), spatial averaging matrix (Numba and NumPy paths produce identical results, row-stochastic invariant maintained), BVH construction (median-split, argpartition correct), Moller-Trumbore ray-triangle intersection (standard implementation), ambient occlusion (origin eps scaling, self-intersection avoidance correct), tangent frame construction (vectorized batch path and single-triangle path both correct), SH fitting (new/old SciPy sph_harm API wrapper correctly swaps theta/phi argument order), projected area (chunked BLAS, ReLU sign convention matches monograph), GltfSkeleton FK+LBS (topological order correct, TRS composition T*R*S matches glTF spec, skin_matrices einsum correct), device_offset face detection heuristic. Also verified kernel usage of geometry data: dot product sign convention `ReLU[n_hat . (-k_hat)]` correct everywhere, array shapes/broadcasting correct, degenerate triangles harmless (zero area -> zero contribution to p_abs). Found 1 bug: `DosimetryResult.__repr__` crashed with ValueError on empty sab (called `peak_sab` unconditionally). Fixed in PR #458. 508 geometry tests + 2370 full suite pass.

- [2026-04-13] feature-agent: Shipped viewer bug fixes and frontend performance improvements. (1) **Basestations stale cache key**: `_handle_basestations_compute` and `_handle_basestations_compute_mimo` used `cache.get("body")` (legacy backward-compat alias) instead of `cache.get("bodies", {}).get(body_name)`. This meant base station dosimetry always used the default phantom, ignoring `body_name` parameter. Now uses the same `bodies` dict lookup as all other compute routes. (2) **Flask secret key**: Replaced hardcoded fallback `"dev-secret-key-change-me"` with `os.urandom(32)` to prevent session cookie forgery when `FLASK_SECRET_KEY` env var is unset. (3) **GLB phantom caching**: Added `conditional=True, max_age=3600` to `send_file` for GLB phantoms (10+ MB files re-downloaded every page load), plus path containment check via `is_relative_to`. (4) **IcosahedronGeometry detail 8 -> 5**: Radiation pattern meshes had 655K vertices (detail=8). Reduced to 10K vertices (detail=5), capped at 6. Visually identical, 65x fewer vertices. AntennaArray was ignoring config and hardcoding detail=8. (5) **Per-frame Vector3 allocations**: FollowCamera and usePhysics created `new THREE.Vector3()` every animation frame. Moved to refs to eliminate GC pressure. (6) **React.memo on scene components**: Wrapped PanelAntenna, AntennaArray, SmartphoneModel in `memo()` to prevent unnecessary re-renders in base station scenes with hundreds of antennas. (7) **Tiles count caching**: `/api/config` was running `glob("*.glb")` filesystem scan on every call. Now cached after first computation. All 2328 tests pass, TS compiles clean, frontend builds.

- [2026-04-13] feature-agent: Spatial kernel auto-batching. `spatial_kernel()` in `kernels/spatial.py` now auto-chunks over paths (N dimension) when M*N exceeds 50M elements (~400 MB per intermediate). Prevents OOM on large scenes (e.g. Duke 56k triangles x 1000+ stochastic paths). Result is mathematically identical (verified to machine epsilon). JAX path bypasses batching. Also cached `gltf_bodies` list at startup in `_preload_bodies()` so `/api/config` avoids re-globbing per request.

- [2026-04-13] code-reviewer: Focus area: error handling and input validation. Found 3 bugs where non-finite inputs (NaN/inf) bypassed validation. (1) `_validate_freq()` in compliance module accepted NaN because `NaN < 100e3` is False in IEEE 754, causing `icnirp_limits()` to return NaN `sinc_local`. Fixed with `math.isfinite()` guard. (2) `body_mass=inf` passed the `<= 0` check in `engine.compute()`, producing `SAR = P_abs / inf = 0` (silently safe). Fixed with `np.isfinite()` guard. (3) `T0(n_tilde=-1+0j)` raised unhandled `ZeroDivisionError` in public API. Fixed with denominator check. Also reviewed: all 9 kernels (levels 0-8), Fresnel coefficients, geometry modules, coherent ECBF solver, viewer routes, coverage layers. No other bugs found that met confidence threshold.

- [2026-04-11] code-reviewer: Focus area: integration bridges (DiffeRT, Sionna). Found 1 bug: `paths_from_sionna_scene` with `synthetic_array=False` and multiple TX elements silently produces wrong results (JAX path: shape mismatch, garbage psi/k_hat) or crashes (NumPy path: IndexError). Both the JAX and NumPy paths hardcode `tx_idx=0` and index angles/CIR assuming the synthetic array tensor layout. The parameter is user-accessible via viewer RT config and Modal tracers. Added guard that raises `NotImplementedError` for this unsupported configuration. Also reviewed: DiffeRT `paths_from_differt` (FSPL, k_hat extraction, degenerate path filtering, polarisation tracking all correct), `_accumulate.py` sort+slice approach (correct), `cloudrf.py` API client (clean), viewer `compute.py` exposure mode reduction (correct), `raytracer.py` voxel meshing (clean). PR pending.

- [2026-04-13] code-reviewer: Focus area: test coverage gaps and edge cases. Found 1 bug: `ComplianceCheck.margin_db` and standalone `margin_db()` crashed with `ValueError: math domain error` when `limit=0` and `value>0` because `math.log10(0)` is undefined. Fixed to return `-inf` (maximally non-compliant). PR #442 merged. Also reviewed: spatial_kernel q broadcasting (correct, false alarm), Fresnel coefficient denominators at grazing incidence (safe for lossy tissue), engine.compute with n_paths=0 at levels 2-6 (correctly returns zero sab), coherent pipeline edge cases (well-tested). Verified all 1844 tests pass.

- [2026-04-13] code-reviewer: Focus area: kernels and physics correctness (scheduled run). Reviewed all 9 incoherent kernels (level0-level6, spatial.py, _base.py), both coherent kernels (level7, level8), body_channel.py (regular + factored), exposure_operator.py, ecbf.py (post-fix version with power-slack and correct infeasibility), fresnel_operator.py, _accumulate.py, fresnel.py, and the engine dispatch. **No physics bugs found.** All equations verified against monograph: T0, Fresnel core (T=1-|r|^2), depth coupling sqrt(sigma/(4*alpha)), Q einsum, rho formula, ECBF solver, curvature correction, physical GELU. Dimensional analysis correct throughout. Broadcasting/shape analysis correct for all einsums and matrix products. The ICNIRP compliance module (new since last kernel review) has correct limit values per Tables 2/5/6. Noted: curvature_H clamping to non-negative is a deliberate design choice (consistent across all kernels and spatial.py), previously flagged in nightlog as needing Robin's input on whether concave curvature correction should be allowed. Test failures on our branch baseline (test_compute_clusters, test_mimo_checkbox_exists) were already fixed on master. 2415 tests pass on current master.

- [2026-04-13] code-reviewer: Focus area: coherent MIMO and ECBF. Found 1 bug: **ECBF solver violates exposure constraint in power-slack regime**. The solver assumed `||x||^2 = P` always holds at optimality (hardcoded nu=1). When Q is invertible and h is not aligned with smallest eigenvalue directions, the parametric curve's asymptotic P_abs exceeds P_abs_max, causing bisection to find a spurious root in the numerical underflow zone. Result: precoders violating exposure constraint by up to 2-3x. Fix: analytically detect the power-slack regime and use `x = alpha * Q^{-1} h*` scaled to meet exposure limit exactly. PR #438. Also reviewed: field_channel.py (correct), body_channel.py + factored variant (correct), exposure_operator.py eigenvalue ordering (consistent across all call sites), fresnel_operator.py (correct), _accumulate.py sort+slice (correct), precoders.py MRT/ZF/MMSE/ZF+exposure (correct), array_paths.py expansion (correct), channel.py dipole model (correct), compute.py orchestration (correct), paths.py k_hat validation (correct). 2370 tests pass.

- [2026-04-13] feature-agent: Performance and UX improvements across backend and frontend. (1) **O(n^2) spatial dedup -> O(n log n)**: `merge.py:spatial_dedup()` used nested `for i/for j` loops computing pairwise haversine distances. Replaced with `scipy.spatial.cKDTree` using equirectangular projection. For Germany-scale datasets (500K+ rows), this turns a multi-hour operation into seconds. (2) **DataFrame iteration 3-5x speedup**: Replaced `df.iterrows()` in `parquet_io.py:dataframe_to_basestations()` and `adapter.py:load_basestations_from_df()` with pre-extracted numpy column arrays and index-based iteration, avoiding per-row Series construction overhead. (3) **Coverage map center persistence**: Fixed jarring UX where transitioning from 3D photorealistic view back to 2D heatmap would reset the map center to Paris (48.8, 2.3) and zoom to 4. Now preserves `lastCenterRef` as the 2D `defaultCenter` and uses a zoom just below the 3D threshold. (4) **Coverage tooltip technology info**: Added 4G/5G technology display to the hover tooltip using existing `siteTechIndices` and `technologyNames` from the store. (5) **Heatmap cache key fix**: The `getHeatmapData` cache used `${byteLength}-${count}` as key, which could produce stale cache hits for different datasets with matching dimensions. Now uses `ArrayBuffer` identity (`siteLats.buffer`) for exact cache invalidation. (6) **Coverage store fetch guard**: Fixed the fetch guard `if (get().loaded || get().loading)` which prevented retry after a fetch error. Now checks `!get().error` so `fetch()` can be called again after a failure without requiring explicit `retry()`. All 1801 tests pass, TS compiles clean, frontend builds.

- [2026-04-13] code-reviewer: Focus area: error handling and input validation. Found 2 bugs, both fixed in PR #434. (1) **Stale coverage endpoint tests**: 4 of 6 tests in test_coverage_endpoint.py were failing because `_compute_coverage` dropped the `clusters` tier and changed binary records from 10 to 12 bytes (added region + antenna count), but tests were not updated. Another agent had independently fixed the same tests on master, so the PR primarily added the NaN fix. (2) **NaN frequency passes compliance validation**: `_validate_freq` used `freq_hz < min or freq_hz > max` to reject invalid frequencies, but NaN comparisons always return False in IEEE 754, so NaN passed validation and propagated into ICNIRP limit calculations producing garbage. Added `math.isfinite()` guard. Also reviewed: viewer routes (basestations.py, coverage.py, compute.py), core engine, geometry, tissue, compliance module. Other findings noted but below 70% confidence threshold or not reachable through normal code paths.

- [2026-04-13] feature-agent: Improvements shipped. (1) **Click-to-place cumulative travel**: Upgraded useClickToPlace from max-distance-from-start to true cumulative pointer travel tracking between successive move events. This catches slow, small circular orbit drags that the previous approach could miss. (2) **Engine error messages**: freq_hz validation warns when value looks like GHz/MHz instead of Hz. NaN/Inf errors now include triangle count, percentage affected, and common causes. Level 7/8 errors explain what parameters are needed. Mode errors list valid options. (3) **RT state cleanup**: setPathSource now clears lastComputeTiming and computeColdStart when switching away from RT, so stale timing badges and cold-start flags don't persist in the StatusBar.

- [2026-04-12] feature-agent: Compliance ring visualization + production hardening. (1) **Compliance ring in 3D scene**: New `ComplianceRing.tsx` component renders a semi-transparent exclusion zone disc + boundary ring on the ground plane centered on the antenna at the minimum ICNIRP-compliant distance (1/r^2 far-field approximation). Green when compliant, red when not. Distance label floats at ring edge. Togglable in Layers panel, on by default. (2) **Retry jitter**: `fetchWithRetry` now adds +/-10% random jitter to retry delays, preventing thundering herd when many clients retry simultaneously. (3) **Flask secret key warning**: Server logs a warning when using the insecure default `FLASK_SECRET_KEY` outside development mode. (4) **Console.error/warn -> Sentry**: Replaced 5 bare `console.error/warn` calls in HullMesh, PatternBrowserPanel, ScenePanel, and coverage store with proper `Sentry.captureException()` calls. (5) **Type safety**: Fixed `as any` cast in CoverageHud color mode selector (now properly typed union). Typed `coverageLayers.ts` array as `Layer[]` instead of `any[]`. All tests pass (2 pre-existing failures unrelated), TypeScript clean, lint clean, build succeeds.

- [2026-04-12] feature-agent: UX and robustness improvements. (1) **Fixed click-to-place antenna vs orbit drag conflict**: `useClickToPlace` hook now tracks cumulative pointer movement via `onPointerMove`, not just start/end displacement. Orbit gestures that return near their start are no longer misidentified as clicks. This was the most impactful UX bug: every orbit drag could accidentally reposition the antenna. (2) **Fixed unsafe buffer overread in coverage binary decoder**: `decodeSitesBinary()` now validates that `raw.byteLength >= count * 12` before decoding. Previously, a truncated response would silently read past the buffer, producing garbage lat/lon data. (3) **Added `has_coverage` to capabilities API**: `/api/config` now reports whether coverage data (regions.yaml) exists, so the frontend can gate coverage UI before attempting to load. (4) **Added HTTP cache headers to coverage endpoint**: Coverage response now includes `Cache-Control: public, max-age=3600` since the data is stable per session. Prevents redundant re-fetches on page reload. (5) **Eliminated `as any` cast in CoverageHud**: Replaced with proper union type for colorMode. All 1801 tests pass, TypeScript compiles clean, frontend builds.

- [2026-04-12] code-reviewer: Focus area: coherent MIMO and ECBF. Found 1 bug in `solve_ecbf()` (safety-critical): the infeasibility check used `P * lambda_min(Q)` as minimum achievable absorbed power, but when the channel vector h has no component in Q's null space, the true minimum is a weighted average that can be much larger. This caused silent constraint violations >150% -- the solver returned precoders exceeding P_abs_max without warning. Fixed the asymptotic analysis to compute the correct limit and also fixed the `norm < NUMERICAL_FLOOR` fallback (was returning MRT which could also violate constraints). Added regression test. PR #421 merged. Also reviewed: exposure_operator.py (correct), body_channel.py (correct), field_channel.py (correct), _accumulate.py (correct), level7/level8 kernels (correct), mimo/precoders.py (correct), mimo/compute.py (correct), mimo/channel.py (correct), mimo/array_paths.py (correct). Pre-existing test failures: test_compute_clusters (stale test) and test_mimo_checkbox_exists (E2E, needs browser).

- [2026-04-12] feature-agent: Performance and resilience hardening across frontend and backend. (1) **GPU memory leak fix**: `useGltfBody.ts` deep-clones Three.js scene on every body switch but never disposed old clones, leaking geometry and materials on the GPU. Added disposal effect that traverses and cleans up previous clone. (2) **RayPaths memoization**: `RayPaths.tsx` re-computed coordinate conversions on every render, even when `rtPaths` hadn't changed. Wrapped in `useMemo`. (3) **Export panel busy states**: All six export buttons now track individual loading state, disable during requests, and show feedback text ("Exporting...", "Capturing...") to prevent double-clicks. (4) **Vite chunk splitting + hidden sourcemaps**: Split Three.js (1.2 MB), deck.gl (707 KB), and vendor (372 KB) into separate chunks for better caching. Switched sourcemaps from `true` to `'hidden'` so maps are generated for Sentry but not served to end users. (5) **Engine averaging matrix race condition**: `_get_G` released the lock while computing the expensive averaging matrix (KD-tree + ball query), allowing concurrent threads to redundantly compute the same matrix. Added a `threading.Event` sentinel so waiting threads block on the event instead of duplicating work. (6) **Coverage endpoint memory**: Removed redundant `df.copy()` in `_compute_coverage`; the DataFrame from `read_parquet` is already owned. (7) **Simulation settings persistence**: Wrapped simulation store with Zustand `persist` middleware, saving user preferences (mode, freq, power, corrections, skin model, exposure mode, display quantity) to localStorage. Transient data (arrays, stats, compliance, patterns) excluded via `partialize`. Users no longer lose their configuration on page reload. All 2328 non-coverage tests pass; 3 pre-existing coverage endpoint test failures from stale format expectations.

- [2026-04-12] feature-agent: NOTE for next agent: `tests/test_coverage_endpoint.py` has 3 pre-existing failures. `test_compute_clusters` expects a `clusters` key that doesn't exist in the response. `test_compute_sites_binary` expects 10 bytes/record but the format is 12 bytes (region + count fields were added). `test_empty_merged_dir` also references `clusters`. These tests need updating to match the current coverage response format.

- [2026-04-12] feature-agent: Coherent pipeline optimization + viewer hardening. (1) **O(N) scatter-add accumulation**: Replaced O(N log N) sort+slice in `_accumulate_by_element_numpy` with `np.add.at` scatter-add, matching the JAX path's O(N) complexity. Eliminates argsort, searchsorted, and Python for-loop over elements. (2) **Eliminated unnecessary ravel/reshape**: `compute_fresnel_operator` and `compute_body_channel` (both regular and factored) were flattening (M,N) arrays to 1D before calling element-wise functions (`_fresnel_core`, `xi_from_mu`), then reshaping back. Removed 3 ravel/reshape round-trips since these functions accept arbitrary shapes. (3) **Fixed silent reverse geocoding failure**: When reverse geocoding fails for lat/lon input in `/api/basestations/load`, the code silently defaulted `country` to "Belgium" regardless of actual location. Now returns 502 with actionable error message. (4) **Hardened free-space compute power cap**: `/api/compute` used config-based power_dbm limits that could be overridden to absurd values. Now enforces `min(config_max, _MAX_POWER_DBM=100)` consistent with RT routes. (5) **Input validation for parametric body**: `/api/parametric-body` accepted unbounded betas/pose arrays. Added size limits (300/600 elements). (6) **Resilient environment cache**: `EnvironmentCache.__init__` crashed the app at import time if cache directory couldn't be created (e.g. read-only Docker filesystem). Now degrades gracefully with cache disabled. All 2330+ tests pass, lint clean.

- [2026-04-12] code-reviewer: Focus area: compliance checks and limits. Reviewed `src/aegis/compliance/__init__.py` (all ICNIRP 2020 limits correct per Tables 2/5/6, occupational 5x GP ratio verified, frequency thresholds correct), `__main__.py` CLI, `result.py` (compliance_kwargs, evaluate_compliance, scale), all 5 compliance API endpoints in analysis.py, _build_stats_response in compute.py, MIMO compliance in mimo.py. Found 1 bug: `/api/compliance/summary` displayed "FAIL" instead of "N/A" when `overall_pass` was None (sub-6 GHz without SAR data). Fixed in PR #414 with regression test. Also noted latent bug: compliance_heatmap route falls back to sab_1cm2 compared against 4cm2 limit, but never triggered from frontend. All 176 compliance + 46 analysis route tests pass.

- [2026-04-12] code-reviewer: Focus: test coverage gaps and edge cases in recently changed code. Found 3 bugs: (1) `_parse_rt_config` in compute.py and optimize.py crashed with 500 on non-numeric RT config values (max_depth, rays_per_source, etc.) due to bare `int()` calls without try/except. Added `_safe_int` helper. (2) CoverageTooltip.tsx was missing `!siteLons` null guard, causing potential crash. (3) `test_parametric_unknown_model` had stale assertion after PR #404 changed error message. Added 7 regression tests. PR #410 merged. Reviewed: paths.py k_hat validation (solid), compliance_kwargs sinc_wb fix (correct), coverage binary encoding (correct), all analysis/compliance routes (clean), server.py scoped cache (acceptable). 2386 tests pass.

- [2026-04-12] feature-agent: Input validation bounds and placement truncation fix (#407). Added parameter clamping to all backend routes to prevent resource exhaustion: RT params (max_depth [0,10], rays_per_source [100,10M], chunk_size [1,1M], max_paths_per_source [100,10M]), optimize params (max_iters [1,10K], grid_size [1,50], grid_spacing [0.1,500]), basestations (max_workers [1,16], max_distance_m (0,50K]), environment (default_building_height [0.1,500], geometric_error [0.1,1000]), patterns (gain/freq range consistency). **Fixed placement optimization silently stopping** when max_iters < grid_size^2 (e.g. grid_size=9 with default max_iters=50 missed 31 of 81 candidates). Now auto-adjusts max_iters upward. Resolved rebase conflicts with concurrent #404 (kept mode validation from #404, added grid_spacing bounds and truncation fix). 2328 tests pass, 37 optimization tests pass.

- [2026-04-12] feature-agent: Security hardening and input validation across backend and frontend. (1) **Flask secret key**: No longer uses hardcoded default in production; generates random key with warning when FLASK_SECRET_KEY unset and password gate is enabled. (2) **Sentry webhook**: Returns 503 when SENTRY_CLIENT_SECRET not configured, preventing unauthenticated issue creation. (3) **Input validation**: parametric.py now whitelists model_type/gender, bounds-checks betas (max 300, finite), validates pose size; optimize.py caps max_iters to 500, grid_size to 20, validates mode enum; basestations.py adds lat/lon range checks [-90,90]/[-180,180] before reverse geocoding; analysis.py validates scenario strings in all 4 compliance endpoints. (4) **SSE sanitization**: location.py no longer leaks raw exception messages (file paths, module names) to browser. (5) **Coverage**: Removed unnecessary DataFrame .copy(), upgraded error logging from warning to exception. (6) **Frontend fetch consistency**: Replaced 5 raw `fetch()` calls with `fetchWithRetry` in BaseStationsPanel, ScenePanel, StochasticPanel, and optimize API. Added reader cleanup (finally/cancel) to SSE optimization stream. All 1801 tests pass, 224 viewer tests pass, TS compiles clean, frontend builds.

- [2026-04-12] feature-agent: Spatial compliance zones and compliance UX improvements. (1) **Spatial compliance grid** (`spatial_compliance_grid()` in compliance module): New vectorized function computes ICNIRP compliance margins over a lat/lon grid using free-space path loss from loaded base stations. Haversine distance, 3D height-aware, EIRP-weighted frequency, checks all applicable ICNIRP limits (S_ab 4cm2/1cm2, S_inc local/wb). O(n_grid x n_stations) pure NumPy. (2) **`/api/compliance/spatial` endpoint**: POST endpoint that takes base stations from cache + optional bbox/resolution, returns 2D compliance margin grid (lats, lons, margin_db, compliant, sinc). Auto-derives bbox from station extent with padding. (3) **Frontend compliance zone overlay**: deck.gl SolidPolygonLayer on coverage map, toggle in CoverageHud with color legend (green/yellow/orange/red by margin). Fetches from backend on enable. (4) **Per-check margin_db in compliance response**: Each compliance check now includes `margin_db` (dB) alongside ratio. CompliancePanel shows per-check dB margin next to each limit. (5) **Curvature cache increased** from 8 to 32 entries to reduce KD-tree rebuild thrashing for viewers with many body presets. 10 new spatial compliance tests. All 2338 tests pass, lint clean, frontend builds.
- [2026-04-11] (entries older than 7 days pruned)

- [2026-04-12] code-reviewer: Focus area: test coverage gaps and edge cases. Found 2 bugs and 6 stale tests. (1) **DosimetryResult.__repr__ crashes on empty sab**: `repr()` called `peak_sab` unconditionally, which raises ValueError when `sab.size == 0`. Fixed to show "N/A (empty)". (2) **peak_triangle_index missing empty guard**: unlike `peak_sab`, `peak_triangle_index` had no empty-array guard, causing confusing NumPy ValueError instead of a clear message. Added guard matching `peak_sab`. (3) **4 stale test_coverage_endpoint tests**: `_compute_coverage` was refactored from 10-byte to 12-byte binary records (added region + count fields) and removed the `clusters` key, but tests still expected the old format. Fixed all 4 tests. (4) **Stale E2E MIMO test**: MIMO panel moved from HUD overlay to sidebar accordion, but test expected direct checkbox access. Fixed `_enable_mimo` helper to navigate sidebar first. (5) Added 8 new edge-case tests: complex psi round-trip serialization, multi-component power summation, 3-way concatenate with reindex, peak_triangle_index/repr on empty results. Also reviewed: CoverageMap.tsx 3D marker budget applied before spatial filter (design choice, not bug), Uint8 antenna count cap at 255 (matches server-side clip), heatmap cache key by length only (stale risk but rare). 2382 tests pass.

- [2026-04-12] code-reviewer: Focus area: kernels and physics correctness. Verified all six core equations against monograph (T0, depth coupling weight, rho, ECBF optimal precoder, curvature correction, physical GELU) - all match. Verified: curvature_H clamping to non-negative is correct (PO correction only valid for convex surfaces, monograph confirms). Fresnel edge cases at grazing incidence thoroughly tested, 1e-10 guard correct. Level 5 vs level 6 activation function difference (ReLU vs GELU) intentional and consistent with composability analysis. Coherent sinc computation, exposure operator Q, eigendecomposition, and ECBF bisection solver all correct. 1841 tests pass, lint clean. No bugs found. The kernels and coherent pipeline are solid.

- [2026-04-12] code-reviewer: Focus area: test coverage gaps and edge cases. Found 1 bug: `normalize_operator()` in `merge.py` did not handle `pd.NA` (pyarrow string-dtype missing values). Fell through None and float(nan) guards, producing bogus operator name `"<na>"` instead of `"unknown"`. Fixed with `pd.isna()`. Also fixed 4 stale tests in `test_coverage_endpoint.py`: clusters tier was removed from `_compute_coverage` and binary record expanded from 10 to 12 bytes, but tests not updated. Added 6 regression tests (normalize_operator with pd.NA/np.nan/float nan, compliance_kwargs sinc_wb with body/zero-area/no-body). Reviewed 15 recently changed Python source files (paths, result, mesh, compute, coverage, basestations, sionna, accumulate, sentry_webhook, server, config, merge, location, data, compute routes) - all correct. PR #402, 2380 tests pass.

- [2026-04-12] code-reviewer: Focus area: coherent MIMO and ECBF (second pass). Deep review of all files in `src/aegis/coherent/`, coherent kernels (level7/level8), `src/aegis/mimo/` (compute.py, precoders.py, array_paths.py, channel.py, array.py), coherent dispatch in engine.py, and viewer MIMO route. Verified all against monograph summary_paper.tex equations. Checked: phase convention, Fresnel TE/TM basis, depth coupling, exposure operator Q einsum, rho formula, ECBF bisection solver, MRT/ZF/MMSE/ZF-exposure precoders, factored body channel element-major layout (verified stride indexing c::N_center matches expand_paths_to_array output), multi-stream coherent_sinc (handles both vector x and matrix W), compute_multistream_sab consistency with monograph eq:Pabs-MU. No bugs found in coherent code. Fixed 1 remaining stale test: added `test_compute_sites_binary_large` (replacement for removed `test_compute_clusters`). PR #417. 2375 tests pass.

- [2026-04-12] feature-agent: Hardened input validation across backend and frontend. (1) **MIMO route** (`routes/mimo.py`): Array dimensions n_h/n_v now validated >= 1 with max 1024 total elements to prevent resource exhaustion. Element spacing validated positive. Position, broadside, device_offset, device_orientation arrays validated for exactly 3 elements. Frequency capped at 300 GHz. (2) **Location route** (`routes/location.py`): Location and geocode query strings capped at 500 chars. Moved empty-string check before other validation. (3) **useVoxelLoader** (`hooks/useVoxelLoader.ts`): Added AbortController so voxel fetches are cancelled on unmount, preventing stale state updates on unmounted components. Added `signal` parameter to `fetchVoxels()` in client.ts. (4) **Binary parsers** (`stores/environment.ts`, `stores/terrain.ts`): Both `parseEnvironmentBinary` and `parseTerrainBinary` now validate metadata (n_vertices/n_triangles finite and non-negative) and check buffer byte length before creating typed arrays, preventing silent data corruption or RangeError crashes from malformed server responses.

- [2026-04-12] code-reviewer: Focus area: type safety and API contracts. Found 2 bugs, both fixed in PR #428. (1) **Coverage endpoint tests still broken**: Despite previous agents noting and partially fixing coverage test desync, `test_coverage_endpoint.py` still had failures from the 10->12 byte format change and removed `clusters` key. Resolved merge conflicts with concurrent fixes and ensured all 6 coverage tests pass. (2) **Basestations compute mode not validated**: `/api/basestations/compute` read `mode` directly from request params without validating against `{bound, aggregate, spatial}`. Invalid modes like `coherent` or `ecbf` reached `DosimetryEngine.compute()` without a precoder, producing 500 instead of 400. Added validation guard. Also reviewed (no bugs found): CoverageMap.tsx 3D marker filtering and Google Maps event translation (`.lng` -> `.lon` correctly maps Google convention to store convention), coverageLayers.ts indexed accessor pattern, useScenario.ts mode/source validation with VALID_MODES/VALID_SOURCES sets, PropagationPaths validation and serialization, coverage binary decoder (12-byte format consistent frontend/backend), coverage store (null checks correct). 2374 tests pass.

## Do not touch

<!-- Things agents have investigated and confirmed are correct or intentional -->

- Curvature_H negative values in level 5 kernel (intentional per monograph eq. 47)
- Fresnel T_avg > T0 near Brewster angle (physically correct, see physics-findings)

- [2026-04-13] feature-agent: Fixed Sentry #448 and stale tests. (1) **Pattern load 500 fix** (#448): When a user selected a Kathrein pattern from the CloudRF-indexed library, `_load_local` tried to open zip files that are gitignored and not deployed to production. The search endpoint worked because it queries the shipped SQLite index, but loading the actual pattern content failed with an unhandled exception (500). Fix: `_load_local` now checks zip file existence before attempting to read, raising `FileNotFoundError` with a clear message. The route handler catches this and returns 404 instead of 500. Frontend `loadPattern()` now parses the JSON error body from the server and displays "Pattern data not available on this server" in the UI. Added test for the new error path.

- [2026-04-12] code-reviewer: Focus area: compliance checks and limits. Found 1 bug: analysis routes (`/api/compliance/power-sweep`, `/api/compliance/heatmap`, `/api/compliance/frequency-sweep`) produced non-standard JSON containing `Infinity` tokens when compliance margins were infinite (e.g. sub-6 GHz frequencies where S_ab limits don't apply, or zero measured values). JavaScript `JSON.parse()` rejects `Infinity`, so any frontend calling these endpoints with edge-case parameters would get a parse error. Fixed by sanitizing inf/NaN to null before jsonification, consistent with `_json_dumps_safe` used in compute routes. Added 2 regression tests. Also reviewed: all ICNIRP 2020 limit values (Tables 2/5/6) verified correct, `evaluate_compliance` logic correct, `compliance_kwargs` in result.py correct, legacy backward-compat API correct, link budget compliance correct, power/frequency sweep math correct. 180 compliance tests pass.

- [2026-04-13] code-reviewer: Focus area: test coverage gaps and edge cases. Found 1 bug: `test_coverage_endpoint.py` still had stale tests referencing removed `clusters` key and 10-byte binary format (despite multiple previous fix attempts, merge conflicts kept re-introducing the stale version). Resolved by merging master, taking the most complete fix version. Also reviewed: CoverageMap.tsx (3D marker tooltips, event listener cleanup, state management all correct), coverageLayers.ts (heatmap/scatter crossfade opacity math correct, no discontinuity at zoom 9 boundary), useScenario.ts (mode/source validation correct), paths.py (k_hat validation, from_powers, serialization all solid), all kernels and physics (dot product signs, ReLU, Fresnel, division guards all correct), result.py (peak_sab/mean_sab/compliance_kwargs correct), viewer routes (coverage, compute, parametric). No physics bugs found. PR #451 merged. 2373 tests pass.

- [2026-04-13] feature-agent: Two PRs shipped. (1) **Harden useBaseStationsDosimetry** (#463): The base station dosimetry hook was missing safety features that both useDosimetry and useMIMODosimetry already had. Added generation counter (discards stale responses from overlapping computes), 120s timeout with user-facing warning, network error detection via isNetworkError() (shows warning instead of Sentry report), error notification dismissal on success, and global UI computing state sync. (2) **Fix Netherlands and Austria scraper quality** (#464): Netherlands scraper had debug file writes (`Test_*.json` dumped to CWD on every API fetch) and debug print statements left from development. Also fixed O(n^2) pd.concat in both Netherlands `create_df_from_antenna_features` and Austria `get_antennas` with list-collect-then-concat pattern. Austria fix is significant for ~18K+ sites. NOTE: The `_accumulate_by_element_numpy` vectorization was already merged by a parallel agent run. All 1801 tests pass, TypeScript compiles clean, frontend builds.

- [2026-04-13] code-reviewer: Focus area: frontend data flow (Zustand stores, API response handling, loading/error states). Found 2 bugs, both fixed. (1) **test_bug_report_success always fails without GITHUB_TOKEN**: The test for the new `/api/bug-report` endpoint (commit 51f3e84) never set `GITHUB_ISSUES_TOKEN` or `GITHUB_TOKEN` env var, so the token check at line 34-36 of `bugreport.py` returned 503 before the patched GitHub API functions were ever reached. It only passed in CI because GitHub Actions sets `GITHUB_TOKEN` automatically. Fixed by adding `@patch.dict(os.environ, {"GITHUB_ISSUES_TOKEN": "ghp_fake_token_for_test"})`. Also added explicit test for the no-token 503 path. (2) **basestations store `clear()` does not reset `isLoading`/`isComputing`**: When a user clicks "back to globe" during a running base station compute, `clear()` resets all data but leaves `isComputing: true`. On subsequent base station loads, the panel shows a permanent spinner. Fixed by resetting both flags in `clear()`. Also reviewed (no bugs found): `useBaseStationsDosimetry` generation counter and abort handling (correct), `useDosimetry` timeout/abort/generation pattern (correct), `useMIMODosimetry` cleanup and generation tracking (correct), `DataQualityHud` provenance map building (correct after PR #457 fix), `PatternBrowserPanel` load/error states (correct), `notifications.ts` dedup logic (acceptable), `api/client.ts` compute endpoint binary parsing (correct), `api/basestations.ts` compute binary parsing (consistent with client.ts). Noted but not fixed: `basestations.ts` `listBasestations`/`computeBasestations` bypass `handle401()` (UX inconsistency, not data corruption). 2430 tests pass, TypeScript compiles clean.

- [2026-04-13] feature-agent: Deep frontend+backend audit and 10-bug fix PR. Ran comprehensive code audit of all Zustand stores, hooks, and HUD components plus Flask backend routes. Found and fixed: (1) **geocoding stuck loading**: `geocodeAndFetch` AbortError path returned without resetting `geocoding: false`, leaving spinner permanently stuck after source switch. (2) **exportForRT corrupted shared loading flag**: `exportForRT` used the same `loading` field as environment fetches (fetchOSM/fetchTilesForRT/fetchGeoJSON), so export finishing during an environment fetch would clear the fetch spinner. Removed loading state mutation from exportForRT. (3) **Persisted displayQuantity mismatch on reload**: `displayQuantity` was persisted but `enabledQuantities` was not. After reload, a persisted value like `sab_1cm2` could refer to a key not in the default set, causing components to render with no data. Added `merge` handler to zustand persist config that validates against defaults. (4) **Coverage retry cleanup**: Simplified retry() to only clear `loaded` and `error`, letting `fetch()` manage its own `loading` guard. (5) **lspHeatmapLoading never cleared on abort**: The finally block skipped clearing when aborted, leaving permanent shimmer animation. Fixed to always clear. (6) **tissue_sigma.toFixed() crash**: ParametersPanel guarded `tissue_eps_r` but not `tissue_sigma`, so if backend returned eps_r without sigma the component crashed. Added null guard. (7) **Blob URL memory leak**: Coverage map blob URLs were never revoked. Added `URL.revokeObjectURL()` in `setCoverageUrl` and `clear()`. (8) **MIMO Add User at origin**: Panel placed new users at `[N, 0, 0]` ignoring body offset. Fixed to read `bodyOffset` from simulation store. (9) **MIMO precoder fallback dead code**: `removeUser` had a fallback that could never trigger (removing users makes K smaller). Moved the fallback logic to `addUser` where it matters (adding users can make K > M). (10) **lsp_heatmap input validation**: Backend route used bare `int()`/`float()`/`tuple()` on user input. Added explicit validation with error messages. All 1842 tests pass, TypeScript compiles clean, frontend builds.

- [2026-04-13] code-reviewer: Focus area: integration bridges (DiffeRT, Sionna) and ray tracing pipeline. No bugs found. Reviewed: `paths_from_differt` full pipeline (FSPL formula, Fresnel reflection TE/TM tracking, propagation phase, degenerate path filtering, padded path handling, scene-level array protection for material_indices/normals), `paths_from_sionna_scene` NumPy and JAX paths (CIR shape handling, synthetic_array tiling, k_hat sign convention, dual-polarized RX probe, LOS detection via argmin-tau), `_extract_path_viz` (viz-only, not physics), `compute_paths_differt` viewer bridge (reflection_loss_per_order, chunk_size iteration, mask handling), `round_triangle_scene` greedy mesher, Modal proxy DiffeRT/Sionna tracers, CloudRF client and MSI-to-pattern conversion, all RT compute routes (scene path validation, input parsing, error handling). Deep analysis of `paths_from_differt` internals (last-segment detection via argmax, total_length summing, valid mask filtering) confirmed correctness. 84 integration tests pass, 2429 total non-E2E tests pass.

- [2026-04-14] code-reviewer: Focus area: frontend data flow (Zustand store updates, API response handling, loading/error states). No bugs found. Thorough review of all Zustand stores and dosimetry hooks. Verified basestations clear(), environment store abort paths, coverage store, LSP heatmap, simulation persist merge, MIMO user management, generation counters, BugReporter state machine, PatternBrowserPanel. 2430 tests pass, lint clean.

- [2026-04-14] feature-agent: Six-bug fix across backend and frontend. Ran comprehensive audit via three parallel exploration agents (frontend stores/hooks/components, backend routes, physics engine/kernels). Found and fixed: (1) **JAX array serialization in DosimetryResult.to_dict()**: `isinstance(val, np.ndarray)` returns False for JAX arrays, causing `TypeError: Object of type ArrayImpl is not JSON serializable` when using JAX backend. Fixed by checking `hasattr(val, "tolist") and hasattr(val, "dtype")` and wrapping with `np.asarray()` for complex arrays. (2) **JAX array chunking in spatial kernel**: `isinstance(q, np.ndarray)` in the chunked path failed for JAX arrays. Fixed with `hasattr(q, "__getitem__") and np.ndim(q) > 0`. Note: currently unreachable because JAX takes the fast path, but fixed for correctness. (3) **Environment store abort leaves loading stuck**: `fetchGeoJSON`, `fetchOSM`, and `fetchTilesForRT` all returned early on AbortError without clearing `loading: true`. If a user switches environment source mid-fetch, the loading spinner would persist until the next successful fetch. Fixed all three to clear loading on abort. (4) **AnalysisPanel crash on missing compliance checks**: `stats.compliance?.checks.find(...)` crashes if `checks` is unexpectedly undefined (e.g., backend returns compliance without checks array). Added `?.` before `.find()` in two locations. (5) **Basestations origin stale cache**: When loading base stations without explicit coordinates (e.g., by country/region name only), `basestations_origin` was not updated, leaving stale coordinates from a prior load. Added fallback that computes centroid from loaded stations, and clears origin when no stations are loaded. All 2389 tests pass, TypeScript compiles clean, frontend builds.

- [2026-04-14] code-reviewer: Focus area: viewer feature completeness, user flow tracing, dead code paths. Found 1 bug, fixed. **Tour restart loses state on page refresh**: `startTour()` set `tourCompleted: false` in Zustand memory but did not clear `localStorage('aegis-tour-completed')`. After a user completed the tour and clicked the toolbar play button to restart, the restart would be lost on page refresh because the localStorage value still read `'1'`. Fixed by adding `localStorage.removeItem('aegis-tour-completed')` in `startTour()`. Also performed comprehensive end-to-end review of: all 15+ Flask route handler files (identified ~10 backend routes with no frontend caller, including `/api/basestations/compute_mimo`, `/api/environment/combine`, `/api/environment/from-voxels`, `/api/environment/materials`, `/api/environment/mesh`, `/api/patterns/build-index`, `/api/patterns/manufacturers`, `/api/parametric-body` - these are API-only or incomplete integrations, not bugs), 4 unused API client functions (`fetchLevels`, `fetchBodyInfo`, `fetchComplianceLimits`, `fetchTileFile` - dead code), all Zustand stores and their persist/merge behavior, all dosimetry hooks (useDosimetry, useMIMODosimetry, useBaseStationsDosimetry, useOptimization), scenario loading flow (useScenario, ScenarioDropdown with CloudRF presets), coverage globe transition to base stations flow, guided tour and welcome overlay integration, BugReporter and AnnotationCanvas, ExportPanel download flows. No physics bugs. 2430 tests pass, TypeScript compiles clean, frontend builds.

- [2026-04-14] feature-agent: Frontend UX fixes and dead code cleanup. Found and fixed 4 issues: (1) **Compliance zone silent failure**: `fetchComplianceZone` in coverage store had bare `console.error` on API failure. User clicked "ICNIRP compliance zones", saw "Computing...", then nothing on error. Fixed to show notification, reset `complianceZoneEnabled`, and report to Sentry. (2) **Coverage map fetch silent failure**: BaseStationsPanel `fetchCoverage` silently caught errors with no user feedback. Added loading state (`coverageLoading`), user notification on failure, and unchecks toggle on error. (3) **Tour active during reset**: Clicking "aegis" wordmark to reset showed welcome overlay but didn't dismiss an active tour, leaving tour state inconsistent. Added `dismissTour()` call. (4) **Dead code removal**: Removed 6 unused API functions (`fetchLevels`, `fetchBodyInfo`, `fetchScenes`, `fetchComplianceLimits`, `fetchTileFile`, `listBasestations`) and 2 unused type interfaces (`LevelInfo`, `ICNIRPLimits`). All 2389 tests pass, TypeScript compiles clean, frontend builds.

- [2026-04-14] code-reviewer: Focus area: 3D scene Three.js resource lifecycle, geometry disposal, camera state, rendering. Found 1 bug, fixed. **Intermediate BufferGeometry leak in SceneGeometry and HullMesh**: When `faceColors` is present, both `SceneGeometry.tsx` and `HullMesh.tsx` call `geo.toNonIndexed()` which creates a new geometry, but the original indexed `geo` was never disposed. Only the returned `nonIndexed` geometry was tracked by the `useEffect` cleanup. This leaks GPU memory every time scene geometry or hull mesh data changes with face colors (e.g. when switching Sionna scenes or reloading hull view). Fixed by adding `geo.dispose()` immediately after `toNonIndexed()`. Also performed comprehensive review of: all 26 scene components (SceneRoot, BodyMeshInstance, AnimatedBody, Antenna, AntennaArray, PanelAntenna, FocusPointMarker, HullMesh, EnvironmentTerrain, EnvironmentOSM, Environment, VoxelField, LSPHeatmap, CoverageMap, RayPaths, ClusterPaths, DistanceLine, ComplianceRing, BaseStationMarkers, SmartphoneModel, PeakIndicator, ErrorBoundary, SceneGeometry, FollowCamera, BodyMesh, coverageLayers), all Three.js hooks (useBodyLoader, useGltfBody, useVoxelLoader, useMIMODosimetry, useClickToPlace, usePhysics), all Zustand stores holding Three.js state (scene.ts bodyGeometry, mimo.ts user bodyGeometries, terrain.ts, environment.ts). Disposal patterns verified correct in: LSPHeatmap (texture disposed on recompute and unmount), ClusterPaths SubpathLines (geometry disposed), DistanceLine (texture disposed via prevTextureRef pattern), Antenna (geometry disposed via prevGeoRef), AntennaArray (geometry disposed on recompute), EnvironmentTerrain (geometry disposed), EnvironmentOSM (geometry disposed), useGltfBody (traversal disposal of all geometries and materials), scene.ts setBodyGeometry (disposes old before setting new), mimo.ts removeUser/setUserBodyGeometry/reset (all dispose correctly). Camera state management (CameraSyncer, CameraController, CameraInitializer, FollowCamera, OrbitControls) all correct. No physics bugs. 2430 tests pass, TypeScript compiles clean, frontend builds.

- [2026-04-14] code-reviewer: Focus area: integration bridges (DiffeRT, Sionna) and ray tracing pipeline. Found 1 bug, fixed. **CloudRF template presets stored EIRP in powerDbm, causing gain double-counting**: `cloudrf_template_to_scenario` computed `power_dbm = 10*log10(P_W*1000) + gain_dBi` (EIRP) but the AEGIS engine treats `powerDbm` as conducted TX power and applies antenna gain separately. All 7 CloudRF-derived scenario presets overestimated dosimetry by the antenna gain (up to 18 dB for Starlink). Fixed the converter to store conducted power and regenerated all preset JSONs. Also reviewed (no bugs found): `paths_from_differt` full pipeline (FSPL, Fresnel TE/TM tracking, phase, degenerate filtering, padded paths, material lookup), `paths_from_sionna_scene` NumPy and JAX paths (CIR extraction, k_hat sign convention, psi scaling, LOS detection, synthetic array guard), `_extract_path_viz` (viz-only), `_handle_validate_sinc` (comparing power density vs received power but it is a debug endpoint), CloudRF client, all RT viewer routes, compute binary response protocol (offset=bytes, length=elements, correctly consumed by frontend). 1551 tests pass (excluding E2E/display-dependent).

- [2026-04-14] feature-agent: Backend and frontend robustness fixes across 6 files. Ran three parallel exploration agents (backend routes, frontend stores/components, physics engine). Found and fixed 8 issues: (1) **Spatial compliance NaN breaks frontend JSON.parse**: `/api/compliance/spatial` used `np.where(np.isinf(...))` but did not handle NaN values in `margin_db` or `sinc` arrays. `jsonify()` serializes NaN as non-standard JSON token that `JSON.parse` rejects, crashing the coverage map overlay. Fixed by adding NaN-to-zero clamping before inf clamping. (2) **Location SSE error emitted twice**: Pipeline errors emitted `event: progress` with the ERROR line before checking `startswith("ERROR:")`, so the frontend received the error as both a progress event and an error event. Reordered to check error first. (3) **Location SSE voxel load failure hangs UI**: When voxel loading failed in the SSE stream, no `event: done` was emitted, so the frontend spinner hung indefinitely. Added `event: done` with empty metadata after the error event. (4) **Spatial compliance missing input validation**: `receiver_height_m` was cast with bare `float()` (unhandled ValueError on bad input → 500), and `scenario` was not validated against the allowed set (silently defaulted to general_public for any garbage input). Added try/except and `_VALID_SCENARIOS` check, matching the pattern in all other compliance endpoints. (5) **Zero-path RT exports serve stale data**: All 4 RT endpoints (`/api/compute/rt`, Sionna RT, Sionna env RT, voxel RT) returned early on zero paths without clearing the export cache. Subsequent CSV/JSON/NPZ exports served the previous non-zero computation while the UI showed zeroes. Fixed by clearing all 5 export cache keys in `_zero_paths_response`. (6) **Backspace deletes antenna while typing in textarea**: `useKeyboard.ts` guarded against `HTMLInputElement` and `HTMLSelectElement` but not `HTMLTextAreaElement`. Pressing Backspace in the BugReporter textarea deleted the antenna and cleared results. Added `HTMLTextAreaElement` check. (7) **Toolbar dead code**: `ComplianceBadge` had two identical `compliant == null` checks; the second was unreachable because the first already returned. Removed the dead block. (8) **useDosimetry timeout leak on early return**: The 60s abort timeout was started before the "no environment mesh" early-return checks, but never cleared on those paths. Each early return leaked a timer. Added `clearTimeout(timeoutId)` before both early returns. Physics audit findings (6 GHz boundary, sinc projection, degenerate normals) reviewed but not changed as they are design decisions requiring Robin's input. TypeScript compiles clean, frontend builds, lint passes, non-E2E tests pass.

- [2026-04-18] feature-agent: **PR #600 (stale body heatmap after MIMO toggle off)**. Fixed the UX bug flagged in the 2026-04-18 08:22 UTC QA pass: disabling MIMO without nudging any control left the body mesh rendering pre-MIMO `sabArray` colors while the toolbar pill showed N/A compliance. Root cause: `useDosimetry` effect deps were `[sim, scene, exposureScenario, antennaStoreState, triggerCompute]` — `shouldSkipCompute` reads `useMIMOStore.enabled` via `getState()`, but the effect had no subscription on it, so toggling MIMO off never rearmed the debounced trigger. Added `const mimoEnabled = useMIMOStore(s => s.enabled)` and included it in the effect dep list. Toggling MIMO on is unaffected (effect fires, `shouldSkipCompute` returns true, debounce short-circuits inside `triggerCompute`); toggling MIMO off now repaints single-user results. One-line change in `aegis-web/src/hooks/useDosimetry.ts`. 76/76 vitest, tsc clean, build clean. Notes for next agent: while tracing this I also confirmed `useActiveSimulation` intentionally falls back to `singleStats` during MIMO-pending (keeps compliance panel visible before MIMO results arrive); no change needed there. The stale-HUD symptom described in that QA note was actually `singleStats` that had been frozen since MIMO was first enabled (since `useDosimetry` never re-ran during MIMO-on even when freq/power changed) — the fix resolves both the heatmap and the HUD pill in one edit because recomputing replaces both.
- [2026-04-18] feature-agent: Picked up two bugs and one polish item that earlier code-review passes had flagged-but-not-fixed. **PR #597 (frontend polish)**. (1) **FocusPointMarker Rules-of-Hooks violation** — `useMemo` sat below `if (!fp || !arrPos) return null`, so the hook call order depended on props/store state. Moved the memo above the guard; it now handles `fp === null` internally. (2) **HeatmapCanvas tooltip off-by-CSS-zoom** — `handleMouseMove` hit-tested with raw CSS pixels while the canvas is CSS-stretched (`className="w-full"`) vs its internal 280x180 backing store, so the tooltip showed the wrong cell at most window sizes. `handleClick` already scaled correctly; aligned the two. (3) **PatternBrowserPanel cap indicator** — showed "10000 results" whether or not the backend `limit=10000` was hit, so users couldn't tell the list was truncated; now "10000+ (refine search)". Also formats `gain_dbi` via `toFixed(2)` to avoid "17.689999999999998 dBi". **Note for next agent**: a parallel agent landed a superset of (2) and (3) on master with a nicer `RENDER_LIMIT` DOM-virtualization-lite cap; took theirs during the rebase and kept only my unique FocusPointMarker fix in the merged PR. Lesson: sanity-check the QA coverage log the same way you sanity-check the bulletin — a lot of items rotate through multiple reviewers, and "flagged but not fixed" can flip to "already fixed" mid-session. 76/76 vitest pass, tsc clean, build clean, ruff clean. 11 commits since v0.29.6 (PRs #592/#595/#596/#597 are all PATCH-level); a v0.29.7 tag is warranted next run.
- [2026-04-17] feature-agent: Two polish PRs driven by the QA coverage log. **PR #574 (Pattern Browser polish)**: fixed three UX issues all flagged by the 2026-04-17 sidebar-panels swarm pass. (a) `gain_dbi` rendered with full float precision (e.g. "17.689999999999998 dBi") because SQLite REAL round-off surfaced directly in the JSX — added `formatGainDbi` / `formatFrequencyMhz` helpers that clamp to one decimal. (b) A blank search hit the backend's `limit=10000` cap and the panel attempted to render 10000 `<button>` nodes up front, intermittently stalling keyboard input in the search box — capped visible list at 300 and append a "Showing first 300 of N, refine your search" footer. (c) The counter said "10000 results" even when the backend had truncated — now says "10000+ results, narrow your search" when `results.length >= BACKEND_CAP`. No backend change. 76/76 vitest pass, tsc clean, build clean. **PR #575 (3D Tiles empty-mesh 404)**: 3D-env QA noted geocoding "Times Square" fired `/api/environment/3dtiles` but showed nothing and no error after ~45s. Root cause: `TileTraverser.traverse()` returns a valid `EnvironmentMesh` with 0 vertices/triangles when no tiles intersect the query (rural regions outside Google's photorealistic coverage) OR when every child-tile fetch fails silently at the `except Exception: pass` inside `_traverse_node`. The route used to pipe that empty mesh through as 200 OK, so the frontend cleared `loading` without ever raising an error. Added a `mesh.triangles.shape[0] == 0` check in `_handle_environment_3dtiles` that returns HTTP 404 with "No 3D Tiles cover this region. Google's photorealistic tiles are only available for major cities. Try a different location or switch the source to OSM." — the existing frontend `ErrorMessage` component surfaces it automatically. Added `test_empty_mesh_returns_404` that patches the traverser to return an empty mesh. 38/38 env-route + tile tests pass, ruff clean. Notes for next agent: there's still a quieter class of silent failure in 3dtiles where some tile requests fail at `_traverse_node` but a non-empty mesh is returned — that case doesn't trigger the new 404 because the user still gets SOMETHING, but they see fewer tiles than expected. A future improvement would be to count parse failures and warn above a threshold, but didn't ship that without confirming it wouldn't be noisy on healthy tile sets.
- [2026-04-17] feature-agent: Fixed the 3 pre-existing vitest module-load failures flagged by an earlier agent. **Root cause**: `aegis-web/src/stores/ui.ts:133` called `localStorage.getItem('aegis-tour-completed')` at module initialization (inside the `useUIStore` `create(...)` initial-state object). Any test importing a component/store that transitively pulls in `ui.ts` crashed at module-load with `ReferenceError: localStorage is not defined` because vitest uses `environment: 'node'` (see `vitest.config.ts`). Failed suites: `src/__tests__/MIMOPanel.test.tsx`, `src/__tests__/useMIMOKeyboard.test.ts`, `src/stores/__tests__/mimo.test.ts` — none of them actually touch the UI store; they only imported the MIMO store whose shared module graph loads `ui.ts`. **Fix**: Added two small helpers (`readTourCompleted` / `writeTourCompleted`) with `typeof localStorage !== 'undefined'` guards + try/catch around all 4 call sites (initial state, startTour, advanceTour, dismissTour). Also SSR-safe now (not currently needed, but cheap insurance). All 75 frontend tests pass (9/9 suites), `npx tsc --noEmit` clean, `npm run build` succeeds. Other `localStorage` usage in the codebase is via Zustand's `persist` middleware which already handles SSR/test safely. No other module-load browser-API access found in `src/stores/`.
- [2026-04-17] feature-agent: Session-handling fix in non-`client.ts` API callers. Three parallel Explore audits (coherent/MIMO/optimize, geometry/compliance/kernels, frontend panels/stores) surfaced mostly false positives on physics (verified `rho` conjugate formula matches monograph docstring; ICNIRP boundary conditions are intentional; `scoped_cache_get` default arg works correctly). **Real bugs fixed**: (1) **`api/basestations.ts:computeBasestations` bypassed `handle401`** — a 401 during base station compute silently threw a generic ApiError instead of logging the user out. (2) Same issue in **`api/coverage.ts:fetchCoverage`** (globe-view site load) and **`api/computeInline.ts:computeWithInlineMesh`** (inline-mesh compute path). Exported `handle401` from `api/client.ts` and added a shared `throwIf401(res, method, path)` helper; all three callers now use it. Also updated `coverage.ts` to throw `ApiError` instead of a generic `Error` so callers can distinguish 4xx from 5xx. Notes for next agent: originally also fixed a CloudRF coverage-overlay 401 bypass and two blob-URL leaks in `BaseStationsPanel.tsx`, but those became moot after rebase — the parallel agent run landed PR #540 which deleted that dead checkbox entirely. My BaseStationsPanel changes were dropped from this PR.
- [2026-04-17] code-reviewer: Focus area: integration bridges (DiffeRT, Sionna) and ray tracing pipeline. Found 1 bug, fixed. **Stale test `test_gpu_unavailable_returns_503` asserts old status code**: PR #512 (commit bb9c55e, merged 2026-04-16) deliberately changed the GPU-unavailable response in `api_compute_sionna_rt` from HTTP 503 to 501 (to prevent wasteful frontend retries), but the test in `tests/viewer/test_compute_routes.py` was never updated and still asserted `status_code == 503`. CI did not catch this because Flask is not installed in the CI image, so all viewer tests are auto-skipped (`pytest.importorskip("flask", ...)` in `conftest.py`). Renamed the test to `test_gpu_unavailable_returns_501` and updated the assertion. All 5 `TestComputeSionnaRtRoute` tests pass locally. Also reviewed (no bugs found): `routes/compute/rt.py` (DiffeRT bridge, `_resolve_rt_body`, `_trace_paths`, `_finalize_rt_response`), `routes/compute/sionna_rt.py` (scene-based Sionna, `_call_modal_sionna_scene` 501 path, `_validate_scene_path` proxy), `routes/compute/voxel_rt.py` (voxel-environment RT, `_load_voxel_request`, `_build_voxel_rt_scene` triangle-cap gate, `_invoke_modal_voxel_trace` NotImplementedError/None → 501), `routes/compute/_parsing.py`/`_responses.py`/`_rt_config.py`, `server/_voxels.py` (cache clear paths invalidate raytracer cache), `integration/differt.py`/`integration/sionna.py` (unchanged since prior review), `raytracer.py` (voxel scene cache key + invalidation). Verified status-code semantics consistent across all 4 RT routes: 501 means "GPU/service unavailable, do not retry"; 400 for validation; 500 for unexpected failure.

- [2026-04-17] feature-agent: Four frontend UX/correctness fixes. Dispatched four parallel Explore agents to audit optimize, export/share, MIMO/compliance, and sidebar/HUD areas (these were underweight in recent audits). After verifying claims against actual code (several agent "bugs" were misreads: CameraWidget globe button works fine, ColorLegend dB range is intentionally quantity-independent, AnnotationCanvas refs reset on remount), found and fixed: (1) **MIMO setArrayConfig ignores precoder feasibility**: `addUser` falls back to MRT when K > M, but shrinking array via `setArrayConfig` (e.g. from 4x4 to 2x1 with existing 4 users) did not re-validate. Result: ZF/MMSE backend calls silently fail or produce wrong results. Added same fallback + warning notification to `setArrayConfig`. (2) **CameraWidget follow button went to orbit from globe mode**: Toggle logic was `cameraMode === 'orbit' ? 'follow' : 'orbit'`, meaning from globe mode the follow button sent you to orbit, not follow. Changed to `cameraMode === 'follow' ? 'orbit' : 'follow'` so it now works consistently from any starting mode. (3) **Export errors hide backend's specific reason**: `handleExportError` showed generic "Failed to export dosimetry CSV" even when the server returned a helpful message (e.g. "No dosimetry result available" on 4xx). Added `ApiError` branch that surfaces `err.message` to the user, and only reports 5xx to Sentry. (4) **Optimization cancel produced no user feedback**: `stop()` aborted the SSE stream, but the frontend silently returned without updating the store summary or notifying the user, so the progress panel just froze mid-iteration. Now shows "Optimization cancelled" notification and writes "Cancelled by user" to the summary. User-report issue #527 is unactionable (just "something is broken" with no details, previous agent already requested info). TypeScript compiles clean, frontend builds, Python ruff clean; 3 pre-existing vitest module-load failures (ui.ts localStorage at import time) unrelated to these changes (verified against master).

- [2026-04-17] code-reviewer: Focus area: base station pipeline (scrapers, adapters, merge, coverage computation, compute routes, frontend hooks). No bugs found. Reviewed all `src/aegis/basestation/*.py` modules: `adapter.py` (CSV/parquet loaders, `paths_from_basestations` EIRP-to-power conversion and single-path-per-bs construction, mMIMO beam decomposition branch), `merge.py` (`spatial_dedup` BFS seed-anchored operator/band comparison correct), `power.py` (`effective_eirp_dbm` reduction factors, 1e-10 floor before log10), `classify.py` (`_NR_TDD_BANDS`, `_LTE_TDD_BANDS`, mMIMO 20 dBi threshold), `antenna.py` (`BaseStation` frozen dataclass, `total_tilt_deg` sum), `orientation.py` (`antenna_rotation_matrix = R_tilt @ R_az`, departure-to-local), `parquet_io.py` (bbox filter `[lon_min, lon_max, lat_min, lat_max]`), `pattern.py` (ITU-R F.1336-5 Gaussian), `coords.py` (small-angle ENU), `library.py` (SQLite 8-column schema matches 8 VALUES), `msi.py` (MSI parser dBd→dBi +2.15 shift, frequency range handling), `provenance.py`, `build.py` extract/merge/validate/report chain. Backend routes verified: `_compute.py` (EIRP-weighted freq, zero-power short-circuit, exposure mode enum, max_distance bounds), `_compute_mimo.py` (`_broadside_from_azimuth_tilt` ENU convention correct), `_data.py` (`_cache_origin` produces `(lat_avg, lon_avg)` from bbox `[lon_min, lon_max, lat_min, lat_max]`), `_load.py` (`_build_bbox` format), `_fidelity.py` (tier rules), `_geocode.py` (lat/lon regex), `coverage.py` (3-tier response, pyarrow schema guard, OOM-safe column projection, fallback on missing SiteCode). Frontend hooks: `useBaseStationsDosimetry.ts` (generation counter, 120s timeout, shared helpers), `useDosimetry.ts`, `useMIMODosimetry.ts`, `useOptimization.ts` (`applyPlacementMove` `[sx, sz, -sy]` matches `toScene`). Coordinate transforms in `aegis-web/src/api/coordinates.ts` symmetric. Origin tuple `(lat, lon)` and bbox `[lon_min, lon_max, lat_min, lat_max]` formats consistent backend-to-frontend (confirmed in `aegis-web/src/api/types.ts:443`). 135 basestation tests pass.

- [2026-04-17] feature-agent: Removed dead "Show coverage map (CloudRF)" checkbox in BaseStationsPanel. `CoverageOverlay.tsx` (which rendered `basestations.coverageUrl`) was deleted in commit 664e141 but the store state, checkbox UI, and backend `/api/environment/coverage` route were left behind. The checkbox fetched CloudRF data (wasting API quota on every click) but the returned blob URL was never displayed anywhere. Removed `showCoverage`/`coverageUrl` state + `setShowCoverage`/`setCoverageUrl` actions from the basestations store, the checkbox + `fetchCoverage`/`handleCoverageToggle` logic + `coverageLoading` state from BaseStationsPanel, and the `_handle_environment_coverage` handler + POST route from environment.py. 3 files changed, +3/-176 lines. TypeScript compiles clean, frontend builds, 78 viewer environment/basestations route tests pass. Note: `CoverageMap.tsx` (different component, uses `useCoverageStore` for the Google Maps globe overlay) is preserved and unaffected; CloudRF usage in `cloudrf.py`, `patterns.py`, and `compute/dosimetry.py` is also preserved.

- [2026-04-17] code-reviewer: Focus area: error handling and input validation at system boundaries (post refactors PR-B/C/D). Found and fixed 3 bugs. (1) **Stale bug-report tests**: `test_bug_report_missing_screenshot` and `test_bug_report_success` were leftovers from before commit 592e491 (made screenshots optional in backend) and commit 69c7b6e (replaced GitHub upload with local-disk storage, removing `_upload_screenshot_to_github`). The first asserted a required-screenshot 400 that no longer exists; the second mocked a function that no longer exists. Rewrote as `test_bug_report_text_only_allowed` (screenshotless report succeeds via GitHub issue creation) and dropped the obsolete mock. (2) **antennas[i].power_dbm bare float() crash in /api/compute**: `_parse_antennas_array` at `routes/compute/dosimetry.py:280` did `float(raw_ant.get("power_dbm", default_power_dbm))` with no try/except. A client posting `{"antennas":[{"position":[1,2,3],"power_dbm":"abc"}]}` returned HTTP 500 instead of 400. Added try/except → 400 and a dict-type guard on `array_config` so a scalar doesn't silently propagate. (3) **basestations /api/basestations/load bare float() crash when country is explicit**: `_geocode_if_needed` only validates lat/lon when `country` is absent, so `{"lat":"abc","lon":"def","country":"Belgium","radius_m":100}` slipped through and crashed `_build_bbox` at `basestations/_load.py:86` (and the Belgian-region branch in `_resolve_country_and_region`). Added try/except + range check in `_build_bbox` and a guarded float in `_resolve_country_and_region`. Added 4 regression tests. Rebased on origin/master (picked up PR #536 503→501 fix, #537, #539, #540). 2395 tests pass, lint + format clean. Reviewed but not changed (no bugs found): all other `routes/compute/**` parsers (power_dbm, freq, rotation, vec3 all wrap in try/except), `routes/compute/_responses.py` NaN/Inf sanitization, scene-path validation via `_validate_scene_path` proxies, zero-paths export cache clearing, other `routes/basestations/*` handlers, `routes/analysis.py`, `routes/data.py`, `routes/location.py` SSE ordering, `routes/mimo.py`, `routes/optimize.py`, `server/*` split.

- [2026-04-17] code-reviewer: Focus area: 3D scene Three.js resource lifecycle, geometry disposal, camera state, rendering. No bugs found. Recent changes (PRs D-G) decomposed panels/hooks/backend routes but did not touch scene/. Verified disposal patterns across all 26 scene components after the maintainability refactor: SceneGeometry and HullMesh intermediate BufferGeometry leak fix (#472) still in place, LSPHeatmap texture disposal on recompute/unmount, ClusterPaths SubpathLines geometry disposal, DistanceLine prevTextureRef pattern, Antenna prevGeoRef + unmount cleanup, AntennaArray patternGeo dispose-on-change, EnvironmentTerrain and EnvironmentOSM geometry disposal, ComplianceRing prevDiscRef/prevRingRef disposal, AnimatedBody frozenGeo goes through setBodyGeometry (which disposes prior), useGltfBody scene traversal disposal, useMIMODosimetry loadMissingBodies routes through setUserBodyGeometry (disposes prior). Camera state (CameraSyncer, CameraController, CameraInitializer, FollowCamera, OrbitControls) unchanged and correct. Reviewed decomposed hooks _dosimetryResult.ts, useDosimetry.ts, useMIMODosimetry.ts, useConfig.ts: generation counters, abort/timeout cleanup, and store writes all preserved across the refactor. Noted but not fixed: FocusPointMarker.tsx has a Rules-of-Hooks smell (useMemo at line 35 after a `if (!fp || !arrPos) return null` at line 32), but unreachable in practice because mimoFp is always defined (non-nullable `[number,number,number]` with default value) and non-MIMO callers only render when ant.focusPoint is truthy. 1843 tests pass (skipped suites need JAX/Flask/plotly/differt not installed in this env), ruff clean, TypeScript compiles clean.

- [2026-04-17] code-reviewer: Focus area: physics kernels and compliance (ECBF, spatial averaging, ICNIRP limits). Found 1 real bug, fixed. **ECBF power-slack-regime fix from PR #438 is dead code; solver returns suboptimal precoder when Q is invertible.** The slack-regime branch added in commit ef268e5 (`src/aegis/coherent/ecbf.py:129-148`) is unreachable because the prior infeasibility branch (`if p_abs_inf > P_abs_max:` at line 119) fires first. When Q is invertible (eigenvalues[0] > NUMERICAL_FLOOR), `p_abs_inf` and `p_abs_asymp` compute the **same value** (both are `P * (h*^H Q^{-1} h*) / (h*^H Q^{-2} h*)`), so any case that would have triggered the slack branch already returned at line 127 with the minimum-eigenvalue direction. With h orthogonal to that direction (e.g., `Q=diag([10,1,0.001]), h=[1,1,0], P=1, P_abs_max=0.5`), the returned `x = sqrt(P)*V[:,0]` gives `|h^T x|^2 = 0` while the true slack-regime optimum gives `|h^T x|^2 = 0.55`. The existing `test_ecbf_power_slack_regime` only checked constraint satisfaction, not optimality, so it didn't catch this. **Fix**: Reordered the checks so the slack-regime branch runs before the infeasibility branch (which is now correctly only reached when Q is singular). Updated the docstring of `solve_ecbf` (`||x*||^2 <= P` rather than `= P`), updated `test_ecbf_respects_power_budget` to assert `<=` (it was asserting strict equality, which is wrong for the QCQP), strengthened `test_ecbf_power_slack_regime` to verify objective optimality (would have caught this bug), and renamed `test_infeasible_constraint_warns_and_returns_vector` to `test_tight_constraint_invertible_Q_uses_slack_regime` since under the QCQP an invertible Q is never infeasible. Also reviewed (no bugs found): `_finalize_rt_response` and zero-path handling in `routes/compute/sionna_rt.py`, `compute_exposure_operator`/`eigendecompose_Q`/`compute_rho` in `coherent/exposure_operator.py` (Hermitian PSD enforcement, ascending-vs-descending sort consistent with each caller), `level8_ecbf` (uses `x_star` directly without assuming `||x_star||^2 = P`, so the fix doesn't break sab calculation), `geometry/averaging.py` Numba inner loop. 2380 tests pass, lint + format clean.

- [2026-04-17] code-reviewer: Focus area: frontend data flow (Zustand store updates, API response handling, loading/error states). Found 1 bug, fixed. **`useConfig.applyInitialSimulation` destroys persisted simulation settings on every page load.** PR #419 added `persist` middleware to `useSimulationStore` that saves `mode, fresnel, powerDbm, skinModel, ...` to localStorage. But `useConfig.ts:26-39` (run on every mount) unconditionally re-sets `{ mode: 'spatial', fresnel: true, powerDbm: 43, skinModel: 'itis' }` — values that are IDENTICAL to the Zustand store defaults, so on first visit this is a no-op, but on every subsequent visit it overwrites whatever the user had persisted. The four hardcoded lines were effectively dead code whose only observable effect was breaking the persistence feature. Removed them; kept the `caps.body_placement` auto-placement logic. TypeScript compiles clean, `npm run build` succeeds. Noted but not fixed (separate design decision): `applyDosimetryConfig` also unconditionally overrides persisted `freqGhz` and `powerDbm` from server config.dosimetry (which always carries `default_power_dbm: 43.0` and `freq_hz: 28 GHz` from `viewer/config.py` DEFAULTS), so persistence for those two fields is still broken — but that one is ambiguous (server admin config vs user preference) and worth a design decision rather than an autonomous fix. Reviewed (no bugs found): `api/client.ts` (`handle401`, `throwIf401`, `fetchWithRetry` jittered backoff, `computeEndpoint` binary parsing), `stores/simulation.ts` (freq-crossover logic at 6/30 GHz, antenna-store sync subscription, partialize+merge displayQuantity validation), `stores/mimo.ts` (`setArrayConfig` precoder fallback from PR #543, `addUser` MRT fallback when K > M), `stores/basestations.ts`, `stores/optimize.ts`, `hooks/useDosimetry.ts` and `hooks/useMIMODosimetry.ts` (generation counter pattern, abort/timeout cleanup), `hooks/useOptimization.ts` (SSE stream, abort on unmount), `hooks/useBaseStationsDosimetry.ts` (generation counters), `hooks/_dosimetryResult.ts` (shared error handler from PR-E).
- [2026-04-17] feature-agent: Focus area: API input validation on `/api/compliance/spatial`. Dispatched three parallel exploration agents (export/share flows, compliance module, scenario/config loading). Most findings overlapped with prior audits or were false alarms (e.g. rtConfig camelCase→snake_case IS transformed in `buildRtConfig` before compute calls; not a bug). Shipped one real fix: **spatial compliance route silently accepted inverted/zero-area bboxes and negative/huge receiver heights**. `np.linspace(max, min, n)` produces a reversed array, and haversine with `receiver_height - station_height` misbehaves with non-physical heights. Added explicit validation at `src/aegis/viewer/routes/analysis.py:394+` — returns 400 on `lon_min >= lon_max`, `lat_min >= lat_max`, or `receiver_height_m` outside (0, 500]. Frontend paths (coverage hud) never produce bad inputs (backend derives bbox from station extent when omitted), so this is defensive for direct API callers and dev-console misuse. Added 4 regression tests in `tests/test_viewer_analysis_routes.py` covering each rejection path (used `patch("aegis.viewer.server.scoped_cache_get")` to inject a fake base station, and set `app.secret_key` on the route-level test fixture so `_sid()` can write to session). 2399 tests pass, lint + format clean. Rebased on origin/master; picked up PR #541-543.

- [2026-04-17] code-reviewer: Focus area: integration bridges (DiffeRT, Sionna) and ray tracing pipeline — **re-review after PR-H maintainability decomposition (#546)**. No bugs found. Verified behavior preservation across three decompositions: (1) `src/aegis/viewer/raytracer.py` greedy voxel mesher split into `_best_rectangle_for_seed` / `_emit_greedy_quad` / `_mesh_slice` / `_mesh_direction` / `_greedy_mesh_faces` — maximal-area rectangle heuristic per face direction preserved; (2) `src/aegis/basestation/msi.py` parser split into `_parse_frequency` / `_parse_gain` / `_parse_tilt` / `_read_pattern_rows` — dBd→dBi +2.15 shift and frequency-range handling intact (one edge-case difference: a malformed second GAIN line now yields NaN rather than silently keeping the first value, arguably more correct); (3) `src/aegis/basestation/merge.py` BFS dedup split into `_project_to_metres` / `_bfs_cluster_labels` / `_merge_cluster_rows` — seed-anchored operator/frequency comparison preserved in `_bfs_cluster_labels`. Reviewed end-to-end: `integration/differt.py` (`paths_from_differt`, `_track_polarisation`, `_pad_and_concatenate` with `mode="edge"` for vertices and `-1` for object indices correctly handling variable path lengths; `argmax` on masked cols finds last nonzero segment correctly), `integration/sionna.py` (JAX and NumPy code paths produce equivalent dosimetry results — element-major reshape `a_raw[rx, 0, tx, :, :].reshape(-1)` matches `jnp.tile(theta_r_raw[rx, tx, :], n_elements)` and `jnp.tile(valid_elem, n_elements)`; JAX path returns M*N paths with zeroed psi for invalid vs NumPy path that skips, equivalent for incoherent and coherent consumers), `modal_rt/differt_tracer.py` (T4 wrapper reconstructs scene dir from XML + files dict), `paths.py` (`PropagationPaths.__post_init__` shape validation, `from_dict` restores empty arrays to `(0,3)`). All 4 RT compute routes still correct post-refactor: `routes/compute/rt.py`, `voxel_rt.py` (`_invoke_modal_voxel_trace` NotImplementedError/None → 501), `sionna_rt.py` (scene-based, `_validate_scene_path` proxy), `scenes.py` (`_api_voxels_hull_mesh_impl`). The prior code-reviewer entry (same day) already fixed the one real stale test (503→501 in PR #536). 2399 tests pass (fast suite, excluding @slow).

- 2026-04-17 10:20 UTC | reviewer #5 | interleaved production fixes #535-#544 (v0.29.1 to v0.29.2) | T1: 0, T2: 1, T3: 2 | area looks healthy overall; filed hunch #553 on useOptimization cancel/restart race that PR #537 amplifies (pre-existing stop() vs. new start() ownership gap, now leaves a persistent "Cancelled by user" summary over a live run). ECBF reorder in #543 is mathematically correct; Q-singular-and-h-orthogonal-to-null corner case still returns zero-objective direction (Tier 3, rare). PR #541 antennas[i].power_dbm and main power_dbm still accept NaN (Tier 3, pre-existing, not regressed by this PR). PR #544 bbox/receiver_height validation correct and handles NaN/inf for receiver_height but not for bbox floats (same Tier 3 class). PR #539 Fresnel label change matches monograph T_avg(mu=1)=T0 invariant. PR #535 Belgium locale fix is clean. PR #536 test-only 503-to-501 fix is clean. PR #540 coverage-map dead-UI removal is clean. PR #542 401-handling + blob revoke is clean.
- 2026-04-17 16:00 UTC | reviewer #1 | Round 1 v0.29.0 structural refactor (PRs #517, #521, #523, #524, #525) | T1: 0, T2: 0, T3: 1 | Round 1 is a clean mechanical refactor; behavior preserved across all five PRs. Compared pre/post-refactor compute_dosimetry and the flattened route closures line by line; the compute/ package __init__ covers every external import (including the _validate_scene_path / _run_dosimetry proxy pattern for test patching); no dangling greedy-mesh v1 refs or config flags. One pre-existing Tier 3 hover/click coord scaling mismatch in HeatmapCanvas surfaced while reading but was preserved verbatim, not introduced. Module looks healthy.
- [2026-04-17] code-review-swarm #4 (PR-H/I/J/K maintainability re-review): Focus area: verify behavior preservation after PR-H (#546) Python mesher/msi/merge splits, PR-J (#547) frontend scene+panels decomposition, PR-K (#548) physics phase-extract + useScenario/useKeyboard hooks. Found and fixed one real bug: **jump impulse re-injected at high framerates** (Tier 1, PR #555). PR-K's `stepVertical` uses `return` to apply the impulse which skips the same-tick gravity step that the pre-refactor inline form applied. Compounding this, `resolveGroundCollision` collapsed OLD's three-case branch (`vy<=0 snap` / `clearly-above` / `within-snap-ascending leave unchanged`) into two cases, unconditionally setting `onGround = (py <= groundY + snap)`. Since `useKeyboard` polls `Space` as held (`s.jump = keys.has('Space')`), any tick where the body is mid-ascent within `ground_snap` re-fires the impulse. With backend defaults (`jump_impulse=5`, `ground_snap=0.05`), this triggers on 120Hz+ monitors: one tick of py=0.04 stays within snap, next tick re-jumps. Test config (`jump_impulse=8`, `ground_snap=0.1`, dt=0.008) had vy pinned at 8 for two consecutive ticks; regression test holds jump for five ticks and asserts vy decays monotonically (fails on buggy code, passes after fix). Fix restores the sequential jump+gravity form and the three-case ground-collision branch (adds `onGround` parameter). Also verified behavior-preservation of PR-H (`_greedy_mesh_faces` seed-iteration order and sweep-both-directions heuristic identical; `parse_msi` dict-dispatch identical for real files; `spatial_dedup` BFS preserves seed-anchored operator+frequency comparison) and PR-J (`AntennaArray`/`ComplianceRing`/`BodyMeshInstance`/`OptimizeGridPreview`/`HeatmapCanvas` scene helpers; panels/HUD decomposition). Tier-3 notes (not fixing): (a) `parse_msi` in PR-H overwrites a previously-valid FREQUENCY/GAIN/TILT value if a later malformed directive of the same kind fails parsing — OLD kept the first valid value; not practical for real MSI files but technically a regression; (b) `HeatmapCanvas.handleMouseMove` uses raw `clientX - rect.left` for hit-testing while `handleClick` scales by `canvas.width/rect.width` — pre-existing inconsistency not introduced by PR-J (same item reviewer #1 saw). 22 physics tests + 58 frontend tests pass.

- [2026-04-17] code-reviewer: Focus area: 3D scene Three.js resource lifecycle, geometry disposal, camera state, rendering (second pass after an earlier same-day reviewer). No bugs found. Re-verified the four PR-J-decomposed scene files: `AntennaArray.tsx` (patternGeo useMemo + unmount-only disposal effect, base IcosahedronGeometry own attributes disposed by geometry.dispose()), `BodyMeshInstance.tsx` (writes directly to the BufferAttribute backing array via useEffect, `setColormapLockedMax` triggers at most one extra render because `colormapLockedMax` is in the deps), `ComplianceRing.tsx` (prevDiscRef/prevRingRef disposed on re-memo and unmount; circular branch leaves refs null correctly; R3F primitives in `CircularBoundary` are auto-disposed), `OptimizeGridPreview.tsx` (relies entirely on R3F-managed sphereGeometry/drei Line). Cross-checked OptimizeGridPreview `bestIdx` (history index) against backend iteration order: `src/aegis/optim/placement.py` outer loop varies `free_axes[0]` (default X) and inner loop varies `free_axes[1]` (default Z); preview outer loop = sx (= server X), inner = sy (= server Z) — matches. `constraintAxis` is in the store but never set from any UI so the theoretical non-default-plane mismatch can't be triggered. `FocusPointMarker` still has the hooks-order smell the previous reviewer called out but remains inert because `mimoFp` has a default and both call sites gate the render. Lint + TS clean, 2399 python tests pass, 58 frontend tests pass.

- [2026-04-17] code-reviewer: Focus area: base station pipeline (scrapers, adapters, merge, coverage computation) — re-review on top of prior same-day review. No bugs found. Re-verified PR-H (#546) decomposition in `src/aegis/basestation/merge.py` (`_project_to_metres`/`_bfs_cluster_labels`/`_merge_cluster_rows`) against pre-PR source: BFS seed-anchored `norm_ops[seed]` and `freq_bands[seed]` comparisons preserved; combined-skip condition `labels[j] >= 0 or norm_ops[j] != norm_ops[seed]` is semantically identical to the prior two-step skip; `has_fb = freq_bands is not None` is equivalent to the prior `has_fb and freq_bands is not None`. Also re-verified `src/aegis/basestation/msi.py` helper split (`_parse_frequency`/`_parse_gain`/`_parse_tilt`/`_read_pattern_rows`): GAIN parser now initializes `gain_dbi = NaN` before token scan, which is marginally more correct than OLD (OLD would add 2.15 to the prior-iteration value if a `GAIN DBD` line had no parseable token), and `_read_pattern_rows` preserves the `min(count, 360)` cap and advances the line cursor identically. Reviewed end-to-end (no new issues): `src/aegis/basestation/adapter.py` (CSV/parquet loaders, `paths_from_basestation` EIRP→TX power and pattern-gain modulation, `_mmimo_beam_decomposition` broadcast+traffic combination math with ACTUAL_MAX/TYPICAL envelopes), `power.py` `eirp_to_tx_power_dbm` correctly passes through when `gain_dbi == 0` (0 dBi = 1.0 linear, identity), `classify.py` `_lookup_tdd` band table and `classify_basestation` mMIMO/sector/small_cell exposure configs, `parquet_io.py` bbox filter format `[lon_min, lon_max, lat_min, lat_max]`, `utils.py` `_sanitize_label` and `_safe_float` NaN guard. Backend routes: `routes/basestations/_compute.py` (`_parse_max_distance` bounds check, `_eirp_weighted_freq` zero-guard fallback 3.5 GHz, `_zero_power_response` on `total_power <= 0`), `_compute_mimo.py` (`_validate_index`/`_validate_level` guards, `_broadside_from_azimuth_tilt` ENU convention, `_build_user` body-rotation applied to device_offset), `_data.py`/`_load.py`/`_fidelity.py`/`_geocode.py` unchanged since prior review. `routes/coverage.py` three-tier schema and OOM-safe column projection with pyarrow schema guard verified. One architectural observation (not a bug): `paths_from_basestations` never threads `archetype` or `beam_config` through to `paths_from_basestation`, so the mMIMO beam-decomposition branch in `paths_from_basestation` is unreachable via the batch compute API — batch compute falls back to the simpler `effective_eirp_dbm`-reduced standard path. This appears to be an intentional simplification (faster batch compute, still realistic via the TDD/PRF/traffic-load factors applied through `ExposureConfig`). All 2399 fast tests pass.

- [2026-04-17] code-reviewer: Focus area: viewer feature completeness — trace user flows end-to-end, find dead code paths and unfinished features. No new bugs found. Re-verified very recent production fixes: **PR #566** (Tilt+power gated on `rtReady = rtPaths !== null && rtPaths.length > 0`) — gate is correct; `streamOptimization` error parser now extracts `error`/`message` field from JSON bodies. **PR #565** (NaN colorbar fix) — MIMO peak + tilt_power now emit `stats.peaks.sab` per iteration; ColorLegend's `stats.peaks?.[displayQuantity] ?? stats.peak_sab` fallback works for the common display=`sab` case. Minor non-bug note: when user has displayQuantity = `sab_4cm2`/`sab_1cm2`/`sinc_local` during an in-flight optimization, the legend falls back to `peak_sab` (peak of raw sab, not the averaged quantity) — not a bug because the non-optimization compute responses do populate all four peaks in `routes/compute/_responses.py:247-253`, and optimizer iterations don't compute spatial-averaged peaks. Also verified PR #553/#562 `useOptimization.start`/`stop` ownership pattern: `abortRef` + `isCurrent()` guard correctly prevents store stomps across rapid stop()-then-start() sequences. Reviewed but no issues: `OptimizePanel.tsx` (ModeSelector gates all three modes correctly; ModeConstraints covers all three modes), `useScenario.ts` (scenario load flow: clearResults → clearScene → applySimulationState → applyEnvironmentState → applyCoverageForScenario → setActiveScenario → fetch OSM/3dtiles → updateScenarioUrl — clean), `useKeyboard.ts` (jump polling + arrow-nudge + delete-antenna paths correct post PR-K decomposition). The Explore-agent sweep rediscovered the already-noted `constraintAxis`/`constraintValue` dead store fields (grid-constraint UI never wired) — previously flagged by a same-day reviewer as Tier 3, nothing new. No Python or frontend caller/callee mismatches found between `aegis-web/src/api/*.ts` and `src/aegis/viewer/routes/*`. 2399 python fast tests pass.

- [2026-04-17] feature-agent: Shipped two tier-3 items flagged by reviewers as separate PRs. (1) **HeatmapCanvas hover tooltip wrong cell on scaled canvas (#569)**: the compliance heatmap uses `width=280 height=180` + `w-full` CSS, so `handleClick` already scaled CSS→canvas coords but `handleMouseMove` did not — tooltip pointed at a different cell than the one under the cursor on any viewport wider than 280 px. Fixed: scale CSS coords to canvas internal coords for hit-testing, keep CSS coords for tooltip positioning (position is CSS-relative to the container). (2) **NaN/Inf leaked through power_dbm and compliance bbox (#570)**: all four `_parse_power_dbm` sites (`/api/compute` main and `antennas[i]`, `/api/compute/rt`, `/api/compute/sionna_rt`) plus `/api/compliance/spatial` bbox accepted NaN because `float('nan')` succeeds and NaN comparisons return False — so NaN passed both range checks and propagated into the dosimetry pipeline, producing NaN outputs. Fixed with explicit `math.isfinite()` guards. 2367 fast Python tests + 76 frontend tests pass, ruff + TypeScript clean.

- [2026-04-17] code-reviewer: Focus area: frontend data flow (Zustand stores, API response handling, loading/error states) — re-review on top of earlier same-day sweep. Found and fixed 1 real bug (#573). **LSP heatmap `.finally` stomps loading state on stale completions.** `aegis-web/src/hooks/useDosimetry.ts:388-418` created an AbortController per effect run and called `controller.abort()` in the cleanup, but `fetchLSPHeatmap` (at `api/client.ts:517`) takes no `AbortSignal` — so abort never actually cancelled the in-flight POST. `.then` skipped stale data via `signal.aborted` (data ownership OK), but `.finally` unconditionally set `lspHeatmapLoading = false`, so when the old promise resolved after a dependency change the LSPLegend shimmer flickered off even though the newer fetch was still in-flight. Replaced the unused AbortController with a `lspHeatmapGenRef` generation counter mirroring the pattern already used at `useDosimetry.ts:367` (`triggerCompute.finally`) and `useBaseStationsDosimetry.ts:59`. Reviewed but no new bugs found: `aegis-web/src/components/hud/ColorLegend.tsx` (smart dynamic-range percentile + tick builder + lock/unlock toggle; persisted/unpersisted state separation consistent with `stores/ui.ts`), `aegis-web/src/components/panels/analysis/HeatmapCanvas.tsx` (post-PR #569 CSS-scaled hit-testing for both mouse move and click, crosshair out-of-bounds guard correct), `aegis-web/src/stores/ui.ts` (localStorage guards via `readTourCompleted`/`writeTourCompleted` still in place), `aegis-web/src/api/optimize.ts` SSE error parser, `aegis-web/src/hooks/useOptimization.ts` (abortRef + isCurrent() ownership pattern still correct post PR #553/#562), `aegis-web/src/hooks/useConfig.ts` (applyInitialSimulation no longer clobbers persisted settings post PR #551), `aegis-web/src/stores/simulation.ts` (freq crossover at 6/30 GHz, Zustand partialize excludes the LSP heatmap loading/data fields correctly). 76 frontend tests pass, `npx tsc --noEmit` clean, `npm run build` succeeds.

- [2026-04-17] code-reviewer: Focus area: integration bridges (DiffeRT, Sionna) and ray tracing pipeline — re-review on top of three prior same-day reviews in this area. No bugs found. Spot-checked after PR #566 (frontend-only Tilt+power optimizer gate on `_last_rt_paths` presence): backend `/api/optimize/tilt-power` still gated on `state.paths is not None` in `src/aegis/viewer/routes/optimize.py`, so the frontend disable is a pure UX improvement not a behavior change. Re-verified: `src/aegis/integration/differt.py` (`_track_polarisation` TE/TM decomposition, `_pad_and_concatenate` with `mode="edge"` for vertices + `-1` fill for object indices, `argmax` on masked columns finds last non-interaction segment for LOS detection), `integration/sionna.py` (`_paths_from_sionna_jax` element-major `reshape(-1)` equivalent to NumPy `tile`; same `valid_mask` across all elements under `synthetic_array=True`; zeroed-psi invalid paths still correct for incoherent and coherent consumers), `viewer/routes/compute/rt.py` + `voxel_rt.py` + `sionna_rt.py` (four RT routes use consistent 501 semantics for GPU-unavailable / scene-conversion-pending, 400 for validation, 500 for unexpected), `viewer/raytracer.py` (voxel scene cache key + invalidation, greedy mesher sweep-both-directions heuristic), `modal_rt/differt_tracer.py` (T4 wrapper), `paths.py` (`PropagationPaths.__post_init__` + `from_dict` empty-array reshape), `integration/cloudrf.py` (`antenna_to_pattern` dBd→dBi +2.15 conversion; `float(antenna_data.get("gain") or 0) + 2.15` fallback subtle but correct — `or 0` handles `None`/missing, not `0.0` literal), `environment/export.py` (`to_differt_scene` and `to_sionna_mesh_data`). Noted but not flagged: (a) `body_center` discrepancy between `rt.py` + `sionna_rt.py` scene-based (uses `default_bc + body_offset` from cached raytracer) versus `voxel_rt.py` + `sionna_rt.py` env-based (uses `transformed_body.centroids.mean(axis=0)`) is pre-existing and intentional per inline comments (Sionna scene mesh is floored at z=0 while DiffeRT uses real terrain elevation); (b) `to_differt_scene._compute_object_bounds` creates one `object_bounds` entry per contiguous material block rather than per unique material name — correct for downstream DiffeRT interaction but subtle if inspected directly. 2399 fast tests pass, 100 RT-related tests + 227 viewer tests pass, lint + format clean.

- [2026-04-18] feature-agent: Two frontend bugs, one PR (#588). (1) **Optimize Run button blocked MIMO/tilt_power workflows on `antennaPos`.** `OptimizePanel.canRun` required `sim.antennaPos !== null` for every mode, but backend `_build_mimo_peak_config` reads `G_tilde` from the cached MIMO scene (no `center`) and `_build_tilt_power_config` reads `_last_rt_paths` (no `center`) — only `_build_placement_config` needs `center` (and even that has a `[5,0,3]` default). Users who enabled MIMO + placed array + ran MIMO compute could not click Run because they had never dropped a single regular antenna. Moved the `antennaPos` guard into `canRunPlacement`; MIMO still gates on `mimoEnabled`, tilt+power on `rtReady`, so accidentally-missing state still surfaces an error. (2) **`simulation.exposureMode` silently dropped from share link.** `collectState()` gathered 40+ fields but missed `exposureMode` (theoretical/actual_max/typical) even though it is user-settable in `ParametersPanel` and sent to every compute via `useDosimetry.ts:107` and `useBaseStationsDosimetry.ts:40`. Sharing a link from `actual_max` made the recipient see `theoretical` with no warning. Added the field to `collectState`, `SHARE_DEFAULTS` (so default-diffing still elides it when untouched), and `applySimulationState`. TypeScript clean, 76/76 vitest pass, build clean, ruff clean. Verified claims before fixing: three agent-reported "bugs" were false — MIMO precoder-weights do round-trip (backend returns them in summary dict at `compute_mimo_scene_with_bodies` line 552 and `jsonify(summary)` at `routes/mimo.py:354`); MIMO user spacing at `MIMOPanel.tsx:108` is correct (each new user at `offset + current_count * 1.0` is per-user offset, not off-by-one); `scenarios[*].webState.clearResults/clearScene` flags are not in the `ScenarioWebState` type (agent fabricated). Also skipped: `tilt_init_deg = 0` hardcoded is deliberate (no UI field exists for tilt init); share-hash-vs-scenario-param interactions are handled correctly by `replaceState(null, '', pathname)` which strips query AND hash. Release reminder: 23 commits now since v0.29.5 — mostly dependabot bumps + QA coverage logging + 4 small fixes (#573, #574, #575, #588). A v0.29.6 patch is warranted after this merges. Next agent should decide.

- [2026-04-18] code-reviewer: Focus area: React components — state management, error boundaries, stale closures, missing cleanup. No bugs found (confident to fix). Audited all 19 hooks in `aegis-web/src/hooks/*`, all 3 error boundaries, and components/panels/scene/hud. Verified cleanup patterns: (a) `useDosimetry`, `useMIMODosimetry`, `useBaseStationsDosimetry`, `useOptimization`, `useVoxelLoader`, `useBodyLoader` all use AbortController + generation counter and abort on unmount or request change; (b) `useMIMOKeyboard` adds/removes `keydown` listener inside its effect — cleanup correct; (c) `useClickToPlace` tracks refs, no listeners to leak; (d) `usePhysics` uses `useFrame` so unmount by R3F is automatic, and its `useEffect` on `controlledUserId` is a pure ref-reset with no side effects to clean up; (e) `useActiveSimulation` correctly calls all zustand selectors unconditionally before branching (no conditional hook calls); (f) `useScenario` uses only `useCallback`/`useMemo` — no effects to clean up, `useMemo` deps are stable; (g) `usePoseExtract`, `useGltfBody`, `useGpuStatus`, `useKeyboard`, `useConfig`, `useAuth`, `useIsMobile` all clean. Error boundaries (`AppErrorBoundary`, `PanelErrorBoundary`, `ErrorBoundary`) correctly implement `getDerivedStateFromError` + `componentDidCatch` with Sentry reporting; boundary scope is appropriate (panel-level for sidebar sections, app-level wraps Suspense). Stores: `stores/ui.ts` tour localStorage guards verified; `stores/environment.ts` AbortController dispatch pattern (`freshAbort`) + Sentry suppression for 429/502/503/504 clean; `stores/scene.ts` `setBodyGeometry` disposes prior geometry correctly; `stores/mimo.ts` `setArrayConfig`/`addUser` precoder fallback present (from prior PR #543), `reset()` disposes all body geometries. Confirmed two items previously flagged Tier 3 are now resolved or still inert: HeatmapCanvas hover/click coord-scaling mismatch was fixed by PR #569 earlier today (both handlers now scale CSS→canvas coords for hit-testing); `FocusPointMarker.tsx:35` useMemo-after-early-return remains inert because `mimoFp` has a default value and non-MIMO callers gate on truthy `ant.focusPoint`. 2399 fast python tests pass.

- [2026-04-18] code-reviewer: Focus area: type safety and API contracts across frontend and backend (post PR #565 NaN-colorbar fix and #566 tilt_power RT-gating). No bugs found. Re-verified recent diff: (1) `src/aegis/optim/mimo_peak.py` now emits `stats.peak_sab = float(np.max(sab))` (zero-guarded) alongside `stats.peaks = {"sab": peak_sab}`, which `ColorLegend` consumes via `stats.peaks?.[displayQuantity] ?? stats.peak_sab` — previously mimo_peak stats had no peak_sab so the colorbar maxSab fell through to undefined → NaN labels (the bug #565 fixed). (2) `src/aegis/optim/tilt_power.py` similarly adds `peaks: {"sab": peak}` — both optim modes now share the `peaks` contract expected by the frontend. (3) `aegis-web/src/api/optimize.ts` wraps the non-OK body in a try/catch JSON parser returning `parsed.error ?? parsed.message ?? text`; backend `_api_optimize_impl` returns `{"error": str(e)}, 400` on ValueError and yields `data: {"error": True, "message": ...}` for SSE exceptions — both branches produce a meaningful `Error.message`, consumed in `useOptimization.ts` as `addNotification('error', ...)`. (4) `aegis-web/src/components/panels/OptimizePanel.tsx` gates the `tilt_power` button on `rtReady = useSceneStore(s => s.rtPaths !== null && s.rtPaths.length > 0)`; backend `_build_tilt_power_config` independently raises ValueError if `_last_rt_paths` cache is missing — two-layer defense. Placement optimizer is correctly NOT gated on rtReady because `_resolve_placement_rt_scene` falls back to free-space propagation when no scene/voxels/env is available. Also reviewed (no bugs): (a) `_parse_rt_config` in `routes/optimize.py` omits `max_paths_per_source`/`specular_reflection`/`los`/`edge_diffraction` — intentional, placement uses DiffeRT (via `compute_paths_differt`) which doesn't accept those Sionna-only params; (b) `PathViz` TS interface (`{vertices, order}`) silently tolerates the extra `length` field the raytracer emits (structural typing); (c) `handleIterationEvent` casts the partial SSE `stats` to `DosimetryStats` via `as unknown as`, papering over many required fields (p_abs, tissue_eps_r, etc.) that optim iterations don't populate — pre-existing pattern, consumers all use optional chaining so no crash. (d) `rtPaths` staleness across non-RT dosimetry: if user runs RT then runs a non-RT compute without changing pathSource, both `rtPaths` and backend `_last_rt_paths` remain set — but they stay consistent (both layers still have the same stale paths), so the gating is self-consistent; same-pre-existing concern applies to tilt_power acting on stale geometry, not caused by this PR. 2399 python fast tests pass, lint clean.

- [2026-04-18] feature-agent: Follow-up to the Tier-3 NaN/Inf polish (#570) and the concurrent reviewer notes. Shipped one PR (#592), closed one duplicate (#591). **Closed #591** — my HeatmapCanvas hover/click coord-scaling fix (rx/ry variables) was functionally identical to the one master had already landed in #569 (cssX/cssY) while the branch was open; master wins, my branch deleted. **Merged #592** — extended the isfinite guards: (a) `_parse_vec3` now rejects NaN/Inf in any parsed vector (antenna_pos, body_offset, and per-antenna position), (b) `_parse_rotation_y` rejects NaN/Inf, (c) `_parse_freq_and_tissue` rejects NaN freq_hz (previously only <=0 rejected, so `float('nan')` slipped through the range check), (d) `_parse_antennas_array` now also enforces the same `[pwr_min, pwr_max]` bounds that already applied to the primary `power_dbm` — closes the "antennas[i].power_dbm still accepts oversized values" gap reviewer #5 noted in the 2026-04-17 Tier-3 list, (e) `basestations/_load._build_bbox` rejects NaN/Inf in lat/lon/radius_m. Regression tests added: two per parser (NaN + Inf) in `test_compute_route_helpers.py`, two bbox tests in `test_viewer_analysis_routes.py`, two radius tests in `test_viewer_basestations.py`. 2408 fast tests pass, ruff + format clean. Defensive at the parsing layer before any physics runs; cheap insurance against callers (browser-console, bots, stale saved scenes) who could produce bad inputs.
**Pinned required reading for any agent touching tests, CI, or the testing surface (added 2026-04-19):**
- [testing-roadmap-2026-04-19.md](testing-roadmap-2026-04-19.md) — the plan: CI tiering by trigger (PR / master / patch / minor), tool-by-tool decisions with honest verdicts, phases 1-5, tools rejected with reasons (GeMTest, Pynguin, ty, Chromatic/Argos SaaS).
- [testing-overhaul-2026-04-19.md](testing-overhaul-2026-04-19.md) — the post-mortem from the 2026-04-18/19 multi-agent run: 12 merged PRs, 12 real bugs caught (1 critical, 5 serious, 6 minor), per-wave verdict (3E/3F/4G did 92% of the bug-catching, 5J Pynguin was a documented loss), lessons for the next multi-agent run (agents merge through red CI; parallel agents share the worktree; basedpyright config ladder is subtle).


- [2026-04-19] orchestrator (one opinion, take or leave): v0.30.0 is out (published at 02:37 UTC after four red pre-runs). The first release-tier sweep actually paid for itself — Schemathesis + ECBF tests on Windows-3.12 surfaced four concrete bugs that fixed landed before the tag went green: (1) `ecbf.py` used `NUMERICAL_FLOOR=1e-30` where it needed a `matrix_rank` tolerance (Windows-3.12 flake), (2) `request.get_json(silent=True) or {}` pattern 500'd when body was a top-level array instead of a dict — two endpoints patched, 16+ more still use the pattern so a migration to `get_json_dict()` helper is a reasonable next step, (3) `/api/compute` leaked FileNotFoundError as 500 on bad `stochastic_preset` paths, (4) OpenAPI declared `text/event-stream` as `type: string` but frames are JSON objects. Also: release.yml `test` job was installing `--extra dev` only, so every viewer integration test was being skipped in the coverage upload — fixed. Codecov should move up on the next release now. 12 freshly-merged PRs (#630, #634, #637, #638, #641, #642, #643, #644, #645, #646, #647, #648) are still under-reviewed relative to the usual sweep cadence; the metamorphic relations in #645 are novel physics claims worth spot-checking against the monograph, the 7 type-fix logic changes in #641 could have shifted behavior beyond the integration tests, and the six direct-to-master CI commits (`5afbf0d`, `2d3629c`, `9c59b57`, `b21ecd9`, `ae3ab39`, `90cc5b0`) never went through PR review. Same underweight product areas previous reviewers flagged remain: scenario save/load/share flows, voxel mesher/cache invalidation, MIMO panel/ECBF solver. Frontend coverage is 7.5% line (high-yield target). Schemathesis config now excludes `positive_data_acceptance` because the OpenAPI spec doesn't mark every required param — spec hardening is a real follow-up, but not critical. Things NOT to touch: don't expand metamorphic tests without monograph re-derivation, don't re-run Pynguin elsewhere, don't re-enable the schemathesis `coverage` phase without triaging what it finds. Pick what fits your shift.

- [2026-04-18] code-reviewer: Focus area: frontend data flow (Zustand store updates, API response handling, loading/error states) — third re-review today, covering PRs merged since the 2026-04-17 same-focus sweep (#562 optimize race guard, #565 MIMO peak NaN colorbar, #566 Tilt+power RT gating, #573 LSP heatmap finally-stomps-loading). No bugs found. Verified ownership guard in `useOptimization.start()`: `isCurrent()` pattern correctly makes stale writes from an aborted run no-ops when a fresh `start()` replaces `abortRef.current`; the `stop()` body is now intentionally minimal (abort + server cancel) with all state writes delegated to the in-flight `start()` — correct since the abort propagates via for-await's next await and the `try/catch/finally` owns all writes guarded by `isCurrent()`. Verified MIMO-peak/tilt_power `stats.peaks: {sab: peak_sab}` payload round-trips through `SSEEvent.stats` → `useSimulationStore.setResults` → `ColorLegend.peakForQty`; for `displayQuantity ∈ {sab_4cm2, sab_1cm2, sinc_local}` during optimization the `stats.peaks?.[displayQuantity]` miss falls back to `stats.peak_sab` (not undefined → not `NaN`). Verified `streamOptimization` JSON-vs-text error parsing: `JSON.parse` is wrapped in try/catch, `parsed.error ?? parsed.message ?? text` fallback chain is correct. Verified `OptimizePanel.rtReady = scene.rtPaths !== null && rtPaths.length > 0`; the gating correctly disables Tilt+power until an RT compute runs. Confirmed the LSP heatmap `.finally` loading-state bug I independently re-discovered during this pass is already fixed on master (PR #573) — generation counter now owns the setLoading writes. Also reviewed (no bugs found): `api/client.ts` (fetchWithRetry jitter, ApiError, throwIf401, extractErrorMessage), `hooks/useDosimetry.ts` (generation counter, abort + timeout cleanup, `shouldSkipCompute` gating for MIMO/gltf/animation), `hooks/useMIMODosimetry.ts`, `hooks/useBaseStationsDosimetry.ts`, `stores/simulation.ts` (freq-crossover at 6/30 GHz, partialize fields, merge validation of `displayQuantity`), `stores/mimo.ts` (`addUser`/`setArrayConfig` MRT fallback when M<K, `removeUser` nextId when current focused is removed), `stores/optimize.ts` (onDone/onError clear running), `stores/scene.ts` (`setPathSource` clears rtPaths on non-RT), `api/coverage.ts` sites-binary decode (raw.buffer from Uint8Array.from is own buffer, offset 0). 2399 Python fast tests pass.

- [2026-04-18] code-reviewer: Focus area: viewer feature completeness (follow-up). Traced optimization flow after PRs #562/#565/#566; gap closed. **Added regression test** `test_stats_dict_has_peaks_sab_for_colorlegend` in `tests/test_optim_mimo_peak.py` and `tests/test_optim_tilt_power.py`. PR #565 restored the `stats.peak_sab` + `stats.peaks.sab` shape consumed by `ColorLegend.tsx:139` (`stats.peaks?.[displayQuantity] ?? stats.peak_sab`) but shipped no test, so a future edit could silently strip `peaks` and bring back NaN legend ticks. Tests pin the contract for both optimizers; verified they fail if the PR #565 changes are reverted. Tier-3 not-fixed observations: (i) `ProvenanceDot.tsx` orphaned since PR #391 (Apr 11) — leave for maintainability sweep; (ii) `placement.py` `idx >= len(grid)` branch at line 83 unreachable dead code but harmless; (iii) `handleDoneEvent` reduction-string math is nonsensical for `tilt_power` (objective can flip sign, yielding "108% reduction" or "-33% reduction") — cosmetic; (iv) `_api_optimize_cancel_impl` has unused `cache`/`cache_lock` params — cosmetic. 2401 fast tests pass, ruff clean.

- [2026-04-18] code-reviewer: Focus area: React components — state management, error boundaries, stale closures, missing cleanup (fourth 2026-04-18 pass after #573/#566/#565 merged). No new bugs found. Independently re-discovered the LSP heatmap `.finally` loading-state race (useDosimetry.ts:388-418) that PR #573 already fixed on master; opened PR #599 before noticing the prior line-53 bulletin entry from an earlier same-day reviewer had already flagged #573 as the fix. Closed #599 as redundant and deleted the branch. Verified hook cleanup ownership still holds across the merged fixes: `useDosimetry` main compute uses generation counter (`.finally` guard at line 367), LSP heatmap branch now also uses `lspHeatmapGenRef` (post #573), `useMIMODosimetry` has unmount abort at line 196-198, `useBaseStationsDosimetry` uses generation counters, `useOptimization.start()` `isCurrent()` guard (abortRef identity) still prevents stale SSE writes after stop()-then-start() sequences. OptimizePanel accordion unmounts panels on collapse (Base UI default `keepMounted=false`), but per-hook unmount cleanup handles that correctly — not a bug. Considered flagging `useOptimization` lacking a component-unmount cleanup, but the behavior is deliberate (user wants SSE to keep running when switching tabs, then reattach on remount); skipped per "confident bugs only" rule. **Note for future code-reviewers**: this focus area has been reviewed 4× today (lines 24, 41, 47, 53, and this entry) — rotate focus to currently underweight areas: scenario save/load/share flows, voxel mesher/cache invalidation, or MIMO panel/ECBF solver surface.

- [2026-04-18] code-reviewer: Focus area: error handling and input validation at system boundaries (post PR #565/#566, fifth input-validation sweep today after #570/#592). Found and fixed 1 bug. **`/api/optimize` placement mode crashes with HTTP 500 on null/string scalar inputs**: `_build_placement_config` at `routes/optimize.py:234` called `float(params.get("grid_spacing", 2.0))` with no try/except. A client posting `{"mode":"placement","grid_spacing":null}` triggered `TypeError: float() argument must be a string or a real number, not 'NoneType'` because `dict.get("key", default)` returns the stored `None`, not the default. The `_api_optimize_impl` try/except only caught `(ValueError, KeyError)`, so TypeError became a 500 with stack trace. Same pattern in `_build_placement_evaluate_fn` for `body_rotation_y`, `power_dbm`, and `body_offset`. Not covered by PR #570/#592 (those covered `/api/compute`, `/api/compliance/spatial`, `/api/basestations/load`). **Fix**: Added `_safe_float` helper (mirrors existing `_safe_int`) and used it for `grid_spacing`, `body_rotation_y`, `power_dbm`; wrapped `np.array(body_offset, dtype=np.float64)` in try/except → ValueError (→ 400). Added 3 regression tests (null grid_spacing, string grid_spacing, list-of-strings body_offset). 2402 tests pass, ruff clean. Also reviewed (no bugs found): PR #565 (MIMO peak `stats.peak_sab`/`peaks.sab` added with correct `sab.size > 0` guard; tilt_power `peaks.sab` field added — contract test already added by 2026-04-18 entry above as #596), PR #566 (Tilt+power gating on `rtPaths !== null && rtPaths.length > 0` correct; JSON error parsing in `streamOptimization` handles non-JSON fallback via try/catch), `_build_tilt_power_config` (passes through scalars without cast, but runtime errors inside the SSE generator are caught by the top-level `except Exception` at `routes/optimize.py:124` and sent as error events — graceful), `_build_mimo_peak_config` (ValueError from shape mismatches in `_resolve_mimo_x_init` caught correctly).

- [2026-04-18] feature-agent: Started on HeatmapCanvas hit-test mismatch (bulletin Tier-3) and PatternBrowser polish (float formatting + render cap). Collision: both were already landed as PR #569 and #574/#597 while I was working (parallel feature-agent). Dropped that work and pivoted. **Shipped #615** — Fixed nonsensical "% reduction" in tilt_power optimizer summary. Objective is `-power + λ·violation²` (signed), not a peak exposure, so `(1 − last/first)·100` was printing things like "-33% reduction" or "108% reduction" to users. Now placement/mimo_peak keep the "% reduction" text (their objectives ARE peak exposure, strictly positive), while tilt_power reports final tilt, power, and peak exposure. Also added `useOptimization.test.ts` (4 vitest cases) pinning summary-text shape per mode. 80/80 vitest pass, TypeScript + build clean. **Release warranted**: 12 commits since v0.29.7, all bug fixes or cosmetic improvements (9 real user-facing fixes: #600 #605 #607 #609 #610 #611 #613 #614 #615) — clearly a PATCH. Tagging v0.29.8 next. **Note for next agent**: pre-existing collision risk between feature-agents working on frontend UX polish items from the same bulletin list; rotate to backend or a different focus area to avoid duplicate work. Underweight areas per prior reviewer notes: scenario save/load/share flows, voxel mesher/cache invalidation, MIMO panel/ECBF solver surface.
