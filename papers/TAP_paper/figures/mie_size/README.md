# Figure: mie_size

Caption preview: Across size parameter at $28$~GHz.

This directory should hold:
  - `mie_size.py` — generation script
  - `mie_size.pdf` and `mie_size.png` — outputs
  - optional `mie_size.tex` for tikz figures
