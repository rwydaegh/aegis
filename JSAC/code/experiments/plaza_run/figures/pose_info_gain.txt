Pose-information-gain ablation for §VII. Pose-aware (tier-A/B body orientation telemetry feeds the per-body $\bm{Q}$) vs.\ pose-ablate (Cauchy worst-case envelope on tier-A/B), same seed, plaza-specular path model. (a) ECDF of per-slot $\Delta$ sum-rate (aware $-$ ablate, Mbps) for four precoders; the curves collapse to a vertical at zero. (b) Per-body $P_{\mathrm{abs}}$ ECDF for multi-body ECBF, aware vs.\ ablate; the curves overlap to float32 precision. At the §VII.A nominal load the Brussels RL constraint is academically active (it appears in the multi-body QCQP) but practically slack ($\sim$\,4 orders of magnitude headroom), so the choice between true pose and a bounding envelope contributes zero measurable sum-rate gain.
