# Current paper workspace

Start with [the current spine](../docs/SPINE.md) and
[the current results inventory](../docs/RESULTS_INVENTORY.md).

The existing `paper.tex`, `body.tex`, `methods.tex`, `si.tex`, and their compiled
PDFs belong to the superseded eleven-city source-law manuscript. They are
retained for provenance and reusable derivations. They are not a drafting base
for current numerical claims.

`ROOFLINE_METHODS_RESULTS_DRAFT.tex` is the closest prose scaffold to the current
method, but it still contains unresolved notes and excess implementation detail.
Use the six-section structure in the current spine when the new paragraph tree
is created.

The current publication figures are indexed in
[figures/README.md](figures/README.md). Their scripts authenticate the source
data and write PDF, PNG, and JSON audit artifacts.

## Current authority order

1. The authenticated five-city JSON and artifact manifest
2. `docs/CURRENT_PRODUCTION_CONTRACT.md`
3. `docs/RESULTS_INVENTORY.md`
4. `docs/SPINE.md`
5. Historical manuscript files, only when they do not conflict with the sources
   above

Do not copy numbers from the old compiled PDFs into the new manuscript.
