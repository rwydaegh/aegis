% Introduce this figure at the start of the physical-configuration section with:
% "The simulation configuration is shown in Fig.\~\ref{fig:configuration}."
\begin{figure*}[!t]
  \centering
  \includegraphics[width=\textwidth]{figures/configuration/configuration.pdf}
  \caption{Simulation configuration at Prague Old Town Square. The registered
  panorama (a) is aligned with the traced support (b), and image evidence
  supplies the material field (c). The physical calculation (d) uses the
  250\,m support mesh, 502 elements of the route-visible roofline as source
  support, and 22 fixed pedestrian standpoints. An anatomical body is placed
  at every standpoint with its yaw fixed by the route tangent. The plan view
  omits propagation rays.}
  \label{fig:configuration}
\end{figure*}
