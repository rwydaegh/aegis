# Night log - 2026-03-24

## Agent 1 (Opus)

### What I did

**1. Fixed `_G_cache` invalidation bug in `engine.py`**
- The averaging matrix cache used `id(body)` as key, which Python can reuse after GC
- Replaced with SHA-256 checksum of `body.centroids` bytes for content-based keying
- Files changed: `src/aegis/engine.py`

**2. Added `PropagationPaths.concatenate()` classmethod**
- Merges paths from multiple sources (antenna panels, ray tracers, scenarios)
- Supports `reindex_elements=True` to keep antenna element indices disjoint
- Handles empty inputs, single inputs, and mixed-size path batches
- Files changed: `src/aegis/paths.py`, `tests/test_paths.py`

**3. Added 44 new tests (372 -> 416 passing)**
- `tests/test_kernel_properties.py`: Property-based Hypothesis tests
  - Sab >= 0 for levels 2-6 with random meshes and paths
  - Energy conservation bounds for levels 2-3
  - Level consistency (L4 q=0 == L3, L5 H=0 == L3)
  - Spatial kernel composability (mode API matches legacy levels)
  - Power scaling linearity
  - Coherent Q is Hermitian PSD
  - ECBF constraint satisfaction
  - Averaging reduces peaks, preserves total power
- `tests/test_edge_cases.py`: Boundary condition tests
  - Single triangle body, zero power, tiny power
  - 1000 paths stress test, all back-facing paths
  - Superposition (2x paths = 2x Sab)
  - Frequency thresholds (1 cm^2 averaging above 30 GHz)
  - Input validation (zero direction, negative index, shape mismatch)
  - Engine validation (level/mode exclusivity, missing required args)

### What I considered but skipped

- **Frontend changes**: No npm/node setup available, risky without full dev environment
- **Fresnel clipping for back-facing paths** (`_base.py:43`): mu clipped to [0,1] before Fresnel instead of zeroed. This is technically wrong (back-facing should get T=0 not T(0)), but changing it could break golden tests and needs careful physics review against the monograph. Flagging for Robin.
- **Negative curvature handling**: Levels 5-6 clamp H to max(H, 0), ignoring concave surfaces. Physically debatable. Left as-is.
- **ECBF infeasibility signaling**: When ECBF constraint is infeasible, the solver warns but the DosimetryResult has no flag. Could add a boolean field but it changes the API.

### Issues discovered

1. **Fresnel back-facing inconsistency**: Incoherent kernels clip mu to [0,1] for Fresnel (so back-facing gets T(mu=0) = nonzero), while coherent kernels zero out the Fresnel operator for back-facing paths. The incoherent path should probably zero out Fresnel for mu <= 0 too, but the ReLU/GELU activation already gates the final Sab to ~0, so the impact is minimal.

2. **Cache growth**: `_G_cache` and `_fibonacci_sphere_cache` grew unboundedly. Fixed by Agent 2.

## Agent 2 (Opus)

### What I did

Picked up all three suggestions from Agent 1, plus added two new features.

**1. Added `max_compliant_power()` to compliance module**
- Given a ComplianceResult at reference power P_ref, computes the maximum transmit power that keeps all ICNIRP checks passing
- Uses linear scaling: P_max = P_ref * min(limit / value) across all checks
- Works for both incoherent (0-6) and coherent (7-8) levels since S_ab is linear in P
- Files changed: `src/aegis/compliance/__init__.py`

**2. Added `DosimetryResult.scale(factor)`**
- Returns a new result with all power quantities scaled by a multiplicative factor
- Enables parameter sweeps: compute once at reference power, then scale to explore compliance boundary
- Correctly scales S_ab, P_abs, SAR, S_inc, Q, eigenvalues; preserves metadata, rho, x_star
- Files changed: `src/aegis/result.py`

**3. Added `DosimetryResult.evaluate_compliance()`**
- Runs a full ICNIRP 2020 compliance evaluation directly on a result
- Populates all available checks (sab_4cm2, sab_1cm2, sar_wb, sinc_local) from result fields
- Supports both general public and occupational scenarios
- Files changed: `src/aegis/result.py`

**4. Added `BodyMesh.from_arrays(vertices, normals=None, name="synthetic")`**
- Convenience classmethod for creating meshes from raw vertex arrays
- Auto-computes centroids, areas, and optionally normals from cross product
- Input validation for shape mismatches
- Files changed: `src/aegis/geometry/mesh.py`

**5. Added LRU cache eviction for `_G_cache` and `_fibonacci_sphere_cache`**
- `_G_cache` bounded to 16 entries (configurable via `_G_CACHE_MAX`)
- `_fibonacci_sphere_cache` bounded to 32 entries
- Evicts oldest entry when full (FIFO, since Python dicts are insertion-ordered)
- Prevents unbounded memory growth in long-running viewer sessions
- Files changed: `src/aegis/engine.py`, `src/aegis/geometry/projected_area.py`

**6. Added 41 new tests (416 -> 457 passing)**
- `tests/test_new_features.py`: Tests for all new features plus integration tests

### What I considered but skipped

- **Viewer bugs (BUG-1 through BUG-7)**: All are frontend (Three.js/React) issues. Without npm/node setup, risky.
- **Thread safety in viewer routes**: Global `app` state means concurrent sessions can interfere. Low priority since the viewer is typically single-user.
- **Precoder optimization module**: Useful but large feature needing JAX.

### Suggestions for next agent

- The `scale()` + `max_compliant_power()` combo enables a `power_sweep()` utility: compute once, then generate a compliance-vs-power curve. Could be a nice addition to `src/aegis/viz/`.
- Consider adding a CLI command for compliance checking: `python3 -m aegis.compliance --freq 28e9 --sab 15.0`
- The `from_arrays()` classmethod could simplify existing test helpers in `conftest.py`
- Explore whether the viewer backend can use `DosimetryResult.evaluate_compliance()` instead of its ad-hoc compliance logic in `routes/compute.py`

## Agent 3 (Opus)

### What I did

Picked up suggestions from Agents 1 and 2, fixed 4 bugs, added 77 new tests (457 -> 534), and built two new features.

**1. Fixed `level1_aggregate` array backend bug**
- `np.full()` hardcoded NumPy, breaking JAX backend
- Changed to `xp.full()` for backend-agnostic behavior
- Files changed: `src/aegis/kernels/level1_aggregate.py`

**2. Fixed `cole_cole_permittivity` array frequency support**
- `complex()` call and scalar `omega != 0` comparison both failed on array inputs
- Rewrote to use `np.asarray`/`np.atleast_1d` with proper scalar/array return handling
- This was a real bug: calling `cole_cole_permittivity(np.array([1e9, 28e9]), params)` crashed
- Files changed: `src/aegis/tissue/cole_cole.py`

**3. Fixed viewer compliance fallback using wrong measurement type**
- When spatial averaging was unavailable, `routes/compute.py` fell back to per-triangle peak S_ab and compared it against the 4 cm^2 spatially-averaged ICNIRP limit
- This is a measurement type mismatch that could report false compliance failures
- Now passes `None` to skip the check instead
- Files changed: `src/aegis/viewer/routes/compute.py`

**4. Added input validation to ECBF solver**
- Validates P > 0, P_abs_max > 0, h is 1D, Q is square, dimensions match
- Previously invalid inputs produced cryptic NaN/inf instead of clear errors
- Files changed: `src/aegis/coherent/ecbf.py`

**5. Added `power_sweep()` to compliance module**
- Computes compliance margin vs transmit power curve using linear scaling
- Returns power arrays (W and dBm), margin_db, compliant boolean, and p_max
- Builds on Agent 2's `max_compliant_power()` and `scale()` foundation
- Files changed: `src/aegis/compliance/__init__.py`

**6. Added `get_tissue_spectrum()` vectorized function**
- Single DB lookup + vectorized Cole-Cole evaluation across frequency array
- Replaces O(n) per-frequency loop in `plot_tissue_spectrum`
- Files changed: `src/aegis/tissue/database.py`, `src/aegis/viz/frequency_plots.py`

**7. Added compliance CLI: `python3 -m aegis.compliance`**
- Quick ICNIRP 2020 evaluation from command line
- Supports all quantities, occupational/public, JSON output, max compliant power
- Example: `python3 -m aegis.compliance --freq 28e9 --sab 15.0 --power 1.0`
- Files changed: `src/aegis/compliance/__main__.py`

**8. Added 77 new tests (457 -> 534 passing)**
- `tests/test_undertested_areas.py` (56 tests): Cole-Cole 4-pole model, Fresnel edge cases (energy conservation, branch selection, Hypothesis property tests), directivity SH fitting, averaging matrix properties (row-stochasticity, peak reduction), ECBF validation, exposure operator (Hermitian, PSD, rho bounds), TissueModel, level1 array fix verification
- `tests/test_power_sweep.py` (10 tests): power_sweep utility, vectorized Cole-Cole
- `tests/test_compliance_cli.py` (11 tests): CLI modes, JSON output, error handling

### What I considered but skipped

- **Fresnel back-facing inconsistency** (flagged by Agent 1): `_base.py:43` clips mu to [0,1] instead of zeroing. The ReLU gates the final result so impact is minimal, but it's technically wrong. Needs monograph review.
- **`run.py` compliance check** (line 135): Uses per-triangle peak without spatial averaging and `<` instead of `<=`. Same bug class as the viewer fix, but the batch runner needs mesh data to test. Flagging for Robin.
- **Thread-unsafe viewer state**: `/api/compliance/report` uses global `app.config` for compliance results. Low priority for single-user viewer.
- **Precoder optimization module**: Would need JAX.

### Issues discovered

1. **`run.py:135` compliance bug**: `peak_sab < ICNIRP_2020.sab_peak` uses per-triangle peak (not spatially averaged) and strict `<` (should be `<=`). Same pattern as the viewer bug I fixed.
2. **`plot_tissue_spectrum` was O(n)**: Called `get_tissue_properties()` per frequency point. Fixed with vectorized `get_tissue_spectrum()`.

### Night summary (all 3 agents)

| Agent | Tests added | Bugs fixed | Features added |
|-------|------------|------------|----------------|
| 1 | 44 | 1 (cache key) | PropagationPaths.concatenate |
| 2 | 41 | 1 (cache growth) | max_compliant_power, scale, evaluate_compliance, from_arrays |
| 3 | 77 | 4 (level1 xp, Cole-Cole array, viewer compliance, ECBF validation) | power_sweep, get_tissue_spectrum, compliance CLI |
| **Total** | **162** | **6** | **7 features** |

Final test count: 534 passed, 22 skipped (372 -> 534 overnight, +43%)

## Agent 4 (Opus)

### What I did

Picked up the run.py compliance bug flagged by Agent 3, fixed two more bugs, added two features, and wrote 60 new tests.

**1. Fixed `run.py:135` compliance bug**
- Changed `<` to `<=` for ICNIRP compliance (standard uses <=)
- Now prefers spatially averaged S_ab (from engine) over per-triangle peak
- Falls back to per-triangle peak with explicit conservative note when averaging unavailable
- Also adds `peak_sab_averaged` to the summary JSON output
- Files changed: `src/aegis/run.py`

**2. Fixed Debye permittivity division by zero at omega=0**
- `debye_permittivity()` divided sigma by omega without safety guard
- Added the same `safe_omega`/`np.where` pattern used in `cole_cole_permittivity()`
- Files changed: `src/aegis/tissue/cole_cole.py`

**3. Fixed `DosimetryResult.to_dict()` crash on complex arrays**
- `to_dict()` called `.tolist()` on complex numpy arrays, then `json.dumps()` failed with "Object of type complex is not JSON serializable"
- Complex arrays (Q, eigenvalues, x_star) now serialize as `{"real": [...], "imag": [...]}`
- Real arrays remain plain lists (no API change for incoherent results)
- Files changed: `src/aegis/result.py`

**4. Added `DosimetryResult.compare()` staticmethod**
- Compares multiple results (e.g. across fidelity levels) for convergence analysis
- Returns peak S_ab, P_abs, pairwise RMSE, max absolute error, and relative error
- Handles shape mismatches (skips incompatible pairs)
- Files changed: `src/aegis/result.py`

**5. Added `DosimetryEngine.sweep_levels()`**
- Runs multiple fidelity levels in one call, returns dict[int, DosimetryResult]
- Silently skips infeasible levels (missing A_ab/D_max for L0-L1, etc.)
- Default: all incoherent levels 0-6
- Combines naturally with `compare()` for convergence studies
- Files changed: `src/aegis/engine.py`

**7. Validate polarisation requires fresnel in spatial kernel**
- `polarisation=True` with `fresnel=False` was silently computing incorrect results
- Now raises ValueError with clear message
- Files changed: `src/aegis/kernels/spatial.py`

**8. Added 62 new tests (534 -> 596 passing)**
- `tests/test_batch_runner.py` (33 tests): SimulationConfig YAML round-trip, validation (bad level/freq/backend/keys), CLI parsing, synthetic path generation, inverse square law, compliance fix verification, DosimetryResult.compare(), sweep_levels, Debye omega=0
- `tests/test_field_channel.py` (16 tests): compute_field_channel shape/phase/accumulation/linearity, complex JSON serialization (Q, eigenvalues, x_star), level1_aggregate directivity paths
- `tests/test_averaging_consistency.py` (11 tests): apply_spatial_averaging vs precompute_averaging_matrix agreement, matrix properties (row-stochastic, nonneg, sparse format), constant field preservation, peak reduction

### What I considered but skipped

- **Fresnel back-facing "inconsistency"** (flagged by Agents 1, 3): Investigated and confirmed this is a false alarm. `_fresnel_core()` already zeroes T for `mu_real < 1e-10`, so after clipping mu to [0,1], back-facing paths get T=0. The code is correct. No change needed.
- **Thread-unsafe viewer state**: Real issue but low priority for single-user viewer, would need API redesign.
- **Viewer RT config bounds validation**: Should add bounds checking to radius/voxel_size query params. Left for Robin since it touches Flask routes without tests.
- **Simplifying conftest.py with `from_arrays()`**: Low value, existing helpers work fine and are used everywhere.

### Issues resolved from previous agents

1. **run.py:135 compliance bug** (flagged by Agent 3): Fixed. Now uses `<=` and prefers spatially averaged S_ab.
2. **Fresnel back-facing concern** (flagged by Agent 1): Investigated, confirmed code is correct. `_fresnel_core` zeroes T for mu < 1e-10.
3. **Complex JSON serialization**: Not previously flagged but was a real latent bug that would crash on any coherent (L7/L8) result serialization.

### Night summary (all 4 agents)

| Agent | Tests added | Bugs fixed | Features added |
|-------|------------|------------|----------------|
| 1 | 44 | 1 (cache key) | PropagationPaths.concatenate |
| 2 | 41 | 1 (cache growth) | max_compliant_power, scale, evaluate_compliance, from_arrays |
| 3 | 77 | 4 (level1 xp, Cole-Cole array, viewer compliance, ECBF validation) | power_sweep, get_tissue_spectrum, compliance CLI |
| 4 | 62 | 4 (run.py compliance, Debye omega=0, JSON complex, spatial kernel validation) | compare(), sweep_levels() |
| **Total** | **224** | **10** | **9 features** |

Final test count: 596 passed, 22 skipped (372 -> 596 overnight, +60%)
