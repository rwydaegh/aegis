# Agent bulletin board

Agents: read this at the start of your run. Append findings at the end.
Prune entries older than 7 days.

- [2026-04-20] code-reviewer: Focus area: **error handling and input validation at system boundaries** (follow-up to 2026-04-20 PR #686). PR #676's frontend work disabled the ZF button when `M_ant < K`, but the backend catchall at `mimo.py:_api_mimo_compute_impl` still mapped every exception from `compute_mimo_scene_with_bodies` to HTTP 500 — so any direct ZF/zf_exposure call with `M_ant < K` (scenario JSON, scripted clients, frontend-state-skew bugs) surfaced `ValueError("M_ant ({M}) must be >= K ({K}) for ZF precoding")` from `precoders.py:44` as a 500 with the full traceback in logs, when it is a user-input error. Same for `compute_precoder`'s `ValueError("Unknown precoder type: ...")` at `precoders.py:148` — any typo in `precoder_type` was 500. **Shipped PR #687**: pre-validate `precoder_type` against `{mrt, zf, mmse, zf_exposure}` and require `M_ant >= K` for `{zf, zf_exposure}` before entering `compute_mimo_scene_with_bodies`; both now return 400 with a specific message. Kept the generic `except Exception` → 500 path unchanged (the `test_compute_exception_returns_500` RuntimeError case still goes through as 500 because it's a legit internal failure). Added 8 regression cases: 5 parametrized unknown precoder_types (`"foo"`, `"ZF"`, `""`, `"maximum_ratio"`, `None`) all return 400 with `precoder_type` in message; 2 parametrized cases for `zf` / `zf_exposure` at M_ant=1, K=2 both return 400 with `M_ant`/`K` in message and assert `compute` was never called; 1 case confirming MMSE at M_ant<K still passes through (200) because MMSE is regularized and works at any M, K. 66/66 mimo route tests pass (was 58), 2839 fast tests pass. Ruff + format clean. **Note for next agent**: the remaining ValueErrors from `mimo/compute.py` (`"scene.tissue must be set"`, `"User X has no body mesh"`, `"User X has no center_paths"`) are internal invariants that `_build_scene` + `compute_mimo_scene_with_bodies` orchestration guarantee are met in practice, so leaving them as 500 is appropriate — if they ever fire, they indicate a genuine backend bug rather than bad user input. The underweight areas remain: ECBF infeasibility fallback warnings not surfaced to frontend (flagged by PR #676 / PR #682 authors), voxel mesher/cache invalidation (two prior reviewers deemed it non-issue — low yield to re-review).

- [2026-04-20] code-reviewer: Focus area: **error handling and input validation at system boundaries**. Independently repro'd the NaN/Inf `ref_power_dbm` bug on `/api/compliance/power-sweep` and `/api/compliance/heatmap` before reading far enough down the bulletin to notice the 2026-04-19 PR #(see entry above) had already shipped `_reject_non_finite` for the same surface. Opened PR #704 with a strict subset fix, then closed it as superseded when the rebase surfaced the existing helper. **No new ship.** Walked the rest of the boundary-guard surface on current master: `/api/compliance/spatial` (validates bbox finite, resolution int, receiver_height range); `/api/parametric-body` (NaN/Inf/ragged betas+pose all caught by PR #670); `/api/tissue/spectrum` (freq finite by PR #668); `/api/lsp-heatmap` (NaN/Inf by PR #701); `/api/compute` family (freq_hz subnormal by PR #657, non-dict users by PR #659, non-dict body by PR #650); `/api/mimo/compute` (precoder_type + M_ant/K by PR #687); `/api/environment/osm` + `/api/environment/3dtiles` (float coercion guards by PR #686); `/api/channel/generator` (NumClusters/NumSubPaths by PR #664). All clean — no new 5xx-on-bad-input surfaces found. Also noted but not worth shipping: (a) `/api/compliance/heatmap` validates `freq_hz` > 0 and finite but never forwards it to `compliance_heatmap()` — the heatmap sweeps `freq_min_hz..freq_max_hz` (7e9..100e9) regardless, so the parameter is effectively dead for callers. Not a correctness bug (matches the UI's "pick a reference" semantic), but a future API cleanup could either forward it or drop it. (b) `/api/compliance/power-sweep` doesn't validate `n_points` finiteness — Flask's `type=int` already rejects NaN/Inf before the handler sees it, so this is safe. Remaining underweight areas per prior bulletins: ECBF infeasibility warnings not surfaced to frontend, voxel mesher/cache invalidation.

- [2026-04-20] feature-agent: PR #689 merged — corrupt env cache files now treated as miss with cleanup, atomic writes via temp+rename. Drive-by fix in same PR: ruff PT006 in `tests/test_channel_generator.py` (pre-existing from #664 broke CI on the new ruff version, parametrize names switched from string to tuple). 2845 fast tests pass on master. **Notes for next agent**: ECBF infeasibility surfacing remains the highest-leverage open item — requires plumbing through `DosimetryResult` + `/api/compute` JSON + HUD. 32 commits since v0.30.0; still PATCH-shaped (`v0.30.1`), bundle further.

- [2026-04-20] code-reviewer: Focus area: **frontend data flow** (Zustand store updates, API response handling, loading/error states) — 9th+ pass on this surface, second since PR #671 merged. **No bugs found.** Re-reviewed the `resetScenarioScopedStores` helper at `aegis-web/src/hooks/useScenario.ts:27-33` — order is `sim.clearResults() → scene.clearScene() → basestations.clear() → mimo.reset() → optimize.reset()`, correctly does NOT clear `useAntennaStore` (per PR #662 semantics: scenarios without `antennaPos` leave user antennas untouched; `applySimulationState`'s `'antennaPos' in webState` branch handles clearing when the scenario declares it). Verified wordmark reset at `Toolbar.tsx:144-158` adds `setState({antennaPos: null}) + clearAntennas()` on top (full wipe) and the order is idempotent — the antenna-store subscription at `simulation.ts:355-370` fires no-op because `antennaPos` was already null. Traced three race-condition edge cases and cleared all: (a) stale MIMO compute after `reset()` — `setUserResult` guards on `!user`, `setSummaryStats` could write but `enabled=false` gates panel visibility and next `setEnabled(true)` triggers a fresh compute via `_configVersion` change; (b) stale basestation compute after `clear()` — narrow window, `applyDosimetryResult` might write stale sim results but subsequent useDosimetry pass overwrites within `debounce_ms`; (c) env store not cleared by `resetScenarioScopedStores` — `osmMeshData` is rendered only when `envSource==='osm'||'3dtiles'` (gated in `SceneRoot.tsx:417`), and `applyEnvironmentState` sets source explicitly. Verified new test file `aegis-web/src/hooks/__tests__/useScenario.test.ts` — 4 cases covering basestations/MIMO/optimize/idempotence all pass; the `'../../stores/simulation'` import-first workaround for the antenna↔simulation module init cycle is correct. Walked the loading/error surface once more: `environment.ts` `freshAbort` + `reportUnexpected` 429/502/503/504 skip list unchanged; `_dosimetryResult.ts` AbortError→network→4xx→Sentry ladder still intact; `useMIMODosimetry` generation counter + `isCurrent()` check on every awaited step prevents cross-compute contamination. 103/103 vitest pass, 2016 fast Python tests pass + 74 skipped (optional-dep gates). **No ship.** **Note for next agent**: this area is now thoroughly covered; rotate to the bulletin-flagged underweight areas — ECBF infeasibility warning surfacing (multi-layer: `DosimetryResult` → `/api/compute` JSON → HUD, highest leverage per PR #689 author), voxel mesher/cache invalidation (two prior reviewers concluded low-yield), and the remaining `float()`/`int()` seams in `optimize.py:222-223,238-241`.

- [2026-04-20] feature-agent: Focus area: **remaining `float()` seams in `/api/optimize`** flagged by the 2026-04-20 code-reviewer entry at `optimize.py:222-223,238-241`. Reproduced via Flask test client: `mimo_peak` mode with `p_max="abc"` / `signal_threshold="NaN"` / `p_max=["bad"]` leaked `TypeError`/`ValueError` tracebacks on the SSE stream (500). `tilt_power` mode with `antenna_direction=[1,2]` / `antenna_direction=["x",0,-1]` / `tilt_init_deg="Infinity"` / `power_init_dbm=[60]` / `icnirp_limit=0` / `T0=-5.0` all either 500'd or silently accepted garbage (NaN/Inf flowing into downstream `np.cos`/`np.sin`/`10**(p/10)` and corrupting the stream). A particularly sneaky case: a stale `_last_dosimetry_stats["T0"] = NaN` poisoned every subsequent tilt_power run because `params.get("T0", cached)` fell back to the NaN default. **Shipping as PR**: added `_require_finite_float(params, key, default, *, positive=False)` and `_require_finite_vec3(params, key, default)` helpers at the SSE boundary of `optimize.py`, then applied them in `_build_mimo_peak_config` (`p_max` positive, `signal_threshold` finite) and `_build_tilt_power_config` (`antenna_direction` vec3, `tilt_init_deg` finite, `power_init_dbm` finite, `icnirp_limit` positive, `T0` positive with cache-NaN sanitization). All bad-input cases now fail fast with a field-specific 400 before the SSE stream opens. Regression tests: 19 new cases in `tests/test_viewer_optimize_route.py::TestMimoPeakInputValidation` + `TestTiltPowerInputValidation` (51/51 pass, was 32). Valid-path SSE streams unchanged. Ruff + format clean. **Notes for next agent**: (1) 3D Tiles silent non-render at `src/aegis/environment/tiles.py::_parse_glb_primitive` — the parser reads raw POSITIONs but does NOT apply the tileset's per-tile `transform` matrix or the glTF scene/node `matrix`/`translation`/`rotation`/`scale` transforms, and silently ignores unhandled glTF extensions. Google Photorealistic tiles heavily use node transforms, so meshes load but render at origin/wrong orientation, indistinguishable from "didn't load" to the user. Best tackled by someone with a Google 3D Tiles API key to verify against a live tileset. (2) ECBF infeasibility surfacing remains the highest-leverage multi-layer item (`DosimetryResult` → `/api/compute` JSON → HUD). (3) `_build_placement_config` was left untouched — its validation surface is larger and already has a mix of try/except around DiffeRT calls; follow-up candidate. 33 commits since v0.30.0 now; still PATCH-shaped (`v0.30.1`).

- [2026-04-20] code-reviewer: Focus area: **viewer feature completeness — trace user flows end-to-end, find dead code paths and unfinished features**. **No bugs found. No ship.** Walked the scenario load → store reset → user interaction → compute pipeline end-to-end against `useScenario.ts`, `Toolbar.tsx`, `simulation.ts`, `antenna.ts`, `mimo.ts`, `basestations.ts`, `optimize.ts`, `MIMOPanel.tsx`, all `routes/` Python endpoints, and `default_config.json` scenario declarations. Three dead-code/dead-config pockets confirmed present (still all "harmless but misleading", not bugs): (1) `aegis-web/src/stores/mimo.ts:61, 104` — `lastSuccessfulUserCount: number` field on the MIMO state interface and `INITIAL_STATE`, never set anywhere and never read (orphan from PR #614 alongside `lastComputeError`/`retryCompute`); (2) `src/aegis/viewer/default_config.json:822-823` — `clearResults: true, clearScene: true` on the `empty` scenario `webState` are dead because `useScenario.loadScenario` now unconditionally calls `resetScenarioScopedStores()` regardless of these flags (PR #671 made them no-ops); (3) `aegis-web/src/api/types.ts:150-151` — `instant: boolean` and `autoCompute: boolean` on `ScenarioEntry` are required in the type but never read by the frontend (one comment at `useScenario.ts:119` says "Apply instant state" but doesn't gate on the flag). Re-verified PR #676's `setPrecoderType` M<K guard via `precoderRequiresMgeK`/`needsMoreAntennas` — works correctly, MMSE preserved past M, ZF/zf_exposure fall back to MRT; **bulletin's prior open concern about `setPrecoderType` lacking M<K guard is fully addressed**. Re-verified PR #687 backend 400-not-500 mapping for unknown precoder_type and ZF/zf_exposure M<K. Re-verified PR #689 atomic env cache write + corrupt-cache recovery. ECBF infeasibility still routes through `warnings.warn` at `src/aegis/coherent/ecbf.py:155-163, 171-176, 186-192, 198-199` without HUD surfacing — unchanged, still the highest-leverage open multi-layer item flagged by 5+ prior bulletin entries. Test baseline: 2845 fast tests pass + 1 pre-existing external `test_viewer_integration_environment.py::TestEnvironmentMalformed::test_unicode_keys_ignored` Overpass 406 (documented in 2026-04-20 PR #688/#691 entries). Decided NOT to ship a cleanup PR for the three dead pockets per workflow guidance — none are bugs, all touch reviewed/recently-shipped code where the maintainer is already aware (PR #671 trimmed `clearScene`/`clearResults`'s reach, PR #676 owned the MIMO state surface). **Notes for next agent**: (1) ECBF→HUD warning surfacing remains the single highest-leverage open item — `DosimetryResult` field, `/api/compute` JSON, HUD chip; multi-layer but tractable. (2) If anyone takes a cleanup-only PR pass, the three dead pockets above + `_clear_voxel_cache` raytracer-cache-skip noted in 2026-04-19 feature-agent entry could land together. (3) 33 commits since v0.30.0 still PATCH-shaped (`v0.30.1`); no change in release recommendation.

- [2026-04-20] code-reviewer: Focus area: **React components — state management, error boundaries, stale closures, missing cleanup**. **No bugs found. No ship.** Overlapping pass with the 2026-04-20 feature-completeness entry above — confirmed everything in that entry plus an explicit re-sweep of React cleanup patterns: subagent-assisted review across `useDosimetry`, `useMIMODosimetry`, `useOptimization`, `useBodyLoader`, `useGltfBody`, `useVoxelLoader`, `useBaseStationsDosimetry`, `usePhysics`, `FollowCamera`, `CoverageMap`, `EnvironmentOSM`, `EnvironmentTerrain`, `AntennaArray`, `BaseStationMarkers`, `ClusterPaths`, `LSPHeatmap`, `VoxelField`, `GuidedTour`, `HistoryScrubber`, `KeyboardHelp`, `BugReporter`, `ServerInfoBadge`, `StatusBar`, `ScenePanel`, `Toolbar.tsx`, `PatternBrowserPanel.tsx`, `StochasticPanel.tsx`, `useKeyboard.ts`, `useScenario.ts`. Generation-counter + AbortController pattern consistent throughout (`useDosimetry` compute+LSP, `useMIMODosimetry` isCurrent, `useBaseStationsDosimetry`, `PatternBrowserPanel` search+load, `useOptimization` abortRef-identity). Three.js disposal paired with every set (body store, MIMO user bodies on `removeUser`/`reset`, LSPHeatmap/EnvironmentOSM/AntennaArray on unmount). Every addEventListener/setTimeout/setInterval/RAF/EventSource has a paired cleanup. Spot-checked two subtle patterns: (a) `PatternBrowserPanel`'s debounce cleanup works even when `debounceRef.current` is overwritten on each effect run, because cleanup fires before next effect body so the ref always points to the active timeout at cleanup time; (b) `StochasticPanel`'s `useEffect(..., [])` with stale `preset` closure is neutralized by the second `useEffect([preset, allPresets])` that always runs after `allPresets` is populated (the stale-closure setPresetParams call is immediately overwritten by the correct one). **Prior bulletin concern about `setPrecoderType` M<K guard is now stale — PR #676 added it.** 103/103 vitest pass, `npx tsc --noEmit` clean. Fast Python suite: 1017 passed + 2 pre-existing flakes (`TestEnvironmentMalformed::test_unicode_keys_ignored` Overpass 406, and `test_viewer_integration_rt.py::test_invalid_mode` scene-path-before-mode ordering). **Note for next agent**: React-cleanup surface is well-covered. ECBF→HUD warning surfacing remains the single highest-leverage open item per multiple prior entries. Overlapped with a concurrent 2026-04-20 pass — expected under parallel agents; merge-resolved bulletin preserves both.

- [2026-04-20] feature-agent: Follow-up to PRs #676/#687 which covered MIMO precoder feasibility but left the toggle-off cleanup hole. **Fixed MIMO `setEnabled(false)` leaving per-user state alive.** `setEnabled(false)` at `aegis-web/src/stores/mimo.ts:109-113` was only clearing `enabled` + `lastComputeError`, leaving `users` Map (with undisposed `bodyGeometry` on the GPU), `focusedUserId`, `controlledUserId`, `summaryStats`, `precoderWeights`, `lastSuccessfulUserCount`, and `_nextUserNumber` intact. Repro: enable MIMO → add User 2 → run compute → toggle MIMO off → toggle back on → both users re-appear with stale results, the `users.size === 0` auto-add branch never fires, and stored `precoderWeights` render on an array that may have been reconfigured between sessions. **Fix**: on `setEnabled(false)`, dispose per-user body geometries and reset per-user state while preserving `arrayConfig`, `precoderType`, `focusPoint`, `showAllHeatmaps`, `showArrayPattern` so a mode toggle keeps the user's array configuration but re-enters via the auto-add User 1 flow. Added 2 regression tests to `aegis-web/src/stores/__tests__/mimo.test.ts` (per-user wipe + config preservation; toggle cycle re-adds User 1). Works cleanly against PR #676 (`precoderRequiresMgeK` + `setPrecoderType` guard), which landed on master concurrently. 105/105 → 113/113 vitest pass after auto-merge with #676's tests, `npx tsc --noEmit` clean, `npm run build` succeeds. **Notes for next agent**: (1) ECBF infeasibility fallback at `src/aegis/coherent/ecbf.py:155-163, 171-176, 186-192, 198-199` still only emits `warnings.warn` — thread through `DosimetryResult` + `/api/compute` → HUD chip remains the highest-leverage open item. (2) Coverage `complianceZone` isn't cleared on scenario change (`useScenario.ts:65-73`); low severity because `complianceZoneEnabled` gates use. (3) `osmMeshData` persists across scenario source-changes (brief visual flicker during re-fetch); `SceneRoot` gates `<EnvironmentOSM />` on `envSource === 'osm'|'3dtiles'` so source-changes to 'none' hide cleanly. (4) Open Sentry #666/#680 were already triaged as duplicates pre-deploy-7bdd91a, Sentry release tracking will auto-resolve once production catches up. 34 commits since v0.30.0 now; still PATCH-shaped (`v0.30.1`).
- [2026-04-20] code-reviewer: Focus area: **3D scene — Three.js resource lifecycle, geometry disposal, camera state, rendering**. **No bugs found.** Walked every component in `aegis-web/src/components/scene/` plus `hooks/useGltfBody.ts`, `hooks/useBodyLoader.ts`, `hooks/useMIMODosimetry.ts`, `stores/scene.ts`, `stores/mimo.ts`, `hooks/useScenario.ts`. Disposal patterns are consistent and correct: `EnvironmentOSM`/`EnvironmentTerrain`/`AntennaArray`/`HullMesh`/`LSPHeatmap`/`ComplianceRing`/`DistanceLine`/`SceneGeometry`/`ClusterPaths`/`Antenna` use the `useMemo` create → `useEffect` cleanup pattern with `geo.dispose()` (or a `prevGeoRef` pattern where the dep changes inline); `BodyMeshInstance` mutates the existing color buffer in place (correct); `VoxelField` uses count-based `instancedMesh` args. Scene store `setBodyGeometry` disposes prior, MIMO store `removeUser`/`reset`/`setUserBodyGeometry` all dispose user body geometries, `useBodyLoader` aborts + disposes on unmount, `useScenario.resetScenarioScopedStores` wipes all per-demo stores. Camera: `FollowCamera` listeners removed on cleanup via refs; `SceneRoot`'s OrbitControls + `CameraController`/`CameraInitializer`/`CameraSyncer` chain uses refs and run-once guards. **Investigated and cleared one suspicion:** `useGltfBody.ts` runs `scene.clone(true)` then disposes all meshes'/materials' geometries on unmount. Verified via `node_modules/@react-three/drei/core/Gltf.js` that `useGLTF` uses `useLoader(GLTFLoader, path)` cache, and via a local `scene.clone(true)` test that the clone shares geometry/material references with the cached scene. So the cleanup does call `.dispose()` on the cached refs — BUT the JS-side `BufferAttribute` arrays stay intact, so Three.js silently re-uploads to the GPU on the next render when the same GLB is re-mounted. This is a redundant-upload perf cost, not a crash or correctness bug; below the 70% confidence bar to file. Worth revisiting if the GLTF phantom workflow becomes hot. Fast tests: 2 pre-existing flakes unrelated (Overpass 406 in `test_viewer_integration_environment.py::test_unicode_keys_ignored` and the documented `test_viewer_integration_rt.py::TestComputeRtInvalidPayload::test_power_dbm_out_of_range` scene-path-check-before-mode-check ordering). No ship. **Overlap note**: this pass overlapped with 2026-04-20 concurrent entries that already covered most of this surface, including the 51-line React-cleanup subagent sweep; my findings corroborate theirs (same clean outcome, same ECBF→HUD surfacing as highest-leverage open item). Prior bulletin concern about `setPrecoderType` M<K guard is stale — PR #676 shipped `precoderRequiresMgeK`. **Remaining highest-leverage item** per multiple prior entries: ECBF infeasibility warning surfacing (`src/aegis/coherent/ecbf.py:155-163` → `DosimetryResult` → `/api/compute` JSON → HUD chip, multi-layer but tractable).

- [2026-04-20] feature-agent: Focus area: **surface `path_contributions` / `path_importance` analysis functions to the viewer UI**. `src/aegis/analysis.py` already ships `path_contributions` + `path_importance` with golden-test coverage and a physics derivation (`c_{m,n} = T_avg(mu) * ReLU(mu) * power_n`), but nothing on the frontend has ever called them — the "which paths drive the peak exposure" question users ask after every RT compute had no answer in the UI. **Shipping as PR**: new `/api/analyze/path-contributions` GET route (session-scoped, reads `_last_rt_paths` + `_last_dosimetry_body` from the existing cache; added `_last_rt_tissue` caching alongside so analysis operates on the exact tissue used for the compute). Supports `top_k` (default 10, clamped to 100) and optional `triangle_index` (default = peak). Returns two ranked views: top paths at the peak triangle (ranked by contribution to S_ab) and top paths by whole-body absorbed power (path_importance). New `PathInsightsSection` in the Analysis panel wired to `useActiveSimulation` + `useSceneStore.rtPaths` — fetches automatically after any RT compute, shows two tables with LOS/NLOS badges, per-row W/m², % of peak, and an inline share bar; gracefully falls back to "Run a ray-traced compute…" when no RT paths cached. All 3 RT compute routes (`rt.py`, `sionna_rt.py`, `voxel_rt.py`) updated to cache tissue via `_cache_dosimetry_for_export(..., tissue=pp["tissue"])`. **Regression tests**: `tests/test_viewer_path_contributions.py` (6 cases: 404 on no cache, paths ranked by contribution, top_k clamped, custom triangle_index, empty-paths edge case, JSON has no Infinity/NaN) — all pass. `aegis-web/src/api/__tests__/pathContributions.test.ts` (4 vitest cases: URL construction, query params, parsed response, error-status rejection) — all pass. 113/113 vitest + 6/6 new pytest + 13/13 `test_viewer_analysis_routes` pass; `npx tsc --noEmit` clean; `vite build` succeeds. Ruff + format clean on changed files. **Notes for next agent**: (1) ECBF→HUD warning surfacing remains the highest-leverage multi-layer open item. (2) The new route only surfaces the RT compute's paths; base-station multi-source coherent runs have their own path cache (different lifecycle) and could get a second panel variant later. (3) 35 commits since v0.30.0; still PATCH-shaped (`v0.30.1`).

- [2026-04-20] feature-agent: Focus area: **ECBF infeasibility surfacing for MIMO path** — the highest-leverage multi-layer item flagged by 5+ prior bulletin entries. PR #706 had already wired single-user compute (`rt.py`/`sionna_rt.py`/`voxel_rt.py` via `_responses.py::_run_dosimetry_with_warnings` → `collect_ecbf_warnings`), but MIMO compute at `src/aegis/viewer/routes/mimo.py` was untouched — `solve_ecbf`'s three `warnings.warn` fallback paths (`ecbf.py:155-163, 171-176, 186-192, 198-199`) were swallowed on the `/api/mimo/compute` POST, so MIMO users running level-8 with infeasible constraints saw silent fallback to min-absorption baseline. **Shipping as PR**: (a) wrapped `compute_mimo_scene_with_bodies` in `warnings.catch_warnings(record=True) + warnings.simplefilter("always")`, reused `collect_ecbf_warnings` from `viewer/compute.py` to filter for `("ECBF", "absorption", "infeasible")` markers, attached to response + cached summary (`mimo.py:386-402`); (b) forwarded through `/api/mimo/summary` GET (`mimo.py:466-470`); (c) added `ecbf_warnings?: string[]` to `DosimetryStats`, `MIMOComputeResponse`, `MIMOSummary` in `aegis-web/src/api/types.ts`; (d) exported `applyMIMOSummary` from `useMIMODosimetry.ts` and added response→summary copy plus one-shot toast via `useNotificationStore.addNotification('warning', ...)` so users see the fallback surface immediately on compute completion; (e) new reusable `EcbfWarningChip.tsx` (amber headline "ECBF precoder fell back to a safety baseline" + expandable chevron listing raw messages, matches existing warning-banner styling); (f) rendered in `MIMOPanel` after the existing degenerate-channel yellow banner and in `CompliancePanel` (main render + no-compliance branch, both gated on `stats.ecbf_warnings?.length > 0`). **Regression tests**: 4 new in `tests/test_viewer_mimo_routes.py` (70/70 pass, was 66) — compute captures ECBF warnings and filters non-ECBF noise; compute omits field when clean; summary surfaces cached ECBF warnings; summary omits when absent. 4 new vitest in `aegis-web/src/hooks/__tests__/useMIMODosimetry.test.ts` (119/119 pass, was 115) — response→summary copy; summary wins over response; notification added on warning; no side effects on clean runs. Backend `tests/test_viewer_ecbf_warnings.py` (9/9, unchanged) still green. `npx tsc --noEmit` clean, `npm run build` succeeds, ruff check + format clean. **Notes for next agent**: (1) ECBF→HUD surfacing is now end-to-end complete for both single-user and MIMO paths; the long-standing "highest-leverage open item" is closed. (2) `CompliancePanel` gates the ECBF chip on `stats` existence — when MIMO compute fails before per-user stats land, the `mimoError` branch runs instead (already shows retry UI) so ECBF messaging is not lost but also not surfaced; if future users want the warning to persist through a failed re-compute, pipe it into `lastComputeError` too. (3) The notification uses a static headline ("fell back to a min-absorption baseline") that covers all 3 solver fallbacks; if we later want per-mode wording (infeasible vs lambda-bracket vs bisection-failure), the raw message strings are already threaded through to `summary.ecbf_warnings`. (4) 36 commits since v0.30.0; still PATCH-shaped (`v0.30.1`).

- [2026-04-20] polish-agent: Focus area: **keyboard shortcut help modal** — spotted a rendering bug in `aegis-web/src/components/hud/KeyboardHelp.tsx` where alternative-key shortcuts were rendering as chords. `ShortcutRow` inserts a `+` separator between every key in the `keys` array (for chord-style shortcuts like `Shift + B`), but four data rows stored alternative keys as separate array entries: `['W','A','S','D']`, `['Q','E']`, `['←','→','↑','↓']`, and `['1','-','9']`. These rendered as `W + A + S + D`, `Q + E`, `← + → + ↑ + ↓`, and `1 + - + 9` — implying you press them simultaneously, which is nonsensical. The existing Shift+arrows row already demonstrated the correct pattern for "alternatives in one pill" by concatenating into a single string (`['Shift', '←→↑↓']`). **Shipped PR**: concatenated the four affected rows into single pills (`['WASD']`, `['QE']`, `['←→↑↓']`, `['1-9']`) so each renders as a single key pill matching user intent. Chord shortcuts (`['Shift','B']`) stay unchanged. Verified: `npx tsc --noEmit` clean, 119/119 vitest pass, `npm run build` succeeds. **No regression test added** — pure data change in a component with no existing rendering tests (test env is node, not jsdom) and the visual contract is self-evident; cost of a jsdom + testing-library setup is not justified for a 4-line data fix. **Notes for next agent**: Minor dead-code pockets still linger per prior bulletin entries (mimo.ts `lastSuccessfulUserCount`, default_config.json `clearResults`/`clearScene` on `empty`, types.ts `instant`/`autoCompute` on `ScenarioEntry`) — a single cleanup PR could bundle these if the maintainer wants tidying.

- [2026-04-20] code-reviewer: Focus area: **viewer feature completeness — trace user flows end-to-end, find dead code paths and unfinished features**. Instead of feature completeness, found **master CI was red**: 28 tests broken by PR #710 (`ca1a032 Reject non-finite JSON literals at the parser, not per-route`). PR #710 relaxed two tests (`test_power_dbm_nan_rejected`, `test_freq_nan_rejected`) to accept either the legacy field-level message or the parser-level "Invalid JSON body: non-finite JSON literal ..." message, but missed the same pattern in four other tests written in earlier PRs: `tests/test_viewer_non_dict_body_guards.py::test_lsp_heatmap_nonfinite_numeric_inputs_are_400` (7 parametrized cases from PR #701), `tests/viewer/test_compute_routes.py::TestComputeRoute::test_invalid_stochastic_override_value` (4 NaN/Inf cases out of 10 from PR #664), `tests/viewer/test_compute_routes.py::TestComputeRoute::test_invalid_stochastic_float_override_value` (14 NaN/Inf cases out of 35 from PR #697), and `tests/viewer/test_compute_routes.py::TestComputeVoxelRtExtended::test_nonfinite_power_dbm_rejected_before_modal` (3 cases from PR #707). All 28 asserted a field-specific substring in the error body, but after PR #710 the parser rejects the raw `NaN`/`Infinity`/`-Infinity` JSON literals at decode time with a generic message, so the field name never reaches the error string. **Shipping as PR**: applied PR #710's same relaxation pattern — `assert field in error or "invalid json body" in error` — to all four test locations. Non-NaN bad values (None, "abc", [1]) still reach the per-field validator as before and the field-name assertion still fires for them, so non-parser-rejectable bad inputs stay pinned. Also noticed a small asymmetry worth a future follow-up (not shipped here): `/api/compute/voxel-rt` uses `request.get_json(silent=True)` and returns its own generic "Invalid or missing JSON body" instead of the parser's specific "non-finite JSON literal" message — migrating it to `get_json_dict()` would surface a clearer error, but behavior is correct (400) so below 70% bar to file. Fast suite goes from 30 failed → 2 failed; the remaining 2 (`test_viewer_integration_rt.py::TestComputeRtInvalidPayload::test_invalid_mode` and `test_power_dbm_out_of_range`) are the pre-existing scene-path-before-mode-check-ordering flakes documented in prior bulletin entries and confirmed against pristine `origin/master` (not my regression). Ruff + format clean on touched files. No collisions on master grep for `"invalid json body"` / `parser-level`.

- [2026-04-20] polish-agent: **Fixed stale compliance zone leaking across scenario switches.** `resetScenarioScopedStores` in `aegis-web/src/hooks/useScenario.ts:27-34` cleared basestations, MIMO, and optimizer stores but left `useCoverageStore.complianceZone` (cached `SpatialComplianceResult`) and `complianceZoneEnabled` (user toggle) untouched. Repro on coverage_globe: set up scene at location A → enable ICNIRP compliance zones → switch scenario (even to `empty`) → switch back to `coverage_globe`: the old zone overlay re-rendered with `n_stations: N` from location A over a now-empty basestations store, since the CoverageMap `buildComplianceZoneLayer(complianceZone)` check at `CoverageMap.tsx:97-99` only gates on `complianceZoneEnabled`, which was still true from the prior session. Same staleness via `CoverageHud.handleBackToGlobe` (clears basestations on "Back to globe" but left the zone cached). **Fix**: (a) `clearComplianceZone` in `stores/coverage.ts:148` now also sets `complianceZoneEnabled: false` — the method had zero prior callers so semantics were free to extend; (b) `resetScenarioScopedStores` calls it; (c) `handleBackToGlobe` calls it too. One new regression test in `hooks/__tests__/useScenario.test.ts` covering both fields. 120/120 vitest pass (was 119), `npx tsc --noEmit` clean, `npm run build` succeeds. **Notes for next agent**: (1) The three dead-code pockets flagged by the 2026-04-20 feature-completeness review are still present (`lastSuccessfulUserCount` MIMO orphan, `clearResults`/`clearScene` no-ops on `empty` scenario, `instant`/`autoCompute` required-but-unread) — low-severity cleanup if anyone takes a bundled pass. (2) `osmMeshData` persists across source-changes causing brief visual flicker — narrow-window cosmetic, below polish bar. (3) The two remaining Sentry issues #666/#680 are triaged duplicates; release tracking will auto-resolve.

- [2026-04-20] code-reviewer: Focus area: **React components — state management, error boundaries, stale closures, missing cleanup** (re-sweep targeting newly landed PR #712 path-contributions + PR #713 ECBF→HUD + PR #714 test-fixes + PR #715 compliance-zone fix). **No bugs found. No ship.** Re-read every new/touched component: `PathInsightsSection.tsx` (generation-token `fetchTokenRef`, effect deps `[stats, rtPaths]`, AbortController-free but `isActive` flag on token mismatch prevents stale writes; only mounted via `<Section open>` conditional so unmount-during-fetch cleanup is trivial); `EcbfWarningChip.tsx` (pure useState, no effects, no cleanup required); `useMIMODosimetry.applyMIMOSummary` ECBF-warnings copy path (mutates the fresh `summary` object from fetch — safe because never aliased; one-shot toast via `addNotification` fires exactly once per compute completion as intended); `MIMOPanel.tsx` + `CompliancePanel.tsx` chip wiring (gates on `stats.ecbf_warnings?.length > 0`, narrow-window gap in `CompliancePanel`'s `mimoError && !stats` branch is documented in the PR #713 notes and deemed acceptable); `stores/mimo.ts::setEnabled(false)` now disposes per-user geometries + preserves `arrayConfig`/`precoderType`/`focusPoint` (per PR #709 feature-agent entry — verified dispose call order, Map iteration, idempotent re-toggle). Investigated and cleared one backend-side staleness suspicion: `src/aegis/viewer/routes/compute/_responses.py::_cache_dosimetry_for_export` at line 51 only writes `_last_rt_paths`/`_last_rt_tissue` when args provided, and non-RT dosimetry compute (`dosimetry.py:521`) calls it without those args — so after an RT-compute → non-RT-compute sequence, `_last_rt_paths`/`_last_rt_tissue` go stale while `_last_dosimetry_body` is fresh. `_zero_paths_response` at `_responses.py:315` clears `_last_rt_paths` but not `_last_rt_tissue`. This could theoretically surface mismatched data on `/api/analyze/path-contributions`, but the frontend fully mitigates: `setPathSource(non-rt)` clears `rtPaths` in `useSceneStore` and `PathInsightsSection`'s render guard `!rtPaths || rtPaths.length === 0` short-circuits before the effect fetches. Below 70% confidence bar (requires a contrived state where frontend `rtPaths` stays populated while backend caches drift — no clear user path). Test baseline: 119/119 vitest pass, `npx tsc --noEmit` clean, Python fast subset 85/85 on touched modules. Prior bulletin noted this surface was already saturated by earlier 2026-04-20 passes (line 21 "3D scene — Three.js lifecycle" pass; line 18 "React components — state management" pass) — this re-sweep just validated PR #712/#713/#714/#715 didn't regress any cleanup patterns. **Notes for next agent**: (1) If a cleanup PR bundles the dead-code pockets noted at line 16 and the `_last_rt_tissue` non-clear in `_zero_paths_response`, it would be a tidy defense-in-depth pass — still below individual-bug bar. (2) React-components surface is now at saturation (9+ passes this day); rotate to a different area. (3) 37 commits since v0.30.0; still PATCH-shaped (`v0.30.1`), bundle more before tagging.
- [2026-04-20] code-reviewer: Focus area: **frontend data flow — Zustand store updates, API response handling, loading/error states** — post PR #712 (path contributions) and PR #713 (MIMO ECBF warnings). Overlaps with the adjacent React-components re-sweep above (which also flagged `_last_rt_tissue` staleness and deemed it sub-threshold); this pass contributes one additional unique observation (a). Reviewed `aegis-web/src/components/panels/analysis/PathInsightsSection.tsx`, `aegis-web/src/components/hud/EcbfWarningChip.tsx`, `aegis-web/src/hooks/useMIMODosimetry.ts::applyMIMOSummary`, `CompliancePanel.tsx`/`MIMOPanel.tsx` ECBF chip integration, `aegis-web/src/api/client.ts::fetchPathContributions`, `aegis-web/src/api/types.ts` `ecbf_warnings` fields, `src/aegis/viewer/routes/analysis.py::_path_contributions_impl`, `src/aegis/viewer/routes/compute/_responses.py` cache writes, `aegis-web/src/stores/environment.ts` + `EnvironmentEmptyState.tsx` (PR #709 cleanup). **No bugs found. No ship.** 119/119 vitest pass. Three sub-70%-confidence observations for future agents: (a) `PathInsightsSection.useEffect` at `aegis-web/src/components/panels/analysis/PathInsightsSection.tsx:43-69` — the early-return branch (lines 44-48, when stats/rtPaths clear) does NOT increment `fetchTokenRef.current`. A fetch started in a prior effect run can resolve after stats/rtPaths clear; the success branch's stale `setResult(data)` is hidden by the render gate at line 71, but the error branch still fires `Sentry.captureException(err)` + `useNotificationStore.addNotification('warning', 'Path insights unavailable for this compute')` even after the user has left RT mode. Minimal user impact (rare, and the error message is technically accurate), but a one-line fix (`++fetchTokenRef.current` before the early `return`) would eliminate the spurious Sentry/toast. (b) Enabling MIMO after a single-user RT compute doesn't invalidate backend `_last_rt_paths` / `_last_dosimetry_body` / `_last_rt_tissue` — the PathInsightsSection (driven by `useActiveSimulation().stats` + `useSceneStore.rtPaths`) could in principle render cached single-user path contributions alongside MIMO summary stats if the UI reached that state, though the current UI gates the Analysis panel on single-user stats so this is theoretical. Technically correct within cached frame of reference. (c) `src/aegis/viewer/routes/compute/_responses.py::_zero_paths_response` clears `_last_rt_paths` and `_last_dosimetry_body` but not `_last_rt_tissue` — minor leak; tissue cache persists after a zero-paths compute until the next RT compute rewrites it. No correctness impact because `_path_contributions_impl` requires all three to be present (404 otherwise). **Note for next agent**: frontend data flow is thoroughly covered (10+ passes); ECBF HUD surfacing closed in PR #713. Rotate to: (i) `src/aegis/analysis.py::path_importance` argsort tiebreak behavior for equal `importance` values (currently relies on NumPy stable sort default — worth a golden test with explicit duplicate-power scenarios); (ii) voxel mesher/cache invalidation (two prior reviewers deemed low-yield; unchanged); (iii) `_build_placement_config` in `optimize.py` (flagged by 2026-04-20 feature-agent entry as follow-up candidate). 36 commits since v0.30.0 now released as v0.31.0 per `6149aea`; cadence on track.

## In progress

<!-- Mark what you are working on to avoid collisions. Clear after merge or if stale >6h -->
