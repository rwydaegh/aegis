% NEXT: Two simplifications act on the exact law in~\eqref{eq:Sab-exact}.
% NEXT: First, the polarization reduction~\eqref{eq:Sab-Tavg} removes the
% NEXT: polarization dependence. Second, we substitute $\Tavg(\theta) \to T_0$ and reinstate
% NEXT: self-shadowing through the binary visibility
% NEXT: $\Vis(\rr,\khat) \in \{0,1\}$. The exact law reduces to the
% NEXT: \textit{geometric absorption law}
% NEXT: \begin{equation}\label{eq:geom-law}
% NEXT:   \boxed{%
% NEXT:     \APD(\rr) \approx \IPD \cdot T_0 \cdot \pospart{\nhat(\rr) \cdot (-\khat)} \cdot
% NEXT:     \Vis(\rr,\khat)
% NEXT:   }\, .
% NEXT: \end{equation}
% NEXT: The tissue physics enters through the scalar $T_0$. The transmission
% NEXT: coefficient $T_{\mathrm{tr}}$ fitted
% NEXT: in~\cite{Kodera2024,Diao2024,Funahashi2018} is identified with $T_0$,
% NEXT: the normal-incidence transmission. All spatial
% NEXT: variation depends on the body shape through the surface-normal field
% NEXT: $\nhat(\rr)$ and the visibility field $\Vis(\rr,\khat)$. For a convex
% NEXT: body, $\Vis \equiv 1$ and~\eqref{eq:geom-law} reduces to the classical
% NEXT: convex form $\IPD\,T_0 \pospart{\nhat\cdot(-\khat)}$.
% NEXT: \Cref{fig:phantom} shows how visibility enters the geometric law on
% NEXT: the Thelonious phantom. Panel~(a) shows the frontal APD map.
% NEXT: Panels~(b) and~(c) show the direction-isotropic exposure fraction
% NEXT: $\eta(\rr)$ from the front and side. Under frontal illumination,
% NEXT: $\Vis$ drops three regions to zero: the medial thighs, the sides of
% NEXT: the torso beneath the arms, and the underside of the jaw.
\subsection{Geometric absorption law}\label{subsec:pB-geom}

## reviews (subsection_header)

_(empty — run /review to populate)_
