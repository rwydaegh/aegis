% NEXT: % claim: ten_route_production_extension
% NEXT: The route-based extension applies the production contract to five additional
% NEXT: prepared scenes and combines them with the original five routes in one
% NEXT: authenticated report. The ten selected routes contain 163 observation points.
% NEXT: Every route uses image-derived surface properties, a Duke phantom facing along
% NEXT: the walk, 64 independent seeds from 7 through 70, 200,000 primary rays per
% NEXT: point and seed, and 4,096 first-diffuse output cells. Direct, exact order-1
% NEXT: specular, and first-diffuse transport use the same first-material model as the
% NEXT: five detailed routes. The extension changes the site and route coverage. It
% NEXT: does not turn the selected routes into a population sample or a city ranking.
\FloatBarrier
\section{Ten-route production-contract extension}
\label{sec:si-ten-route-extension}
