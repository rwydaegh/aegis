interface:
  display_name: "Writing Plans"
  short_description: "Use when you have a spec or requirements for a multi-step task, before touching code"
  default_prompt: "$writing-plans "
policy:
  allow_implicit_invocation: false
