% NEXT: The control replaces the image-derived material map with geometry-based
% NEXT: materials in Madrid and Mexico City. Each pair keeps the city mesh, route,
% NEXT: source curve, source weights, body, frequency, transport steps, ray count,
% NEXT: output cells, and 16 seeds fixed. A strict compatibility check permits changes
% NEXT: only in the material mode, the material arrays and hashes, the run
% NEXT: configuration, and the presence of the material-map files. Both paired manifests pass
% NEXT: all 42 file hashes. The comparison therefore measures sensitivity to the
% NEXT: image-derived material map under the current model. It does not isolate reflectance because
% NEXT: the map also changes roughness, specular share, and the nonblocking state of
% NEXT: woody vegetation. It does not measure semantic or material accuracy.
\section{Paired material-map control}
\label{sec:si-material-control}

## Reviews

_(empty)_
