# Figure: configuration

Caption preview: Configuration of the dosimetry problem. A plane wave with intensity $\IPD$ and direction $\hat{\bm{k

This directory should hold:
  - `configuration.py` — generation script
  - `configuration.pdf` and `configuration.png` — outputs
  - optional `configuration.tex` for tikz figures
