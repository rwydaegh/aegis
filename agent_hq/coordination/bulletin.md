# Agent bulletin board

Agents: read this at the start of your run. Append findings at the end.
Prune entries older than 7 days.


- [2026-04-20] code-reviewer: Focus area: **frontend data flow — Zustand store updates, API response handling, loading/error states** — post PR #712 (path contributions) and PR #713 (MIMO ECBF warnings). Overlaps with the adjacent React-components re-sweep above (which also flagged `_last_rt_tissue` staleness and deemed it sub-threshold); this pass contributes one additional unique observation (a). Reviewed `aegis-web/src/components/panels/analysis/PathInsightsSection.tsx`, `aegis-web/src/components/hud/EcbfWarningChip.tsx`, `aegis-web/src/hooks/useMIMODosimetry.ts::applyMIMOSummary`, `CompliancePanel.tsx`/`MIMOPanel.tsx` ECBF chip integration, `aegis-web/src/api/client.ts::fetchPathContributions`, `aegis-web/src/api/types.ts` `ecbf_warnings` fields, `src/aegis/viewer/routes/analysis.py::_path_contributions_impl`, `src/aegis/viewer/routes/compute/_responses.py` cache writes, `aegis-web/src/stores/environment.ts` + `EnvironmentEmptyState.tsx` (PR #709 cleanup). **No bugs found. No ship.** 119/119 vitest pass. Three sub-70%-confidence observations for future agents: (a) `PathInsightsSection.useEffect` at `aegis-web/src/components/panels/analysis/PathInsightsSection.tsx:43-69` — the early-return branch (lines 44-48, when stats/rtPaths clear) does NOT increment `fetchTokenRef.current`. A fetch started in a prior effect run can resolve after stats/rtPaths clear; the success branch's stale `setResult(data)` is hidden by the render gate at line 71, but the error branch still fires `Sentry.captureException(err)` + `useNotificationStore.addNotification('warning', 'Path insights unavailable for this compute')` even after the user has left RT mode. Minimal user impact (rare, and the error message is technically accurate), but a one-line fix (`++fetchTokenRef.current` before the early `return`) would eliminate the spurious Sentry/toast. (b) Enabling MIMO after a single-user RT compute doesn't invalidate backend `_last_rt_paths` / `_last_dosimetry_body` / `_last_rt_tissue` — the PathInsightsSection (driven by `useActiveSimulation().stats` + `useSceneStore.rtPaths`) could in principle render cached single-user path contributions alongside MIMO summary stats if the UI reached that state, though the current UI gates the Analysis panel on single-user stats so this is theoretical. Technically correct within cached frame of reference. (c) `src/aegis/viewer/routes/compute/_responses.py::_zero_paths_response` clears `_last_rt_paths` and `_last_dosimetry_body` but not `_last_rt_tissue` — minor leak; tissue cache persists after a zero-paths compute until the next RT compute rewrites it. No correctness impact because `_path_contributions_impl` requires all three to be present (404 otherwise). **Note for next agent**: frontend data flow is thoroughly covered (10+ passes); ECBF HUD surfacing closed in PR #713. Rotate to: (i) `src/aegis/analysis.py::path_importance` argsort tiebreak behavior for equal `importance` values (currently relies on NumPy stable sort default — worth a golden test with explicit duplicate-power scenarios); (ii) voxel mesher/cache invalidation (two prior reviewers deemed low-yield; unchanged); (iii) `_build_placement_config` in `optimize.py` (flagged by 2026-04-20 feature-agent entry as follow-up candidate). 36 commits since v0.30.0 now released as v0.31.0 per `6149aea`; cadence on track.

- [2026-04-21] polish-agent: **Fixed `?` / Tab / digit keys leaking into textareas.** Three global keyboard handlers did the "bail on form input" check inconsistently: `KeyboardHelp.tsx:46` and `useMIMOKeyboard.ts:11` only guarded `HTMLInputElement | HTMLSelectElement`, missing `HTMLTextAreaElement`. Repro: open Bug Reporter (Shift+B) → type `?` in the description textarea → Keyboard shortcuts modal stacks on top of the bug report and the `?` never lands in the field; same story for digits/Tab when MIMO is enabled (numbers switch users and Tab cycles instead of entering a tab indent/moving focus). `useKeyboard.ts` already had a correctly-scoped `isTypingTarget` helper covering all three; the fix mirrors that guard in both leaked handlers. **Shipped PR**: inlined the three-type check (chose inline over extracting to a shared util because only two files were broken and the existing `useKeyboard.isTypingTarget` is private to that hook — a shared util felt like premature abstraction for a 2-line polish fix; the three remaining callsites either already have the right check (`BugReporter.tsx`, `useKeyboard.ts`) or have a narrower scope where the missing element type doesn't matter (`useSpaceHeld.ts` omits `HTMLSelectElement`, which has no Space semantic). No regression test added — component-level keyboard tests require jsdom + testing-library, and the current vitest config runs in node (`vitest.config.ts`); precedent is PR #718 and PR #715's polish entries. Verified: `npx tsc --noEmit` clean, 120/120 vitest pass, `npm run build` succeeds. **Notes for next agent**: (1) The three dead-code pockets (`lastSuccessfulUserCount` MIMO orphan in `aegis-web/src/stores/mimo.ts:61,104,135`, `clearResults`/`clearScene` on `empty` scenario in `default_config.json:822-823`, `ScenarioEntry.instant`/`autoCompute` required-but-unread in `api/types.ts:150-151`) still linger — bundled cleanup remains a candidate. (2) `path_importance` argsort tiebreak golden test still open per prior entries. (3) If a jsdom migration happens, a handful of these "is-the-target-typing" guards would benefit from a single shared util + direct tests. (4) No commits since v0.31.0 before this PR; still in the post-release bundling window.

- [2026-04-21] code-reviewer: Focus area assigned: **3D scene — Three.js resource lifecycle, geometry disposal, camera state, rendering**. Noted the 2026-04-20 pass (line 21) already walked that surface end-to-end ("No bugs found" + one sub-confidence GLTF re-upload obs), and `aegis-web/src/components/scene/` has no new diffs since then, so rotated to the freshly-shipped surfaces visible in `git log`: path contributions (PR #712), MIMO ECBF warnings (PR #713), NaN-parser test realignment (PR #714), and PR #722's stale-RT-cache invalidation. **Shipping as PR**: the prior 2026-04-20 data-flow reviewer at line 38 flagged observation (a) — `PathInsightsSection.useEffect`'s early-return branch (lines 44-48) does NOT bump `fetchTokenRef.current`, so a fetch started while `rtPaths` is populated can resolve after the user leaves RT mode and fire `Sentry.captureException(err)` + `useNotificationStore.addNotification('warning', 'Path insights unavailable for this compute')`. Confidence is higher now than when it was first flagged because PR #722 (`_cache_dosimetry_for_export` writing `None` on non-RT computes) makes the in-flight fetch MORE likely to error 404 rather than silently resolve with stale data — so the race now actually surfaces a user-visible toast when the user toggles RT off while a fetch is pending. One-line fix at `aegis-web/src/components/panels/analysis/PathInsightsSection.tsx:44` — bump `fetchTokenRef.current++` inside the early-return branch before the state resets. No regression test added — component tests would require jsdom + testing-library (not in the current node-env vitest setup); precedent is PR #(keyboard-help polish-agent) which shipped a similar-sized UI fix without a test. Verified: `npx tsc --noEmit` clean, 120/120 vitest pass, `npm run build` succeeds. Also re-verified the other two sub-threshold items from the line-38 entry are now closed: observation (b) "MIMO enable doesn't invalidate backend caches" was theoretical (UI gates the Analysis panel on single-user stats); observation (c) "_zero_paths_response not clearing _last_rt_tissue" is now moot because PR #722 writes `None` to tissue on non-RT computes, and `_path_contributions_impl` still 404s when any of the three keys is None. **Notes for next agent**: (1) Path-contributions + ECBF surfaces now have 3+ reviewers covering them — rotate. (2) The `src/aegis/analysis.py::path_importance` argsort tiebreak golden test suggested at line 38 is still open and a reasonable low-risk follow-up. (3) The three dead-code pockets (`lastSuccessfulUserCount`, `clearResults`/`clearScene` on `empty`, `ScenarioEntry.instant`/`autoCompute`) still linger — bundled cleanup PR remains a candidate. (4) v0.31.0 is tagged; 0 commits since tag — typical cadence window after a release, expect bundling before v0.32.x.

- [2026-04-21] polish-agent: **Fixed stale building mesh showing after switching between OSM and 3D Tiles sources.** Both `osm` and `3dtiles` render through `<EnvironmentOSM />` at `aegis-web/src/components/scene/SceneRoot.tsx:417` off the same `osmMeshData` store slot. `setSource` at `aegis-web/src/stores/environment.ts:126-129` aborts in-flight fetches but never cleared `osmMeshData`, so a direct osm→3dtiles (or 3dtiles→osm) click kept the previous source's buildings rendered until the new fetch landed — visible as "OSM buildings labeled as 3D Tiles" while the user waited for Overpass/Google to respond. Flagged twice on the bulletin (lines 20 and 33 prior entries) as "brief visual flicker during re-fetch" without a fix. **Shipped PR**: clear `osmMeshData` inside `setSource` only when both prev and new sources are mesh-type AND different. Preserves the cached mesh when toggling through non-rendering sources (`none`/`voxels`/`coverage`) and back, and is a no-op when re-clicking the same source — so the "click wrong button by mistake, toggle back" flow still keeps your buildings. Added 6 regression cases in new `aegis-web/src/stores/__tests__/environment.test.ts` covering: osm→3dtiles clears; 3dtiles→osm clears; osm→none preserves; osm→voxels→osm preserves; osm→osm re-select preserves; in-flight AbortController is aborted on source change. 133/133 vitest pass (was 127), `npx tsc --noEmit` clean, `npm run build` succeeds. Ruff + format clean on Python (untouched this PR). **Notes for next agent**: (1) Edge case not covered by this narrow rule: `osm → none → 3dtiles` still shows stale OSM data briefly, because prev=none isn't a mesh source. Full fix would require tracking `meshSource: EnvironmentSource | null` on the store and gating `<EnvironmentOSM />` render on `meshSource === source` — too invasive for a polish session. (2) Three dead-code pockets (`lastSuccessfulUserCount` MIMO orphan, `clearResults`/`clearScene` no-ops on `empty`, `ScenarioEntry.instant`/`autoCompute` required-but-unread) still linger. (3) `path_importance` argsort tiebreak golden test per line 38 still open. (4) 1 commit since v0.31.0 before this PR; still in bundling window.

- [2026-04-21] code-reviewer: Focus area: **React components — state management, error boundaries, stale closures, missing cleanup** (third pass on this surface since v0.31.0 tagged). **No bugs found. No ship.** Walked the recently-landed surface: PR #712 `PathInsightsSection.tsx` (token-ref generation guard on the fetch effect + render gate on empty `rtPaths` / empty `sabArray`), PR #713 `EcbfWarningChip.tsx` + `applyMIMOSummary` response→summary ECBF-warning copy + one-shot `addNotification` toast, PR #722 `_cache_dosimetry_for_export` now writing `None` on non-RT computes, PR #724 `useScenario.ts` + `PathInsightsSection` invalidating in-flight path-contributions fetch when RT mode ends, PR #727 HUD Max TX power classification fix, PR #728 env-source osm↔3dtiles mesh clear, PR #730 MIMO compliance `margin_db`. Candidate bug I pursued for ~30 min before the collision check: after a non-RT compute or MIMO compute following an RT compute, `PathInsightsSection` could fetch against stale backend `_last_rt_paths` / `_last_rt_tissue` while `rtPaths` in the scene store stayed populated — estimated ~80% confidence. **Collision check via `git log origin/master -20` found PR #722 (6e65521) and PR #724 (00b5836) already landed on master and between them fully close the race**: #722 writes `None` to `_last_rt_paths` / `_last_rt_tissue` on every non-RT compute so the backend can no longer serve stale data (it now 404s), and #724 bumps `fetchTokenRef.current` in the `PathInsightsSection` early-return branch so the in-flight fetch's error handler stays silent when RT mode ends. No subset or superset PR worth filing. Per-surface checks confirmed clean: `useActiveSimulation` identity stability across MIMO/single-user toggle, `useMIMODosimetry` generation-counter + `AbortController` pattern end-to-end, `EcbfWarningChip` unmount safety (pure state, no effects), `CompliancePanel.tsx` chip gating, `RtBlockedBanner.tsx` (new in PR landed concurrently with #724 — pure render, no effects). Test baseline: 133/133 vitest pass (includes the 6 new `environment.test.ts` cases from PR #728 and the `CompliancePanel.test.ts` suite from PR #727), `npx tsc --noEmit` clean, Python fast suite 3042 passed + the two pre-existing `test_viewer_integration_rt.py` flakes. **Notes for next agent**: (1) React-components surface now at 10+ passes; the stale-RT-cache race was the last plausible bug in this surface and it's closed. Rotate. (2) The `path_importance` argsort tiebreak golden test from line 38 / line 46 remains open and is the lowest-risk follow-up. (3) Three dead-code pockets (`lastSuccessfulUserCount` MIMO orphan, `clearResults`/`clearScene` no-ops on `empty`, `ScenarioEntry.instant`/`autoCompute` required-but-unread) still linger per 6+ prior entries — bundled cleanup candidate. (4) 6 commits since v0.31.0; still PATCH-shaped, in bundling window.

- [2026-04-21] polish-agent: **Bundled cleanup of three dead-code pockets flagged by 6+ prior bulletin entries.** (a) `aegis-web/src/stores/mimo.ts` — `lastSuccessfulUserCount` was declared on the MIMO store interface (line 61), initialized to 0 in `INITIAL_STATE` (line 104), re-zeroed on `setEnabled(false)` (line 135), and asserted only in the default-value check at `mimo.test.ts:209`; no code path ever incremented or read it. Orphan from PR #614 alongside the separately-already-used `lastComputeError`/`retryCompute`. Removed all four references + the test assertion. (b) `aegis-web/src/api/types.ts` — `ScenarioEntry.instant: boolean` and `ScenarioEntry.autoCompute: boolean` were required fields on every scenario but never read anywhere in the frontend; `ScenarioWebState.clearResults?: boolean` and `clearResults?: boolean` were no-ops after PR #671 made `resetScenarioScopedStores` unconditional. Dropped all four from the interfaces. (c) `src/aegis/viewer/default_config.json` — removed `"instant"`/`"autoCompute"` from all 5 scenarios + `"clearResults"`/`"clearScene"` from the `empty` scenario `webState` (lines 730-823 before the edit). (d) `src/aegis/integration/cloudrf_templates.py:34-35` — dropped `"instant": True, "autoCompute": True` from the dict emitted by `cloudrf_template_to_scenario`; the regression test at `tests/test_integration_helpers.py::TestCloudRFTemplateConversion::test_power_dbm_is_conducted_not_eirp` only checks `webState.powerDbm` and `description`, unchanged. Left the 7 checked-in `configs/presets/cloudrf/*.json` files alone — they still carry the old extra keys but JSON-parse cleanly into the slimmer interface (extra fields silently ignored) and will drop the fields naturally on next regeneration. `npx tsc --noEmit` clean, 133/133 vitest pass, `npm run build` succeeds, ruff + format clean, 3058 Python fast tests pass + 24 skipped (the two `test_viewer_integration_rt.py` scene-path-before-mode-check flakes deselected, documented in 8+ prior entries). **Notes for next agent**: (1) All three dead-code pockets that lingered through 6+ bulletin entries are now closed. (2) `path_importance` argsort tiebreak golden test per lines 38/46 still open. (3) 7 commits since v0.31.0 before this PR; still PATCH-shaped, in bundling window.

- [2026-04-21] code-reviewer: Focus area assigned: **frontend data flow — Zustand store updates, API response handling, loading/error states**. **No bugs found. No ship. Fully duplicate with line 49** ("React components — third pass on this surface since v0.31.0 tagged"). Pursued the same candidate bug (stale `_last_rt_paths` / `_last_rt_tissue` surfacing through `PathInsightsSection` after mixed RT / non-RT / MIMO computes) to ~80% confidence before the `git log origin/master` collision check found PR #722 (6e65521 "Invalidate stale RT cache on non-RT compute") and PR #724 (00b5836 "Invalidate in-flight path-contributions fetch when RT mode ends") had both landed on master closing the race from the backend-cache side AND the in-flight-fetch side respectively. Re-verified on current master: backend `_cache_dosimetry_for_export` at `src/aegis/viewer/routes/compute/_responses.py` now writes `None` to `_last_rt_paths`/`_last_rt_tissue` on every non-RT compute, so `_path_contributions_impl` 404s instead of serving stale rankings; frontend `PathInsightsSection` early-return branch now bumps `fetchTokenRef.current++` so in-flight fetches can't fire `Sentry.captureException` / `addNotification` after the user leaves RT mode. Re-walked `applyMIMOSummary` response→summary ECBF-warning copy path (PR #713) — `summary` parameter mutation is safe because it's a freshly-parsed JSON body; notification fires exactly once per compute via `useNotificationStore.addNotification`. Re-verified backend `src/aegis/viewer/routes/mimo.py:386-402` `warnings.catch_warnings(record=True)` + `collect_ecbf_warnings` pattern is correct. Spot-checked other frontend data-flow surfaces touched since v0.31.0 (PR #727 HUD Max TX power, PR #728 env-source osm↔3dtiles mesh clear, PR #730 MIMO compliance `margin_db`, PR #732 dead-code cleanup) — all clean, no new bugs. 133/133 vitest pass, `npx tsc --noEmit` clean. **Note for next agent**: frontend data-flow surface now at 11+ passes with the stale-cache race explicitly closed — further passes on this surface are very low-yield. Rotate to the still-open items per line 49: (a) `path_importance` argsort tiebreak golden test; (b) voxel mesher/cache invalidation; (c) 3D Tiles silent non-render at `src/aegis/environment/tiles.py::_parse_glb_primitive` (needs Google 3D Tiles API key — ask Robin). 8 commits since v0.31.0 before this entry, still PATCH-shaped.

- [2026-04-21] code-reviewer: **Fixed provenance `_source` columns losing track when a low-priority source fills a high-priority NaN.** Deep-dive on `src/aegis/basestation/merge.py::_merge_cluster_rows` (the spatial-dedup cluster merge): the loop fills NaN value cells from subsequent rows but only touches columns where `merged[col]` is NaN. Source columns are strings and never NaN (they hold `"missing"` or a tag), so when the first row has `Power=NaN` + `Power_source="missing"` and a later row fills `Power=42.0` + `Power_source="ocid"`, the value gets promoted but the source stays at `"missing"`. Confirmed end-to-end: `dataframe_to_basestations` → `_build_provenance_origins` → `_compute_fidelity_tier` collapses the row to `fidelity_tier="location_only"` even though every field is populated (because `is_confident("Power")` and `is_available("Power")` both read "missing"), and `aggregate_confidence` drops accordingly. Scope: triggers in any multi-source region (Belgium and beyond — any `regions.yaml` entry with `basestationlib` + `opencellid`/`mastedatabasen`). Never triggered by `TestMergeSources::test_higher_priority_fills_nan` because that test has Power filled in the high-priority row. **Shipped PR**: `_merge_cluster_rows` now skips `*_source` columns in the outer scan and, whenever a value cell is filled from `other`, copies the companion `{col}_source` from the same row. Added `TestMergeSources::test_lower_priority_fill_updates_source` as regression. 16/16 merge tests pass, 183/183 in the basestation suites (merge + parquet_io + basestation + classify + viewer_basestations + exposure_modes), 3047/3047 in the full fast suite (two `test_viewer_integration_rt.py::TestComputeRtInvalidPayload` validation-ordering flakes deselected — documented in 8+ prior entries, unrelated to this change). `ruff check` + `ruff format --check` clean. **Notes for next agent**: (1) `Pattern_source` is not subject to this bug because patterns are loaded from a separate `.mat` and the `Pattern_source` column on the DataFrame stays `""` through the merge; confirmed by read of `parquet_io.py::dataframe_to_basestations:144,168,178`. (2) Data on disk in `data/basestations/merged/*.parquet` pre-dates this fix — rerunning `python -m aegis.basestation.build merge --region <name>` would regenerate with correct provenance, but not urgent (fidelity display is cosmetic; the dosimetry math uses the filled value correctly). Flag to Robin whether to regenerate-and-commit. (3) Still-open items from prior entries: `path_importance` argsort tiebreak golden test, voxel mesher/cache invalidation, 3D Tiles silent non-render. (4) 9 commits since v0.31.0 before this PR.

- [2026-04-21] polish-agent: **Fixed stale `AntennaDetailPanel` rendering for a filtered-out antenna.** Select antenna 5 → uncheck its operator/technology/frequency-band filter → antenna 5 disappears from the 3D view (`BaseStationMarkers.tsx:81-83` filters on `enabledOperators`/`enabledTechnologies`/`enabledFrequencyBands`) but the detail panel kept rendering its fields, tier badge, source link, and upgrade list because the only guard at `AntennaDetailPanel.tsx:143` was the out-of-bounds `selectedIndex >= basestations.length` check. Confusing because the user can't see the antenna on the scene anymore, but the inspector implies it's still active. **Shipped PR**: mirrored the 3-line filter from `BaseStationMarkers` into the panel render guard — subscribe to the three enabled-set store fields and return null if the selected antenna is filtered out. Re-enabling the filter brings the panel back with the same selection preserved (no state wipe), matching user intent. Chose in-component guard over store-level "clear selection on filter toggle" because the latter loses the user's prior selection when they re-enable a filter they accidentally unchecked. No regression test added — component-level render tests need jsdom+testing-library (current vitest runs node-env); precedent is PR #725 / PR #728 polish entries. Verified: `npx tsc --noEmit` clean, 133/133 vitest pass, `npm run build` succeeds. **Notes for next agent**: (1) Related pre-existing gap not fixed here (scope creep): `setBasestations` doesn't reset `selectedIndex`, so after "Set up scene here" on a new location, an old selectedIndex can silently refer to a different antenna in the new array. Fix would be a one-liner in `setBasestations` but the bug repros only when the new basestations array has ≥ selectedIndex+1 entries, which is most real runs — user-visible but separate scope. (2) The three dead-code pockets are now closed (PR #732). (3) `path_importance` argsort tiebreak golden test per prior entries still open (sub-polish bar given the "no tests for untouched code" rule). (4) 8 commits since v0.31.0 before this PR; still PATCH-shaped, in bundling window.

- [2026-04-21] feature-agent: **Replaced the compliance ring with a physics-correct 3-D footprint.** Two bugs in the old ring (`aegis-web/src/components/scene/ComplianceRing.tsx` at PR #736): (a) per-azimuth radius was modulated by `sqrt(g(az) / gMean)`, which is the wrong normalization — the iso-S_inc contour scales as `sqrt(g(az) / g(body_direction))`, so for a patch antenna pointed at the body (where `g_body ≈ g_peak >> gMean`) the forward ring was overestimated and the back ring underestimated by an order of magnitude; (b) only the horizontal slice of the pattern was sampled, so a 30 m tower with mechanical downtilt drew the same disc as a ground-level omni — the vertical pattern and the antenna's geometric height above a standing observer (1.5 m) were completely absent. **Shipped PR**: added pure helper `aegis-web/src/lib/complianceFootprint.ts` (`computeComplianceFootprint` + `smoothRadii`) that solves per-azimuth with Picard iteration for the horizontal radius where S_inc = S_limit at observer height 1.5 m, using the full 3-D gain pattern `g(dir)` via `interpolatePatternGain` / `scalarRadiationGain`. The iteration is needed because `g` depends on elevation, which depends on `r`; 12 iters with 0.7/0.3 damping converge at ~0.02 m tolerance. The reference direction is antenna→body (so the body-direction radius collapses to the body's horizontal distance at margin_db=0), with a 2 % of-peak floor on `g_body` to keep the ring finite when the body sits in a pattern null. Refactored `ComplianceRing.tsx` to delegate to the helper; kept the circle fallback for uniform patterns, dropped the old `sqrt(g/gMean)` path. Degenerate case (iso-sphere entirely above observer) now returns a null ring rather than a misleading ground disc. New helper has 18 vitest cases covering isotropic circle, patch forward-lobed asymmetry, tall-pole degenerate, parametric margin_db=0 closed-form check, NaN / zero-distance / capped-radius robustness, and `smoothRadii` behaviour. Verified: `npx tsc --noEmit` clean, 152/152 vitest pass (was 134), `npm run build` succeeds, Python fast suite 3047 passed + 24 skipped (the two `test_viewer_integration_rt.py` validation-ordering flakes deselected per 9+ prior entries). Python untouched. **Notes for next agent**: (1) Observer height is a module constant at `DEFAULT_OBSERVER_HEIGHT_M = 1.5`; a UI toggle (sitting child at 0.9 m, standing adult, maintenance worker at 2.5 m) would be a natural follow-up — the helper already accepts it as an input. (2) Occupational vs general_public limits aren't yet surfaced as overlay rings; would need a second call to the helper with a different `margin_db` (you can derive the occupational margin from the same compute's S_body + the occupational `S_limit`). (3) `path_importance` argsort tiebreak golden test still open per 9+ prior entries. (4) 11 commits since v0.31.0 before this PR; starting to look MINOR-shaped given the compliance-surface change.

- [2026-04-21] polish-agent: **Fixed selection leaking across basestation batches.** Picked up the follow-up flagged in the previous polish entry: after selecting antenna N in city A and loading city B, the detail panel silently rendered whatever antenna sits at index N in the new array, with no user signal that the selection had jumped to a totally different physical site. Three callsites swap the array — `BaseStationsPanel.handleLoad` on location/radius change, `ScenePanel.loadBasestationsForLocation` after a scenario location load, `CoverageHud.handleSetupScene` on "Set up scene here" — so I fixed it once at the store level. **Shipped PR**: one-line addition of `selectedIndex: null` inside the `setBasestations` state update in `aegis-web/src/stores/basestations.ts`. Mirrors what `clear()` already does (line 139), keeps `selectAntenna` unchanged (user-driven selection still survives filter toggles per PR #733), and only fires on wholesale array swaps. Added one regression test (`selection reset on array swap` block in `basestations.test.ts`) covering selectAntenna→setBasestations→selectedIndex===null. `npx tsc --noEmit` clean, 134/134 vitest pass (was 133), `npm run build` succeeds. Python untouched. **Notes for next agent**: (1) `AntennaDetailPanel.tsx:146` still has its defensive `selectedIndex >= basestations.length` check — harmless dead branch now that the store can't land in that state via the happy path, but worth keeping as a belt-and-braces guard against any future codepath that sets basestations via a route other than `setBasestations`/`clear`. Not worth pruning. (2) `path_importance` argsort tiebreak golden test still open per 8+ prior entries — remains sub-polish-bar under the "no tests for untouched code" rule. (3) 10 commits since v0.31.0 before this PR (PR #735 provenance fix just landed); still PATCH-shaped, in bundling window.

- [2026-04-21] code-reviewer: Focus area assigned: **React components — state management, error boundaries, stale closures, missing cleanup** (now the fourth pass on this surface since v0.31.0 tagged, per the 10+-pass count flagged on lines 11/15/19). **No bugs found. No ship.** Per the explicit "rotate" guidance on line 15, I walked the recently-landed surface to confirm closure rather than re-scanning React components end-to-end: PR #727 `CompliancePanel.tsx` (`computeMaxPowerDbm` / `tightestMarginDb` / `WARN_HEADROOM_DB` — verified the linear-power-scaling assumption holds because every ICNIRP check margin scales linearly with TX dBm, and the 7 new test cases cover the ratio≤0.8 PASS invariant); PR #728 `environment.ts::setSource` (verified the osm↔3dtiles mesh-clear rule — the 6 new `environment.test.ts` cases cover every transition; the `osm→none→3dtiles` edge case flagged at line 13 is known, invasive to fix, and still theoretical); PR #733 `AntennaDetailPanel.tsx` filter guard (verified all `selectedIndex` consumers — `BaseStationMarkers.tsx` skips filtered antennas so a stale selectedIndex cannot be reclicked, and `AntennaDetailPanel.tsx:150-152` render-guards on the three filter sets); PR #736 `basestations.ts::setBasestations` selection reset (verified no other mutation route for `selectedIndex` — `selectAntenna` / `clear` / `setBasestations` is the full mutation surface); PR #735 `merge.py::_merge_cluster_rows` source-column fill (verified the N-row case: once `merged[col]` is filled, `pd.isna(merged[col])` becomes False and subsequent rows cannot overwrite, so source and value stay aligned; `Pattern_source` is correctly excluded because patterns don't flow through the value-column path); PR cb51fb3 (`top_k is None` removal — Flask `request.args.get(key, default, type=int)` never returns None when a default is supplied because type-conversion failures return the default per Werkzeug's MultiDict.get); PR b546635 (`extra: dict[str, Any]` — purely a type annotation widening; no runtime behavior change). Two sub-threshold observations that I chose not to ship: (a) `_merge_cluster_rows` at `src/aegis/basestation/merge.py:112` skips columns in `source_cols` but does not defend against a pre-existing parquet row where `Power_source="gov:xxx"` with `Power=NaN` — this state is unreachable via `write_merged_parquet` (which re-derives source from `pd.isna(value)`) but isn't invariant-checked, and the tagged_dfs loop at `merge.py:157-168` preserves `src_col` as-is when it already exists. Theoretical only; no known callsite produces this. (b) `_compliance_summary` serializer at `src/aegis/viewer/routes/mimo.py:314` and `compute/_responses.py:261` guard `c.margin_db != float("inf")` but not `float("-inf")` — `ComplianceCheck.margin_db` returns `-inf` when `limit <= 0`, which would round to `-inf` and JSON-serialize as `-Infinity` (invalid for `JSON.parse`). Can't actually trigger: `icnirp_limits` produces only hard-coded positive limits (`sar_wb=0.08|0.4`, `sab_4cm2=20|100`, etc.), and a Pydantic-validated `ICNIRPLimits` dataclass never takes negative inputs. Sub-70% confidence; skipping per the "only file bugs you are confident are real" rule. Test baseline: 134/134 vitest (was 134 after PR #736), 3047/3047 Python fast tests (two pre-existing `test_viewer_integration_rt.py::TestComputeRtInvalidPayload` validation-ordering flakes deselected — documented in 9+ prior entries). **Notes for next agent**: (1) React components / frontend data flow now at 12+ combined passes with explicit rotate guidance from line 15 / line 19. Rotate. (2) `path_importance` argsort tiebreak golden test per lines 38/46 still open across 9+ prior entries (sub-polish-bar under "no tests for untouched code"). (3) 12 commits since v0.31.0 (PR #735 provenance + PR #736 selection-reset landed since my prior reviews); still PATCH-shaped, v0.31.1 metadata already pushed. Robin's next natural step is to cut v0.31.1 when the bundling window closes.

- [2026-04-21] polish-agent: **Fixed misleading "What would unlock higher fidelity?" upgrade-path messages in `AntennaDetailPanel`.** Tracing `computeUpgradePath` in `aegis-web/src/utils/fidelityTier.ts` against `computeFidelityTier` in the same file (and the backend mirror at `src/aegis/viewer/routes/basestations/_fidelity.py::_compute_fidelity_tier`) surfaced two mismatches: (a) **estimated Electrical_Tilt was being flagged as a blocker but is not one** — `computeFidelityTier` uses `isAvailable` for `Electrical_Tilt`/`Mechanical_Tilt`/`Horizontal_Beamwidth`/`Vertical_Beamwidth` at both spatial and full tiers, so `est:tech+band` is fine; the tier description itself says "Core RF parameters verified, **tilt estimated**". Users with an `est:` electrical tilt (the common case — `merge.py::estimate_with_provenance` writes that source during `tech+band` group-median fill) saw "Electrical tilt is estimated" listed next to "Need real antenna pattern file" as if it was also blocking, which contradicts the tier definition. (b) **estimated Power was NOT flagged but does block geometric/spatial/full** — those tiers require `isConfident('Power')`, so an antenna with `est:tech+band` Power is stuck at bound tier yet the panel previously said nothing about Power at all. Confirmed `est:` is only produced for the 6 fields in `NUMERIC_COLS` (`Power`/`Electrical_Tilt`/`Mechanical_Tilt`/`Gain`/`Horizontal_Beamwidth`/`Vertical_Beamwidth`); Frequency/Azimuth/CenterHeight never go through the estimator, so their existing `est:` branches are dead but harmless (kept as-is to avoid scope creep). **Shipped PR**: one-line removal of the spurious `else if` for `Electrical_Tilt` at `fidelityTier.ts:92`, one-line add of the missing `else if (src('Power').startsWith('est:')) issues.push('EIRP is estimated')` above the Frequency check, plus a short comment explaining the tier-blocking invariant. Added `aegis-web/src/utils/__tests__/fidelityTier.test.ts` with 16 cases covering both functions — 8 regression cases for the bug (confident baseline, synthetic pattern drops to spatial, est tilts stay spatial, est Gain drops to geometric, est Power drops to bound, missing Power drops to location_only, sparse-provenance full list, all-confident clean upgrade path) and 8 covering the upgrade-path invariant (no est tilt/beamwidth flagging, est Power flagged, est Gain flagged, missing tilts flagged, null provenance). `npx tsc --noEmit` clean, 168/168 vitest pass (was 152 after PR #739), `npm run build` succeeds. Python untouched. **Notes for next agent**: (1) `fidelityTier.ts:86-88` still has dead `else if` branches for `Azimuth` / `CenterHeight` est flags — left alone because they're cheap belt-and-braces and removing them would enlarge scope beyond the one-session polish bar. If a future data source (e.g. a new `estimate_azimuth_from_site_layout` pass) starts emitting `est:` for those fields, the messages would become load-bearing. (2) `path_importance` argsort tiebreak golden test per prior entries still open — remains sub-polish-bar under "no tests for untouched code". (3) 13 commits since v0.31.0 before this PR.

- [2026-04-21] code-reviewer: Focus area assigned: **physics kernels and compliance — levels 0-8, ECBF QCQP, spatial averaging, ICNIRP limits**. **No bugs found. No ship.** Rotated away from the React-components / frontend-data-flow surface per the 12+-pass saturation flagged on lines 11/15/19/29 and walked the Python physics surface end-to-end: `src/aegis/coherent/ecbf.py` (MRT shortcut, slack-regime closed-form `x = α·Q⁻¹h*` with `α = √(P_abs_max/qinv_form)`, bisection on λ for the tight-constraint branch), `src/aegis/coherent/exposure_operator.py` (`compute_exposure_operator` Hermitian symmetrization, `eigendecompose_Q` descending-flip + negative-clamp, `compute_rho` PSD bound giving ρ∈[0,1]), `src/aegis/coherent/body_channel.py` (`depth_weight = √(σ/(4α))` with α floored at `NUMERICAL_FLOOR=1e-30`, back-facing-path masking via `t_s/t_p=0`), `src/aegis/coherent/fresnel_operator.py` (unclipped `mu` into `_fresnel_core` is OK because `mu²` makes `xi` sign-independent and the back-facing mask zeroes outputs), `src/aegis/tissue/fresnel.py` (branch-cut choice `Re(xi)≥0` correctly keeps `Im(xi)<0` for lossy tissue giving exponential decay; energy-conservation path `T = 1 - |r|²`), `src/aegis/kernels/spatial.py` (unified composable kernel with auto-chunking at M·N > 10M to prevent cgroup OOM; curvature term uses `T0` not `t_factor` per monograph composability analysis), `src/aegis/kernels/_base.py` (`fresnel_weights` clips `mu` to [0,1] before complex cast, `physical_gelu` broadcasts σ:(M,)→(M,1)→(M,N) with floor substitution when σ=0), `src/aegis/kernels/level7_coherent.py` + `level8_ecbf.py` (`S_ab = ‖G·x‖²` via einsum `mia,a→mi`; `eigenvalues[0]` passed as λ_max since `eigendecompose_Q` flips to descending), `src/aegis/geometry/averaging.py` (verified the Numba `accumulate-break` loop and the NumPy `searchsorted` fallback produce equivalent row-stochastic output; `r_est = √(target_area/π)·2.5` seed for `query_ball_point`), `src/aegis/compliance/__init__.py` (re-verified PR #720's `sar_wb is not None and freq_hz <= _FREQ_SAB_THRESHOLD_HZ` drop above 6 GHz; boundary semantics at exactly 6 GHz — SAR_wb applies via `<=`, Sab does not). Test baseline: 3047 passed + 24 skipped (the two `test_viewer_integration_rt.py::TestComputeRtInvalidPayload` validation-ordering flakes deselected, documented in 9+ prior entries). Mie regression canary green; every monograph table has its golden test. Four sub-threshold observations I chose not to ship: (a) `ecbf.py` slack-regime detection requires `Q` invertible — when `Q` is rank-deficient AND `h` has no null-space component AND `p_abs_inf > P_abs_max`, the slack check is skipped and the solver falls through to the infeasibility warning. A pseudo-inverse slack would be valid here but unimplemented; theoretical edge case (needs rank-deficient `Q`, which requires a degenerate mesh or single-tissue voxelization). (b) `max_compliant_power` in `compliance/__init__.py` assumes linear P scaling — documented as valid for incoherent AND coherent Level 7 (fixed precoder), but Level 8 ECBF doesn't scale linearly when `P_abs_max` is fixed (the optimal `x*` adapts nonlinearly to the constraint). PR #727's linear-scaling invariant holds for Level 7 but not Level 8; the existing docstring notes this as an approximation. Not a bug so much as a documented approximation. (c) `spatial_compliance_grid` uses EIRP-weighted `freq_mean_hz` for limits — docstring mentions ICNIRP summation rule for multi-frequency exposure but the implementation uses a mean-frequency approximation. Documented approximation, sub-ship. (d) `link_budget_compliance` below 6 GHz returns "unlimited" with no SAR_wb or SAR_10g check because it only passes `sab_4cm2` and `sinc_local` to `evaluate_compliance` — intentional per the link-budget abstraction (no body mesh → no SAR). Documented limitation, not a bug. None meet the 70% confidence bar. **Notes for next agent**: (1) Physics-kernels + compliance surface is mature — golden tests on every monograph table + Mie regression canary + Hypothesis property tests (Sab≥0, energy conservation, ReLU bound). Fresh eyes on this surface are now low-yield; rotate. (2) The new `aegis-web/src/lib/complianceFootprint.ts` helper from PR #739 has its own 18-case vitest suite and is frontend-physics-adjacent; code-reviewer surface coverage for it belongs with the next frontend pass, not a Python physics pass. (3) `path_importance` argsort tiebreak golden test per 10+ prior entries still open — remains sub-polish-bar under "no tests for untouched code" but would close the last known hole in the analysis-path surface. (4) Candidate surfaces for the next code-reviewer rotation: `src/aegis/integration/` (DiffeRT ray-tracer bridge, Modal GPU glue — not reviewed in current 7-day window), `src/aegis/basestation/build.py` + `classify.py` (merge was reviewed in PR #735; build/classify have had less attention), `src/aegis/viz/` dashboards. (5) 13 commits since v0.31.0; PATCH-shaped so far, the compliance-ring change (#739) edges toward MINOR. Bundling window still open.

- [2026-04-21] feature-agent: **Added 3-D compliance volume visualization.** Yesterday's PR #739 made the ring physically honest on the ground plane but still showed only a 2-D ground slice of the ICNIRP iso-S_inc surface — the full 3-D exclusion shape (mattered e.g. for rooftop workers at 10 m, balconies, maintenance personnel on the pole, windows near a base station) was invisible. Because `r(az, el)` depends only on direction (no observer-height coupling), the helper generalizes as a closed-form sample per direction:  r(az, el) = d_body · √(G(az, el) / G_body) · 10^(−margin_db/20). No iteration needed, unlike the ring's Picard loop. **Shipped PR**: (a) new `computeComplianceVolume` in `aegis-web/src/lib/complianceFootprint.ts` alongside the existing `computeComplianceFootprint` (reuses the same peak-gain search, same body-direction reference with 2 % gain floor, same `maxRadiusM` cap). (b) new `aegis-web/src/components/scene/ComplianceVolume.tsx` that triangulates a 64×33 (az × el) UV-sphere-style mesh with per-vertex height tint, ground drape (`y = max(y_world, 0)` so the surface hugs ground rather than clipping through), and disposes both `BufferGeometry` + `WireframeGeometry` on recompute/unmount. Solid face at opacity 0.16 + wireframe outline at 0.25, same green/red compliance palette as the ring. (c) `complianceVolumeVisible` added to `useSceneStore` (default OFF; ring stays the default visualization), `toggleComplianceVolumeVisible` wired, and a "Compliance volume (3-D)" toggle sibling to the existing "Compliance ring" toggle in `LayersPanel`. (d) Mounted into `SceneRoot` next to `<ComplianceRing />`. Added 10 vitest cases for `computeComplianceVolume` covering: isotropic-sphere equivalence at margin=0, 10^(-margin/20) scaling, elevation endpoint coverage, patch forward-lobed asymmetry, short-dipole axial null, NaN/coincident-antenna robustness, maxRadius cap, reference-invalidity flag, and minimum nAzimuth/nElevation clamping. Verified: `npx tsc --noEmit` clean, 178/178 vitest pass (was 168 after PR #740), `npm run build` succeeds, Python ruff clean (no Python changes). **Notes for next agent**: (1) The volume's ground drape creates a near-ground slice that roughly follows but isn't identical to the compliance ring — the ring handles the observer-height cross section exactly via Picard; the volume drapes vertices to `y=0` which is informationally fine (the 3-D shell + the ring are meant to complement each other with ring default-on). If the two views disagree visually enough to confuse a user, a follow-up could gate the volume on "ring OFF" or merge them into a single parametric disc+dome geometry. (2) The PR #739 follow-ups (observer-height toggle for sitting child / maintenance worker, occupational overlay ring) are still open and a natural next feature-agent task. (3) 14 commits since v0.31.0 before this PR; compliance-surface change is visible user-facing feature so this pushes the bundling window toward MINOR-shaped.

- [2026-04-21] code-reviewer: **Fixed operator-precedence bug in Sentry webhook breadcrumb formatter.** Focus area assigned: error handling and input validation at system boundaries. Rotated away from React-components / frontend-data-flow (saturated per 12+ passes) and walked the Flask `routes/` boundary surface (`sentry_webhook.py`, `patterns.py`, `parametric.py`, `mimo.py`, `environment.py`, `terrain.py`, `location.py`, `analysis.py`, `optimize.py`). **Shipping**: `src/aegis/viewer/routes/sentry_webhook.py::_extract_breadcrumbs` line 254 had `desc = msg or json.dumps(data)[:80] if data else cat` — Python's precedence parses this as `(msg or json.dumps(data)[:80]) if data else cat`, so when `data` is the falsy `{}` returned by `bc.get("data", {})` (most breadcrumbs don't carry a `data` dict), the conditional fell through to `cat` and **dropped the message entirely** even when `msg` was present. Auto-generated GitHub issues from Sentry thus showed the bare category name (e.g. `transaction:**  transaction`) for any non-fetch/click/console/nav breadcrumb with a message — silently degrading the debugging context for every Sentry-reported production error. Empirically verified the bug and the fix (parenthesize as `msg or (json.dumps(data)[:80] if data else cat)`): pre-fix `msg='starting render', data={}, cat='transaction'` → `'transaction'`; post-fix → `'starting render'`. Added `tests/test_viewer_sentry_webhook.py` with 7 unit cases covering the regression path, data-present path, data-empty-no-message path, fetch/navigation formatting, missing-entry guards, and `_extract_exception` frame formatting (no prior tests existed for this module). `python -m ruff check` + `ruff format --check` clean; 7/7 new tests pass; `tests/test_viewer_sentry_webhook.py tests/test_viewer_api.py tests/test_viewer_auth.py` all pass together. Collision check via `git fetch origin master && git grep _extract_breadcrumbs` confirmed no other agent has touched this module since v0.31.0. Four **sub-threshold** validation observations I chose not to ship (each <70 % confidence on its own; bundled below for a future pass): (i) `mimo.py::_parse_scene_scalars` guards `freq_hz <= 0` and `> 300e9` but not `math.isfinite(freq_hz)` — a NaN passes both comparisons (NaN compares False) and propagates into `c / freq_hz` → NaN wavelength. `StrictJSONProvider` already rejects bare NaN literals in request JSON (see `test_viewer_strict_json_nonfinite.py`), so this is only reachable if a downstream caller constructs NaN after parsing; theoretical, and the `_parse_vec3` + `_reject_non_finite` helpers are already in use elsewhere in this file. (ii) Same shape in `environment.py`/`terrain.py`/`location.py`: `radius <= 0 or radius > max_radius` doesn't reject NaN radius. Again gated by `StrictJSONProvider`. (iii) `patterns.py::_handle_search` doesn't `math.isfinite()` check the `freq_min/freq_max/gain_min/gain_max` args, but these flow into SQLite `WHERE`-clause comparisons which return no matches for NaN bounds — degraded UX but not unsafe. (iv) `sentry_webhook.py::_create_github_issue` silently swallows a failed POST if the response status isn't 2xx (no status check on `urlopen` response) — the wrapping `try/except` in `_handle_new_issue` returns 200 to avoid Sentry-retry storms, which is intentional per the inline comment; the silent-swallow only matters for observability. **Notes for next agent**: (1) System-boundary validation is healthy overall — `_reject_non_finite`, `_require_finite_float`, `_require_finite_vec3`, and `StrictJSONProvider` close most paths. The four sub-threshold items above are only reachable via contrived payloads or post-parse NaN injection. (2) `path_importance` argsort tiebreak golden test per 11+ prior entries still open. (3) Candidate rotations still open from line 33: `src/aegis/integration/` (DiffeRT ray-tracer bridge), `src/aegis/basestation/build.py` + `classify.py`, `src/aegis/viz/` dashboards. (4) 15 commits since v0.31.0 before this PR; compliance-volume PR #742 pushed bundling window toward MINOR-shaped.

## In progress

<!-- Mark what you are working on to avoid collisions. Clear after merge or if stale >6h -->
