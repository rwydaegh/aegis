% NEXT: For a lossy sphere of radius $a$ and complex refractive index
% NEXT: $\ntilde$, the Mie series gives an exact solution for the absorption
% NEXT: efficiency $Q_{\mathrm{abs}}$. The geometric law predicts
% NEXT: $P_{\mathrm{abs}} = \IPD\,T_0\,\pi a^2$, so its error is
% NEXT: $(T_0/Q_{\mathrm{abs}} - 1)$. We use skin properties from the
% NEXT: IT'IS database~\cite{ITISv5,Gabriel1996} at each frequency. The total error splits into two
% NEXT: contributions. The Fresnel approximation error is shape- and
% NEXT: frequency-dependent but size-independent. On a sphere it is the
% NEXT: sphere ratio $R = T_0/\langle\Tavg\rangle$, which crosses unity at
% NEXT: approximately $39$~GHz (\cref{fig:R-of-f}). The diffraction error is
% NEXT: size-dependent and scales as $x^{-2/3}$ in the optical regime, where
% NEXT: $x = \pi d/\lambda$ is the size parameter. Diffraction bends waves
% NEXT: into the geometric shadow, adding absorption that the surface law
% NEXT: misses. We refer to the regime where $x$ is small enough that this
% NEXT: diffracted contribution exceeds a few percent of total absorption as
% NEXT: the \emph{body-Mie regime}. For body-scale targets it corresponds to
% NEXT: frequencies below approximately $6$~GHz.
\subsection{Mie theory on lossy spheres}\label{subsec:val-mie}

## reviews (subsection_header)

_(empty — run /review to populate)_
