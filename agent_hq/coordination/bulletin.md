# Agent bulletin board

Agents: read this at the start of your run. Append findings at the end.
Prune entries older than 7 days.


- [2026-04-19] code-reviewer: Focus area: viewer backend routes and API edge cases. **Found and fixed 500 on `/api/mimo/compute`** with non-dict elements in `users` array. `_validate_users_cfg` (mimo.py:52) called `"id" in u` on every element — raises `TypeError` when an element is `None`/`int` (non-iterable) and silently accepts strings/lists. Downstream `_build_user_state` then crashes in `u.get("phantom", ...)` → 500. Reproduced all four shapes (`[null]`, `[42]`, `["hello"]`, `[[1,2,3]]`) returning 500 via Flask test client. Added `isinstance(u, dict)` guard after the list check — all cases now return 400 with clear message. Added regression test to `tests/test_viewer_non_dict_body_guards.py`. Shipped as PR.

- [2026-04-19] feature-agent: Focus area: half-baked feature polish — **BaseStations frequency-band filter silently inverts when all bands are unchecked**. The three sites that filter by `frequency_band` (`basestations.ts` `computeActiveCount` + `activeIndices`, and `BaseStationMarkers.tsx`) all gated on `enabledFrequencyBands.size > 0`, so once the user unchecked every band the filter turned into a no-op: banded antennas came back on the globe, `activeCount` silently re-included them, and the "Compute exposure" button re-enabled itself on a state the user had just explicitly rejected. Operator/technology filters don't have this asymmetry (unchecking all hides everything), so the band UX was the odd one out. **Fix**: dropped the `.size > 0` guard in all three sites — antennas without a `frequency_band` are still always shown (`b.frequency_band &&` preserves that), but antennas with a band are now consistently hidden when their band is absent from the enabled set. Added `aegis-web/src/stores/__tests__/basestations.test.ts` with 3 cases: (a) unchecking every band hides all banded antennas, (b) band-less antennas stay visible across the empty-filter state, (c) operator filter regression check. 94/94 vitest pass (was 91), `npx tsc --noEmit` clean, `npm run build` succeeds, `python -m ruff check` clean. **Note for next agent**: voxel mesher/cache invalidation is still on the underweight list — looked at `clear_voxel_scene_cache()` + `_cache_loaded_voxels` on the way past and every known mutation site clears both caches, but it deserves a fresh pass from a different reviewer. MIMO panel/ECBF solver surface also still open.

- [2026-04-19] code-reviewer: Focus area: **frontend data flow** (Zustand store updates, API response handling, loading/error states) — 7th+ same-day pass on this surface. **No bugs found.** Re-verified the fix landed by PR #661 at `aegis-web/src/stores/basestations.ts` (`computeActiveCount` + `activeIndices`) and `components/scene/BaseStationMarkers.tsx`: dropping the `enabledFrequencyBands.size > 0` guard makes band filtering consistent with operator/technology (unchecking all hides all banded antennas); band-less antennas stay visible via the `b.frequency_band &&` short-circuit. 3 regression cases in `stores/__tests__/basestations.test.ts`. Re-verified PR #659 `_validate_users_cfg` `isinstance(u, dict)` guard before `"id" in u`. Re-verified PR #657 subnormal `freq_hz` guard at `routes/compute/_parsing.py`. Walked the Zustand surface: `optimize.ts` `beginRun` clears `history/currentIter/summary/playbackIter` (fixes #623); `environment.ts` `freshAbort` + `reportUnexpected` skip 429/502/503/504; `parseEnvironmentBinary` validates nV/nT bounds; `mimo.ts` `removeUser` nextId fallback + geometry disposal + `_configVersion` tracking. Walked the loading/error surface: `_dosimetryResult.ts` shared `applyDosimetryResult` and `handleDosimetryError` with AbortError→isNetworkError→isClientError(4xx warning, no Sentry)→Sentry ladder (PR #633); `useDosimetry` generation counter + `lspHeatmapGenRef`; `useMIMODosimetry` null-on-abort vs summary-on-error; `useOptimization.isCurrent()` abortRef identity check. Investigated theoretical edge cases in `DataQualityHud.tsx`/`RegionDataCard.tsx` `buildProvMap` (empty `provenance_sources` + populated `provenance`): not triggerable — `routes/basestations/_fidelity.py:_bs_summary` always populates `provenance_sources` with all `_PROVENANCE_FIELDS` keys. Investigated `FollowCamera.tsx` closure staleness (`followCfg` missing from effect deps, wheel handler captures stale min/max): fallbacks are close enough to config values in practice, not worth shipping. 2471 fast tests pass + 1 pre-existing flake (`test_viewer_integration_rt.py::test_invalid_mode`, scene-path-check-before-mode-check ordering, documented in earlier bulletin entries) + 24 skipped. No ship. **Note for next agent**: this focus area has now had 7+ same-day passes with no findings across PRs #622/#624/#625/#628/#630/#631/#633/#634/#652/#657/#659/#661. Rotate — voxel mesher/cache invalidation, scenario JSON load path, and MIMO panel/ECBF solver surface remain underweight.

- [2026-04-19] code-reviewer: Focus area: **type safety and API contracts across frontend and backend**. Fast suite was not clean on HEAD — `tests/test_viewer_schemathesis_smoke.py::test_compute_endpoint_no_5xx_on_fuzz` failed with a 500 from `/api/compute` when hypothesis synthesised `freq_hz=5e-324` (smallest positive IEEE-754 subnormal). Traced to `src/aegis/tissue/fresnel.py:18` `n_complex`: `omega * EPS_0 = 2π * 5e-324 * 8.854e-12` underflows to exactly `0.0`, then `sigma / 0.0` raises `ZeroDivisionError` on Python's scalar complex path (numpy paths like `cole_cole.py:50` are insulated by `safe_omega`, but `n_complex` is scalar). Existing parser validation (`_parse_freq_and_tissue` in `src/aegis/viewer/routes/compute/_parsing.py:116-117`) only caught `freq_hz <= 0` or non-finite — subnormals slipped through. Reproduced at the library level: `n_complex(2.5, 0.5, 5e-324)` raises `ZeroDivisionError: complex division by zero`. **Fix** strengthens the parser check to reject any `freq_hz` where `2π * freq_hz * EPS_0` underflows to ≤0 (the exact condition that causes downstream blowup). Threshold is ~4e-298 Hz — astronomically below any physical RF scenario so no legitimate callers are affected. Added `tests/test_viewer_schemathesis_repro_freq_hz_underflow.py` (9 cases: explicit 5e-324 → 400 with `freq_hz` in message, parametrised scan 1e-320/1e-310/1e-300/1e-200/1.0/1e3/28e9 all non-5xx, 28 GHz still 200). Affects only `/api/compute` directly because that's what schemathesis hits, but the shared `_parse_freq_and_tissue` helper is also used by `/api/compute/rt`, `/api/compute/sionna-rt`, `/api/compute/voxel-rt`, and `/api/optimize` — all those now get the same protection. 2794 fast tests pass (was 2793 + 1 failing). `test_viewer_integration_rt.py::test_invalid_mode` is still flaky on master (unrelated scene-path-check-before-mode-check ordering, pre-existing per 2026-04-19 bulletin entry). **Shipping as PR.** Continuing next runs on bulletin-flagged underweight areas: voxel mesher/cache invalidation and scenario JSON load path.

- [2026-04-19] feature-agent: Focus area: half-baked feature polish — **scenario switches and top-left "aegis" wordmark reset do not actually clear the user's previously-placed antennas**. Repro: load `urban_ghent` (1 antenna at building height), place 2 extra antennas by click, then switch to the `empty` scenario or any other scenario whose `webState.antennaPos` is declared — all 3 antennas stay on the scene. Root cause: `useScenario.applySimulationState` invoked `setAntennaPos(webState.antennaPos ?? null)`, which for `null` only calls `selectAntenna(null)` (deselect) and for `[x,y,z]` only moves the selected antenna; the `useAntennaStore.antennas` Map is never wiped. The `Toolbar` wordmark "aegis" reset had the same hole — it called `setAntennaPos(null)` and relied on `clearScene()` to hide poles, but `clearScene()` (scene store) doesn't touch the antenna store at all. **Fix**: added `clearAntennas()` to `useAntennaStore` (empties the Map, resets `selectedId` and `_nextNumber`) and called it from both sites. Semantics: a scenario that declares `antennaPos` (null *or* position) is stating the full antenna state; we wipe first, then apply. Scenarios that omit `antennaPos` (none do today, but the field is optional) leave user antennas untouched. **Files**: `aegis-web/src/stores/antenna.ts` (+interface+impl), `aegis-web/src/hooks/useScenario.ts` (clearAntennas before setAntennaPos), `aegis-web/src/components/layout/Toolbar.tsx` (clearAntennas + explicit `setState({antennaPos:null})` so the simulation-store mirror is reset without going through the legacy setAntennaPos branch). Added `aegis-web/src/stores/__tests__/antenna.test.ts` (3 cases: empties+resets selectedId, numbering resets to ant_1 after clear, no-op on empty). 97/97 vitest pass (was 94), `npx tsc --noEmit` clean, `npm run build` succeeds, `python -m ruff check` clean. **Notes for next agent**: (1) the Delete/Backspace keybinding in `useKeyboard.ts:26` still calls `setAntennaPos(null)` with the intent of deleting one antenna — it now wipes only the simulation mirror / deselects, leaving the remaining antenna poles; consider rewriting it to call `removeAntenna(selectedId)` directly (out of scope here to keep PR focused). (2) `ScenarioWebState.clearResults` and `clearScene` flags on the `empty` scenario in `default_config.json:822-823` are dead — `loadScenario` unconditionally calls both. Harmless but misleading; could be dropped in a cleanup pass. (3) Voxel mesher/cache invalidation investigation: the raytracer's `_voxel_scene_cache` keyed on `"default"` is safe because all four caller routes guard on `voxel_positions is None` before calling `get_or_build_voxel_scene`, and `_cache_loaded_voxels` always clears the raytracer cache on success. The only minor issue is `_clear_voxel_cache` (failure path at `src/aegis/viewer/server/_voxels.py:75-80`) doesn't re-clear the raytracer cache, but since voxel_positions also gets nulled there, no caller can reach `get_or_build_voxel_scene` with stale data — it's a trivial memory leak, not a correctness bug. **No ship needed** on voxel cache.

- [2026-04-19] code-reviewer: Focus area: **React components — state management, stale closures, missing cleanup**. **Found and fixed Delete/Backspace shortcut leaving orphaned antennas** flagged as "out of scope" by the #662 author. `useKeyboard.ts:26` `deleteAntenna` still called `setAntennaPos(null)`, which post-multi-antenna only sets `sim.antennaPos = null` and `selectAntenna(null)` (see `simulation.ts:200-210`). With >=1 antenna in the store the `useAntennaStore` subscription's `state.antennas.size === 0` branch never fires, so the antenna stayed in the map, its pole stayed visible in the scene, and subsequent click-to-place hit the dead `pos && selectedId` branch (nothing added). Repro: place 2 antennas -> select one -> press Delete -> pole remains; single-antenna case has the same pole-remains problem plus stale `sim.antennaPos = null` so compliance/stats blank out. Fix: call `antStore.removeAntenna(selectedId)` so `removeAntenna`'s own selection-transfer logic runs; the existing subscription then syncs `sim.antennaPos` to the new selection (or null when the map empties). Guard on `selectedId` instead of `sim.antennaPos` so the handler is idempotent when nothing is selected. Added 2 regression tests in `aegis-web/src/stores/__tests__/antenna.test.ts`: (a) remove on sole selected id empties store + clears `sim.antennaPos` via subscription, (b) remove on selected id with another antenna transfers selection + syncs `sim.antennaPos` to remaining. 99/99 vitest pass (was 97), `npx tsc --noEmit` clean, `npm run build` succeeds, `python -m ruff check` shows only pre-existing PT006 in `test_channel_generator.py` (unrelated). Also walked the rest of the React diff (`FollowCamera.tsx` wheel closure captures `followCfg.{min,max}_distance` via `[mode,gl]`-only deps — prior reviewer punted as "fallbacks close enough", and since viewerConfig is loaded once pre-mount this is genuinely not triggerable; `useBodyLoader` abort cleanup correct, disposes old geometry via scene store; `EnvironmentOSM`/`AntennaArray` geometries disposed on unmount via `useEffect` + `geo.dispose()`; `CoverageMap` `overlay.setMap(null)` + `listener.remove()` correct; `Toolbar` aegis reset now clears antennas+results+scene, does NOT reset MIMO/environment/basestations/optimize stores but that's arguably intentional for a "return to default" vs "full wipe"). 2817/2820 fast tests pass (3 pre-existing `test_viewer_integration_rt.py::TestComputeRtInvalidPayload` failures on master). **Shipping as PR.**

- [2026-04-19] code-reviewer: Focus area: **type safety and API contracts across frontend and backend**. **Found and fixed 500 on `/api/parametric-body`** for non-numeric or ragged `betas`/`pose`. The 4408cbc/PR #650 migration replaced top-level `body.get(...)` crashes with a 400 guard, but inside `parametric.py` the `np.array(raw_betas, dtype=np.float64)` at line 34 and `np.array(pose, dtype=np.float64)` at line 48 still propagated ValueError (e.g. `["abc"]` → "could not convert string to float"; `[[1,2],[3]]` → "inhomogeneous shape") as a 500. Reproduced all four shapes via Flask test client. **Fix** wraps both np.array calls in try/except `(TypeError, ValueError)` returning 400 with a clear message, and adds `ndim != 1` rejection so `betas=[[1.0,2.0],[3.0,4.0]]` (same-length nested) also returns 400 instead of crashing inside `pb.generate()`. Added 4 parametrized regression cases (`betas=["abc"]`, `betas=[[1,2],[3]]`, `pose=["abc"]`, `pose=[[1,2],[3]]`) to `tests/test_viewer_parametric.py`. Surveyed the other `np.array(..., dtype=np.float64)` call sites in viewer routes — `optimize.py`, `compute/misc.py` (lsp-heatmap), `mimo.py` `_parse_vec3`, and the compute parsers all already have try/except guards; parametric was the last unguarded one. 6/6 parametric tests pass, full fast suite 2753 passed + 24 skipped (pre-existing `test_viewer_integration_rt.py::test_invalid_mode` flake unrelated, same scene-path-check-before-mode-check ordering noted in prior entries). Ruff clean. **Shipping as PR.** **Note for next agent**: viewer request-body boundary guards are now exhaustive (14 endpoints migrated by PR #650 + `_parse_users_cfg` isinstance in PR #659 + freq_hz subnormal in PR #657 + this one). Rotate: bulletin-flagged underweight areas remaining are voxel mesher/cache invalidation and scenario JSON load path.

- [2026-04-20] code-reviewer: Focus area: **error handling and input validation at system boundaries**. The prior 2026-04-19 bulletin claim that "viewer request-body boundary guards are now exhaustive" was wrong — three unguarded `float()` call sites remained on POST routes, each returning 500 on non-numeric input. **Fixed 500s on three endpoints**: (1) `/api/mimo/compute` with a `users[*].orientation` that is a string/list/dict/None — `float(u.get("orientation", 0.0))` at `mimo.py:169` raised `TypeError`/`ValueError`; (2) `/api/environment/osm` with `options.default_building_height` non-numeric — `float(default_building_height)` at `environment.py:63` inside `max(0.1, min(..., 500.0))` raised `ValueError`; (3) `/api/environment/3dtiles` with `geometric_error` non-numeric — `float(body.get("geometric_error", ...))` at `environment.py:149` raised `ValueError`. Reproduced all three against the Flask test client before the fix (returned 500 with `ValueError`/`TypeError` in the stack) and after the fix (return 400 with field-specific message). Fix adds a `_parse_orientation` helper to `mimo.py` that also rejects `inf`/`NaN` before they propagate into `np.cos`/`np.sin` and produce NaN device positions; the two env routes get try/except `(TypeError, ValueError)` wraps matching the existing pattern. Regression tests added: 5 parametrized cases to `tests/test_viewer_mimo_routes.py::TestBuildScene` (4 bad-type orientations + 1 non-finite), 3 parametrized cases to `tests/test_viewer_environment_routes.py::TestEnvironmentOsm` (default_building_height), 3 to `TestEnvironment3DTiles` (geometric_error). 95/95 in those two files pass (was 83); full fast suite 2826 passed + 24 skipped with 1 unrelated pre-existing failure (`test_viewer_integration_environment.py::TestEnvironmentMalformed::test_unicode_keys_ignored`, Overpass API returning 406 Not Acceptable — confirmed pre-existing by stashing and re-running on master). Ruff + format clean. **Shipping as PR.** **Note for next agent**: a narrower scan of remaining unguarded `float()`/`int()` at request boundaries remains worthwhile (e.g. `optimize.py:222-223,238-241` pass `p_max`/`tilt_init_deg`/`power_init_dbm`/`icnirp_limit` straight through without type coercion, relying on `run_optimization` to fail gracefully — not a known 500 source but a thin seam); MIMO panel/ECBF solver surface and voxel mesher/cache invalidation remain the underweight review areas.

- [2026-04-19] feature-agent: Focus area: scenario JSON load path (one of the two bulletin-underweight areas). **Shipped PR #671**: `useScenario.loadScenario` and the Toolbar aegis-wordmark reset only wiped antennas/sim-results/scene-geometry — `useBaseStationsStore`, `useMIMOStore`, and `useOptimizeStore` were never cleared, so switching from `coverage_globe` to `urban_ghent` painted world-wide basestations over Ghent, MIMO users stuck across `urban_ghent` → `open_ground`, and optim history/scrubber survived unrelated scene changes. Factor the wipe into `resetScenarioScopedStores()` in `aegis-web/src/hooks/useScenario.ts` (exported) and call it from both the scenario loader and `Toolbar.tsx` wordmark click, so the two "start fresh" affordances stay in lockstep. `useMIMOStore.reset()` also disposes user body geometries (prior code only emptied the Map but the geometries were already disposed there). Added `aegis-web/src/hooks/__tests__/useScenario.test.ts` with 4 vitest cases (basestation wipe, MIMO user + precoder wipe, optim history + mode wipe, idempotence on empty state). 103/103 vitest pass (was 99), `npx tsc --noEmit` clean, `npm run build` succeeds. **Sentry issue #666** was investigated (label `sentry`, "index 0 out of bounds" on `/api/compute` — pathSource=stochastic + freq=28 GHz + powerDbm=43) — Robin already triaged it as a duplicate of #663 that's fixed on master by PR #664 (`f579093`); the event is on release `7bdd91a`, pre-fix, so Sentry release tracking will auto-resolve on the next production deploy. No code change needed. **Remaining bulletin-underweight areas**: voxel mesher/cache invalidation (prior `feature-agent` entry 2026-04-19 deemed it "trivial memory leak, not correctness" so no ship needed — a fresh reviewer could re-check) and MIMO panel/ECBF solver surface (`aegis-web/src/stores/mimo.ts:263` `setPrecoderType` has no M<K guard, and ECBF infeasibility fallback in `src/aegis/coherent/ecbf.py:155-163` returns the min-absorption precoder without surfacing the warning to the frontend — both candidates for a follow-up). 24 commits since v0.30.0 (mostly bug fixes / QA logging / boundary validation, with new features from #661/#662/#665/#671 touching scenario UX); likely a PATCH release candidate (`v0.30.1`), but bundle it with ongoing work rather than tagging mid-cycle.

- [2026-04-20] code-reviewer: Focus area: **integration bridges (DiffeRT, Sionna) and ray tracing pipeline**. **Found and fixed NaN `power_dbm` bypass on `/api/compute/voxel-rt`**. `_parse_voxel_rt_params` in `src/aegis/viewer/routes/compute/voxel_rt.py:83-88` only had the `< 0` / `> _MAX_POWER_DBM` range check — NaN passes both (all comparisons with NaN return False). Inf was already caught by the `> _MAX_POWER_DBM` branch. Meanwhile the sibling routes `/api/compute/rt` (`rt.py:91`) and `/api/compute/sionna-rt` (`sionna_rt.py:59`) both use a `math.isfinite` guard before the range check. In our local dev env Modal is unavailable so the bug masquerades as 501 "GPU unavailable"; in production with Modal enabled a NaN `power_dbm` would silently propagate through to Sionna → NaN CIR coefficients → NaN `psi`/`power` → NaN dosimetry output with a 200 response. **Fix**: added the same `math.isfinite(power_dbm)` guard + error message used by the other two RT routes. Added a parametrized regression test (`test_nonfinite_power_dbm_rejected_before_modal` covering `nan`/`+inf`/`-inf`) in `tests/viewer/test_compute_routes.py::TestComputeVoxelRtExtended` that stuffs voxel positions into the cache so the parser runs (previous `test_invalid_power_dbm_returns_400` accepted either 400 or 404 for no-voxels + string power, which made it uninformative). Verified against Flask test client: `power_dbm=nan` now → 400 `"power_dbm must be a finite number"`. Walked the rest of the RT surface: `paths_from_differt` (`src/aegis/integration/differt.py`) and `paths_from_sionna_scene` (`src/aegis/integration/sionna.py`) are solid — degenerate-path filtering preserves scene-level normals/materials (PR #653 regression test still passes), TE/TM polarisation tracking via `_track_polarisation` handles LOS + padded/edge-mode paths correctly, JAX path ordering (`_paths_from_sionna_jax`) for multi-element synthetic arrays is consistent between `element_index`, `psi`, `k_hat`, `tau`, and `is_los`. Also looked at the `paths.mask` handling asymmetry (`integration/differt.py:427` has a `None` guard; `viewer/raytracer.py:229` doesn't) — not triggerable on DiffeRT 0.7.0, verified empirically that `scene.compute_paths(order=N)` always returns a non-None mask (even when empty for 0 candidates). Ruff clean. 98/98 tests pass in `tests/viewer/test_compute_routes.py` + `tests/test_integration.py`. Two unrelated pre-existing failures in `tests/test_viewer_integration_rt.py` (`test_rt_at_60ghz`, `test_basic_rt_compute_box_scene`) are worktree-environment issues — they hardcode `/home/user/aegis/data/scenes/box/box.xml` which doesn't match `_validate_scene_path`'s resolved allowlist inside the `/tmp/aegis-agent-code-review-agent-*` worktree; confirmed these fail on unmodified master too. **Shipping as PR.**

- [2026-04-19] code-reviewer: Focus area: **frontend data flow** (Zustand store updates, API response handling, loading/error states) — 8th same-day pass. **No bugs found.** Re-verified PR #671 (`resetScenarioScopedStores` in `aegis-web/src/hooks/useScenario.ts` + Toolbar wordmark click, shared wipe basestations/MIMO/optim), PR #665 (`useKeyboard.ts` Delete → `removeAntenna(selectedId)` with idempotent guard), PR #662 (`useAntennaStore.clearAntennas` + `applySimulationState` wipe-before-apply for scenarios declaring `antennaPos`), PR #661 (basestation band filter consistency: `computeActiveCount`/`activeIndices`/`BaseStationMarkers.tsx` gate on `b.frequency_band && !enabledBands.has(...)` without `.size > 0` guard), PR #669 (`PatternBrowserPanel` `loadGenRef`/`searchGenRef` race-safety on select/clear/search). Walked the `simulation.ts:355` antenna-store subscription: the Delete-one-of-two flow (removeAntenna transfers selection, subscription syncs `sim.antennaPos` to remaining antenna's position) and the clear-all flow (antennas empty + selectedId null → `antennaPos = null`) both work correctly. Walked `PatternBrowserPanel.handleSelect` × `handleClear` interleaving: A-load-then-clear-then-B-load correctly invalidates A's stale response via `loadGenRef` bump, keeps `patternLoading=true` only for the latest active request via `if (loadGenRef.current === myGen) setPatternLoading(false)` guard in `finally`. `StochasticPanel` `NumClusters`/`NumSubPaths` onChange guards (`Number.isInteger(n) && n >= 1`) correctly reject `""` (→0), `"0"`, `"1.5"`, `"abc"` (→NaN). `MIMOStore.reset()` disposes all user `bodyGeometry` before `set({ ...INITIAL_STATE, users: new Map() })` — the fresh Map is critical because `INITIAL_STATE.users` is created once at module load and would leak if reused. `useScenario.applySimulationState` correctly orders `clearAntennas()` before `setAntennaPos(webState.antennaPos)`: the clear triggers subscription → `sim.antennaPos = null`, then the subsequent `setAntennaPos` path re-adds the scenario's antenna via `addAntenna(pos)` (size=0 branch). 103/103 vitest pass. **No ship** — bulletin-only update committed direct to master.

- [2026-04-19] code-reviewer: Focus area: viewer backend routes and API edge cases. **Found and fixed silent NaN/Inf acceptance on three analysis GET endpoints**: `/api/compliance/power-sweep`, `/api/compliance/heatmap`, `/api/compliance/frequency-sweep`. Flask's `type=float` accepts `"NaN"`/`"Infinity"` (Python `float()` parses them), so the endpoint-level `value is None` / `value <= 0` checks let non-finite floats through. Downstream compliance math then produced all-True or all-False compliance arrays with HTTP 200 — the worst kind of silent failure for a compliance tool because the user cannot tell the answer is meaningless. Reproduced 6+ cases (`sab_4cm2=NaN`, `freq_hz=Infinity`, `ref_power_dbm=NaN`, `sinc_local=NaN`, etc.) via Flask test client returning 200 with garbage payloads and `RuntimeWarning` in logs. The query-arg campaign from PRs #668/#670 covered POST request bodies but these GET endpoints were missed. Sibling `_tissue_spectrum_impl` in the same file already has the `math.isfinite` guard (was PR #668's scope). **Fix** adds `_reject_non_finite` helper in `analysis.py` and calls it on all numeric query args in all three endpoints. Added `TestComplianceNonFiniteQueryArgs` to `tests/viewer/test_analysis_routes.py` with 24 parametrized cases (10 power-sweep, 8 heatmap, 6 frequency-sweep). 76/76 analysis tests pass, 270/270 viewer tests pass, 2834/2837 fast tests pass (3 pre-existing `test_viewer_integration_rt.py::TestComputeRtInvalidPayload` failures on master, unrelated, documented in prior bulletin entries). Ruff clean. **Shipping as PR.** **Note for next agent**: viewer float-boundary guards for both POST bodies and GET query args are now exhaustive across analysis/compute/parametric/mimo/basestations routes. Voxel mesher/cache invalidation and MIMO panel/ECBF solver surface remain the bulletin-flagged underweight areas.

- [2026-04-19] feature-agent: Focus area: MIMO panel / ECBF solver surface (bulletin-underweight). **Shipped PR #676**: the MIMO panel treated all non-MRT precoders as requiring M >= K, but MMSE is regularized (`HHH + noise_power * I` always invertible) and works at any M, K — only ZF and zf_exposure strictly need M >= K. Concrete impact: with M=16 antennas and K=20 users the MMSE button was greyed out, and if MMSE was already selected beforehand the store silently fell back to MRT when user count crossed M. Verified against the precoder directly: `mmse(H, P=1.0)` with `H.shape=(4, 2)` returns a valid (2, 4) matrix with `||W||_F^2 ≈ 1`, while `zf` raises `M_ant (2) must be >= K (4)`. **Fix**: new exported `precoderRequiresMgeK(type)` helper in `aegis-web/src/stores/mimo.ts`; `MIMOPanel.tsx` `needsMoreAntennas` and the store's `addUser` / `setArrayConfig` auto-fallback now gate on it, so MMSE is preserved across user/array changes. `setPrecoderType` itself now also applies the guard as defense-in-depth for programmatic callers (scenario JSON, tests, future URL state). 7 new vitest cases in `stores/__tests__/mimo.test.ts` (helper selectivity, MMSE preserved past M via addUser/setArrayConfig, ZF/zf_exposure fallback to MRT, `setPrecoderType` rejection of ZF at M<K). 109/109 vitest pass (was 103), `npx tsc --noEmit` clean, `npm run build` succeeds. **Notes for next agent**: (1) the ECBF infeasibility warnings at `src/aegis/coherent/ecbf.py:155-163, 171-176, 186-192, 198-199` still go to `warnings.warn` only — not captured into the `/api/mimo/compute` summary payload, so the frontend has no way to tell the user that the min-absorption fallback was used. This is the remaining "MIMO panel/ECBF" gap in the bulletin-flagged list. (2) On the backend, `_api_mimo_compute_impl` still returns 500 on any `compute_mimo_scene_with_bodies` ValueError — it should probably return 400 for the `M_ant (...) must be >= K (...)` case (user-facing validation, not a server bug). Out of scope here to keep the PR focused on the frontend bug. (3) 28 commits since v0.30.0 (now #675/#676 added today); still PATCH-shaped (`v0.30.1`), still no mid-cycle tag.

- [2026-04-19] code-reviewer: Focus area: **base station pipeline — scrapers, adapters, merge, coverage**. **Found and fixed a spatial-dedup clustering bug**: `_bfs_cluster_labels` in `src/aegis/basestation/merge.py` compared each candidate neighbour's FrequencyBand against the **seed** row's band, not against the cluster's accumulated known band. When the seed's band was NaN (common for OCID rows that lack band metadata), the guard `pd.notna(fb_seed) and pd.notna(fb_j) and fb_seed != fb_j` never fired, so two neighbours with **explicitly different** known bands (e.g. a 1800MHz panel and a 2100MHz panel co-located at the same site) both joined the seed's cluster. `_merge_cluster_rows` then kept the first non-null band and dropped the other antenna's data on the floor. Reproduced: 3 rows at the same location, operator=Proximus, bands=[NaN, Band1800, Band2100] collapsed to a single merged row (bands 1800 + 2100 silently merged into one entry). Fix: track the cluster's known band during BFS, initialise from the seed, upgrade on the first non-null row, and reject subsequent rows whose known band disagrees. Same-band and NaN-only clusters still merge; `test_different_band_does_not_merge` still passes. Added `tests/test_merge.py::TestSpatialDedup::test_different_bands_do_not_merge_via_nan_bridge`. Full basestation/merge/coverage/viewer_basestations suites: 143 passed. Fast suite: 2827 passed + 4 pre-existing failures (`TestComputeRtInvalidPayload` {3}, `TestEnvironmentMalformed::test_unicode_keys_ignored`) unrelated to this change (verified by stashing). Ruff clean. **Shipping as PR.** Other bulletin-flagged underweight areas remaining: voxel mesher/cache invalidation (reviewed earlier, deemed non-issue) and MIMO panel/ECBF solver surface. Other observations while reading the pipeline: `coverage.py:131-132` casts `op_indices`/`tech_indices` to uint8 without `.clip(upper=255)` — silently wraps at 256 operators/technologies, but real data has <20, so theoretical only; `_compute.py:_parse_max_distance` accepts NaN (`nan <= 0 or nan > 50000` is False on both sides) — `dist > nan` is False so all basestations get included, non-crashing but no bounding — flag it and move on, not worth a separate PR; `msi.py::_read_pattern_rows` silently leaves out[j]=0.0 (= peak gain!) on malformed rows instead of leaving them as NaN — possible data integrity issue but MSI files in the repo are well-formed.
- [2026-04-19] code-reviewer: Focus area: **physics kernels and compliance** (levels 0-8, ECBF, spatial averaging, ICNIRP). **Found and fixed chunk-size-dependent drift in `spatial_kernel` chunked path** (PR #682) at `src/aegis/kernels/spatial.py`. The inner `_spatial_kernel_unbatched` clamped `sab = max(sab, 0)` before returning when `curvature or diffraction`; the outer chunked wrapper then summed those per-chunk clamped vectors. Because the physical GELU activation can go negative at the shadow boundary (mu slightly <0 → `g ≈ mu/2 * (1 + erf(mu/sigma))` is negative), individual chunks can sum negative per triangle while the full sum is positive (or vice versa). Per-chunk clamping loses that signed information so the final answer depended on chunk size, which itself depends only on `_MAX_MN_ELEMENTS` (50M) / M. Repro (4 triangles, 20 paths, diffraction=True, fresnel=False): chunk sizes 1/2/3/5/7/13 all drifted from the unchunked reference by up to ~1.3% per triangle. Fix: drop the clamp in `_spatial_kernel_unbatched`, clamp once at the end of `spatial_kernel` after all chunks are summed. Fast path behaviour is bitwise unchanged. Production levels 0-6 use their own per-level kernels (`level5_curvature.py`, `level6_diffraction.py`, etc.) which already clamp at end only, so this bug only affected the unified `spatial_kernel` API (`engine.py` `mode="spatial"` path, reached when callers compose non-standard flag combos with M*N > 50M on the NumPy backend). JAX backend was unaffected (chunking disabled). Added `TestChunkingDeterminism` to `tests/test_spatial_kernel.py` (4 parametrized flag combos × 6 chunk sizes = 24 cases, monkeypatching `_MAX_MN_ELEMENTS`). Verified the new test fails on master with rtol ≥1e-2. 2830 fast tests pass (+4 new); 4 pre-existing failures on master (`test_viewer_integration_rt::TestComputeRtInvalidPayload` ×3, scene-path-check-before-mode-check ordering; `test_viewer_integration_environment::test_unicode_keys_ignored`, overpass 406) unrelated to physics. Ruff clean for changed files (pre-existing PT006 in `test_channel_generator.py`). Also reviewed but found clean: ECBF QCQP solver (`coherent/ecbf.py` — slack regime, bisection bracket expansion, rank tolerance, MRT fallback), compliance module (`compliance/__init__.py` — ICNIRP limit computation, power/frequency sweeps, haversine, qinv guards), spatial averaging (`geometry/averaging.py` — Numba + NumPy paths match on ICNIRP 4 cm² squares, CSR has no duplicates), shared kernel primitives (`kernels/_base.py` — Fresnel mu-clipping, physical GELU sign preservation). **Notes for next agent**: physics kernel surface is now well-covered; ECBF solver's infeasibility fallback (min-absorption MRT) still doesn't surface a warning to the frontend as flagged in prior bulletin entry — candidate for a follow-up. Voxel mesher/cache invalidation remains the only bulletin-flagged underweight area without a ship (two prior reviewers concluded "trivial leak, not correctness"), so re-reviewing would likely be low-yield.

- [2026-04-20] feature-agent: Focus area: **defensive hardening of OSM environment cache** (`src/aegis/viewer/cache.py`). Open Sentry issues #680 (`/api/environment/osm` 500) and #666 (`/api/compute` "index 0 out of bounds") were already triaged in earlier bulletin entries — both are pre-deploy events on release `7bdd91a`, both fixed by merged PRs (#679 and #664 respectively), Sentry release tracking will auto-resolve once the new release ships. **Proactive fix**: `EnvironmentCache.get_binary` / `get` previously propagated `OSError`/`json.JSONDecodeError` from corrupt or partially-written cache files, surfacing as a 500 to the next request that hit that exact (lat, lon, radius, options) cache key. Corruption was reachable because `put_binary` / `put` wrote `bin`/`meta` non-atomically — a process kill between the two `open()` blocks (deploy, OOM, manual restart) left a real bin paired with a missing or truncated meta. Fix: (1) `put_binary` and `put` now write via temp-file + `os.replace` so partial writes can't be observed; on failure both files are removed so the next request refreshes cleanly. (2) `get_binary` and `get` catch `(OSError, json.JSONDecodeError)`, log a warning, delete the offending entry, and return `None` so the caller treats it as a normal cache miss. Six new vitest-style tests in `tests/viewer/test_cache.py` cover: round-trip, corrupt-JSON-on-read, corrupt-meta-on-read with both files cleaned, missing-meta sidecar, and atomic-write rollback when the second write fails. 11/11 cache tests + 71/71 environment route tests pass; 315 cache+env+osm tests pass overall. Ruff + format clean. **Shipping as PR.** **Notes for next agent**: the bulletin-flagged underweight areas remain — ECBF infeasibility fallback warnings still aren't surfaced to the frontend (`src/aegis/coherent/ecbf.py:155-163, 171-176, 186-192, 198-199` all `warnings.warn(...)` only — `level8_ecbf` returns the min-absorption fallback silently). Could be addressed by capturing warnings in `engine._build_result` for `level == 8`, propagating through `DosimetryResult` into the `/api/compute` JSON response, and showing them in the HUD; multi-layer change but tractable. Voxel mesher/cache invalidation: two prior reviewers concluded "non-issue, low yield" — agreed.

- [2026-04-20] code-reviewer: Focus area: **error handling and input validation at system boundaries** (follow-up to 2026-04-20 PR #686). PR #676's frontend work disabled the ZF button when `M_ant < K`, but the backend catchall at `mimo.py:_api_mimo_compute_impl` still mapped every exception from `compute_mimo_scene_with_bodies` to HTTP 500 — so any direct ZF/zf_exposure call with `M_ant < K` (scenario JSON, scripted clients, frontend-state-skew bugs) surfaced `ValueError("M_ant ({M}) must be >= K ({K}) for ZF precoding")` from `precoders.py:44` as a 500 with the full traceback in logs, when it is a user-input error. Same for `compute_precoder`'s `ValueError("Unknown precoder type: ...")` at `precoders.py:148` — any typo in `precoder_type` was 500. **Shipped PR #687**: pre-validate `precoder_type` against `{mrt, zf, mmse, zf_exposure}` and require `M_ant >= K` for `{zf, zf_exposure}` before entering `compute_mimo_scene_with_bodies`; both now return 400 with a specific message. Kept the generic `except Exception` → 500 path unchanged (the `test_compute_exception_returns_500` RuntimeError case still goes through as 500 because it's a legit internal failure). Added 8 regression cases: 5 parametrized unknown precoder_types (`"foo"`, `"ZF"`, `""`, `"maximum_ratio"`, `None`) all return 400 with `precoder_type` in message; 2 parametrized cases for `zf` / `zf_exposure` at M_ant=1, K=2 both return 400 with `M_ant`/`K` in message and assert `compute` was never called; 1 case confirming MMSE at M_ant<K still passes through (200) because MMSE is regularized and works at any M, K. 66/66 mimo route tests pass (was 58), 2839 fast tests pass. Ruff + format clean. **Note for next agent**: the remaining ValueErrors from `mimo/compute.py` (`"scene.tissue must be set"`, `"User X has no body mesh"`, `"User X has no center_paths"`) are internal invariants that `_build_scene` + `compute_mimo_scene_with_bodies` orchestration guarantee are met in practice, so leaving them as 500 is appropriate — if they ever fire, they indicate a genuine backend bug rather than bad user input. The underweight areas remain: ECBF infeasibility fallback warnings not surfaced to frontend (flagged by PR #676 / PR #682 authors), voxel mesher/cache invalidation (two prior reviewers deemed it non-issue — low yield to re-review).

- [2026-04-20] code-reviewer: Focus area: **error handling and input validation at system boundaries**. Independently repro'd the NaN/Inf `ref_power_dbm` bug on `/api/compliance/power-sweep` and `/api/compliance/heatmap` before reading far enough down the bulletin to notice the 2026-04-19 PR #(see entry above) had already shipped `_reject_non_finite` for the same surface. Opened PR #704 with a strict subset fix, then closed it as superseded when the rebase surfaced the existing helper. **No new ship.** Walked the rest of the boundary-guard surface on current master: `/api/compliance/spatial` (validates bbox finite, resolution int, receiver_height range); `/api/parametric-body` (NaN/Inf/ragged betas+pose all caught by PR #670); `/api/tissue/spectrum` (freq finite by PR #668); `/api/lsp-heatmap` (NaN/Inf by PR #701); `/api/compute` family (freq_hz subnormal by PR #657, non-dict users by PR #659, non-dict body by PR #650); `/api/mimo/compute` (precoder_type + M_ant/K by PR #687); `/api/environment/osm` + `/api/environment/3dtiles` (float coercion guards by PR #686); `/api/channel/generator` (NumClusters/NumSubPaths by PR #664). All clean — no new 5xx-on-bad-input surfaces found. Also noted but not worth shipping: (a) `/api/compliance/heatmap` validates `freq_hz` > 0 and finite but never forwards it to `compliance_heatmap()` — the heatmap sweeps `freq_min_hz..freq_max_hz` (7e9..100e9) regardless, so the parameter is effectively dead for callers. Not a correctness bug (matches the UI's "pick a reference" semantic), but a future API cleanup could either forward it or drop it. (b) `/api/compliance/power-sweep` doesn't validate `n_points` finiteness — Flask's `type=int` already rejects NaN/Inf before the handler sees it, so this is safe. Remaining underweight areas per prior bulletins: ECBF infeasibility warnings not surfaced to frontend, voxel mesher/cache invalidation.

- [2026-04-20] feature-agent: PR #689 merged — corrupt env cache files now treated as miss with cleanup, atomic writes via temp+rename. Drive-by fix in same PR: ruff PT006 in `tests/test_channel_generator.py` (pre-existing from #664 broke CI on the new ruff version, parametrize names switched from string to tuple). 2845 fast tests pass on master. **Notes for next agent**: ECBF infeasibility surfacing remains the highest-leverage open item — requires plumbing through `DosimetryResult` + `/api/compute` JSON + HUD. 32 commits since v0.30.0; still PATCH-shaped (`v0.30.1`), bundle further.

- [2026-04-20] code-reviewer: Focus area: **frontend data flow** (Zustand store updates, API response handling, loading/error states) — 9th+ pass on this surface, second since PR #671 merged. **No bugs found.** Re-reviewed the `resetScenarioScopedStores` helper at `aegis-web/src/hooks/useScenario.ts:27-33` — order is `sim.clearResults() → scene.clearScene() → basestations.clear() → mimo.reset() → optimize.reset()`, correctly does NOT clear `useAntennaStore` (per PR #662 semantics: scenarios without `antennaPos` leave user antennas untouched; `applySimulationState`'s `'antennaPos' in webState` branch handles clearing when the scenario declares it). Verified wordmark reset at `Toolbar.tsx:144-158` adds `setState({antennaPos: null}) + clearAntennas()` on top (full wipe) and the order is idempotent — the antenna-store subscription at `simulation.ts:355-370` fires no-op because `antennaPos` was already null. Traced three race-condition edge cases and cleared all: (a) stale MIMO compute after `reset()` — `setUserResult` guards on `!user`, `setSummaryStats` could write but `enabled=false` gates panel visibility and next `setEnabled(true)` triggers a fresh compute via `_configVersion` change; (b) stale basestation compute after `clear()` — narrow window, `applyDosimetryResult` might write stale sim results but subsequent useDosimetry pass overwrites within `debounce_ms`; (c) env store not cleared by `resetScenarioScopedStores` — `osmMeshData` is rendered only when `envSource==='osm'||'3dtiles'` (gated in `SceneRoot.tsx:417`), and `applyEnvironmentState` sets source explicitly. Verified new test file `aegis-web/src/hooks/__tests__/useScenario.test.ts` — 4 cases covering basestations/MIMO/optimize/idempotence all pass; the `'../../stores/simulation'` import-first workaround for the antenna↔simulation module init cycle is correct. Walked the loading/error surface once more: `environment.ts` `freshAbort` + `reportUnexpected` 429/502/503/504 skip list unchanged; `_dosimetryResult.ts` AbortError→network→4xx→Sentry ladder still intact; `useMIMODosimetry` generation counter + `isCurrent()` check on every awaited step prevents cross-compute contamination. 103/103 vitest pass, 2016 fast Python tests pass + 74 skipped (optional-dep gates). **No ship.** **Note for next agent**: this area is now thoroughly covered; rotate to the bulletin-flagged underweight areas — ECBF infeasibility warning surfacing (multi-layer: `DosimetryResult` → `/api/compute` JSON → HUD, highest leverage per PR #689 author), voxel mesher/cache invalidation (two prior reviewers concluded low-yield), and the remaining `float()`/`int()` seams in `optimize.py:222-223,238-241`.

- [2026-04-20] feature-agent: Focus area: **remaining `float()` seams in `/api/optimize`** flagged by the 2026-04-20 code-reviewer entry at `optimize.py:222-223,238-241`. Reproduced via Flask test client: `mimo_peak` mode with `p_max="abc"` / `signal_threshold="NaN"` / `p_max=["bad"]` leaked `TypeError`/`ValueError` tracebacks on the SSE stream (500). `tilt_power` mode with `antenna_direction=[1,2]` / `antenna_direction=["x",0,-1]` / `tilt_init_deg="Infinity"` / `power_init_dbm=[60]` / `icnirp_limit=0` / `T0=-5.0` all either 500'd or silently accepted garbage (NaN/Inf flowing into downstream `np.cos`/`np.sin`/`10**(p/10)` and corrupting the stream). A particularly sneaky case: a stale `_last_dosimetry_stats["T0"] = NaN` poisoned every subsequent tilt_power run because `params.get("T0", cached)` fell back to the NaN default. **Shipping as PR**: added `_require_finite_float(params, key, default, *, positive=False)` and `_require_finite_vec3(params, key, default)` helpers at the SSE boundary of `optimize.py`, then applied them in `_build_mimo_peak_config` (`p_max` positive, `signal_threshold` finite) and `_build_tilt_power_config` (`antenna_direction` vec3, `tilt_init_deg` finite, `power_init_dbm` finite, `icnirp_limit` positive, `T0` positive with cache-NaN sanitization). All bad-input cases now fail fast with a field-specific 400 before the SSE stream opens. Regression tests: 19 new cases in `tests/test_viewer_optimize_route.py::TestMimoPeakInputValidation` + `TestTiltPowerInputValidation` (51/51 pass, was 32). Valid-path SSE streams unchanged. Ruff + format clean. **Notes for next agent**: (1) 3D Tiles silent non-render at `src/aegis/environment/tiles.py::_parse_glb_primitive` — the parser reads raw POSITIONs but does NOT apply the tileset's per-tile `transform` matrix or the glTF scene/node `matrix`/`translation`/`rotation`/`scale` transforms, and silently ignores unhandled glTF extensions. Google Photorealistic tiles heavily use node transforms, so meshes load but render at origin/wrong orientation, indistinguishable from "didn't load" to the user. Best tackled by someone with a Google 3D Tiles API key to verify against a live tileset. (2) ECBF infeasibility surfacing remains the highest-leverage multi-layer item (`DosimetryResult` → `/api/compute` JSON → HUD). (3) `_build_placement_config` was left untouched — its validation surface is larger and already has a mix of try/except around DiffeRT calls; follow-up candidate. 33 commits since v0.30.0 now; still PATCH-shaped (`v0.30.1`).

- [2026-04-20] code-reviewer: Focus area: **viewer feature completeness — trace user flows end-to-end, find dead code paths and unfinished features**. **No bugs found. No ship.** Walked the scenario load → store reset → user interaction → compute pipeline end-to-end against `useScenario.ts`, `Toolbar.tsx`, `simulation.ts`, `antenna.ts`, `mimo.ts`, `basestations.ts`, `optimize.ts`, `MIMOPanel.tsx`, all `routes/` Python endpoints, and `default_config.json` scenario declarations. Three dead-code/dead-config pockets confirmed present (still all "harmless but misleading", not bugs): (1) `aegis-web/src/stores/mimo.ts:61, 104` — `lastSuccessfulUserCount: number` field on the MIMO state interface and `INITIAL_STATE`, never set anywhere and never read (orphan from PR #614 alongside `lastComputeError`/`retryCompute`); (2) `src/aegis/viewer/default_config.json:822-823` — `clearResults: true, clearScene: true` on the `empty` scenario `webState` are dead because `useScenario.loadScenario` now unconditionally calls `resetScenarioScopedStores()` regardless of these flags (PR #671 made them no-ops); (3) `aegis-web/src/api/types.ts:150-151` — `instant: boolean` and `autoCompute: boolean` on `ScenarioEntry` are required in the type but never read by the frontend (one comment at `useScenario.ts:119` says "Apply instant state" but doesn't gate on the flag). Re-verified PR #676's `setPrecoderType` M<K guard via `precoderRequiresMgeK`/`needsMoreAntennas` — works correctly, MMSE preserved past M, ZF/zf_exposure fall back to MRT; **bulletin's prior open concern about `setPrecoderType` lacking M<K guard is fully addressed**. Re-verified PR #687 backend 400-not-500 mapping for unknown precoder_type and ZF/zf_exposure M<K. Re-verified PR #689 atomic env cache write + corrupt-cache recovery. ECBF infeasibility still routes through `warnings.warn` at `src/aegis/coherent/ecbf.py:155-163, 171-176, 186-192, 198-199` without HUD surfacing — unchanged, still the highest-leverage open multi-layer item flagged by 5+ prior bulletin entries. Test baseline: 2845 fast tests pass + 1 pre-existing external `test_viewer_integration_environment.py::TestEnvironmentMalformed::test_unicode_keys_ignored` Overpass 406 (documented in 2026-04-20 PR #688/#691 entries). Decided NOT to ship a cleanup PR for the three dead pockets per workflow guidance — none are bugs, all touch reviewed/recently-shipped code where the maintainer is already aware (PR #671 trimmed `clearScene`/`clearResults`'s reach, PR #676 owned the MIMO state surface). **Notes for next agent**: (1) ECBF→HUD warning surfacing remains the single highest-leverage open item — `DosimetryResult` field, `/api/compute` JSON, HUD chip; multi-layer but tractable. (2) If anyone takes a cleanup-only PR pass, the three dead pockets above + `_clear_voxel_cache` raytracer-cache-skip noted in 2026-04-19 feature-agent entry could land together. (3) 33 commits since v0.30.0 still PATCH-shaped (`v0.30.1`); no change in release recommendation.

- [2026-04-20] code-reviewer: Focus area: **React components — state management, error boundaries, stale closures, missing cleanup**. **No bugs found. No ship.** Overlapping pass with the 2026-04-20 feature-completeness entry above — confirmed everything in that entry plus an explicit re-sweep of React cleanup patterns: subagent-assisted review across `useDosimetry`, `useMIMODosimetry`, `useOptimization`, `useBodyLoader`, `useGltfBody`, `useVoxelLoader`, `useBaseStationsDosimetry`, `usePhysics`, `FollowCamera`, `CoverageMap`, `EnvironmentOSM`, `EnvironmentTerrain`, `AntennaArray`, `BaseStationMarkers`, `ClusterPaths`, `LSPHeatmap`, `VoxelField`, `GuidedTour`, `HistoryScrubber`, `KeyboardHelp`, `BugReporter`, `ServerInfoBadge`, `StatusBar`, `ScenePanel`, `Toolbar.tsx`, `PatternBrowserPanel.tsx`, `StochasticPanel.tsx`, `useKeyboard.ts`, `useScenario.ts`. Generation-counter + AbortController pattern consistent throughout (`useDosimetry` compute+LSP, `useMIMODosimetry` isCurrent, `useBaseStationsDosimetry`, `PatternBrowserPanel` search+load, `useOptimization` abortRef-identity). Three.js disposal paired with every set (body store, MIMO user bodies on `removeUser`/`reset`, LSPHeatmap/EnvironmentOSM/AntennaArray on unmount). Every addEventListener/setTimeout/setInterval/RAF/EventSource has a paired cleanup. Spot-checked two subtle patterns: (a) `PatternBrowserPanel`'s debounce cleanup works even when `debounceRef.current` is overwritten on each effect run, because cleanup fires before next effect body so the ref always points to the active timeout at cleanup time; (b) `StochasticPanel`'s `useEffect(..., [])` with stale `preset` closure is neutralized by the second `useEffect([preset, allPresets])` that always runs after `allPresets` is populated (the stale-closure setPresetParams call is immediately overwritten by the correct one). **Prior bulletin concern about `setPrecoderType` M<K guard is now stale — PR #676 added it.** 103/103 vitest pass, `npx tsc --noEmit` clean. Fast Python suite: 1017 passed + 2 pre-existing flakes (`TestEnvironmentMalformed::test_unicode_keys_ignored` Overpass 406, and `test_viewer_integration_rt.py::test_invalid_mode` scene-path-before-mode ordering). **Note for next agent**: React-cleanup surface is well-covered. ECBF→HUD warning surfacing remains the single highest-leverage open item per multiple prior entries. Overlapped with a concurrent 2026-04-20 pass — expected under parallel agents; merge-resolved bulletin preserves both.

- [2026-04-20] feature-agent: Follow-up to PRs #676/#687 which covered MIMO precoder feasibility but left the toggle-off cleanup hole. **Fixed MIMO `setEnabled(false)` leaving per-user state alive.** `setEnabled(false)` at `aegis-web/src/stores/mimo.ts:109-113` was only clearing `enabled` + `lastComputeError`, leaving `users` Map (with undisposed `bodyGeometry` on the GPU), `focusedUserId`, `controlledUserId`, `summaryStats`, `precoderWeights`, `lastSuccessfulUserCount`, and `_nextUserNumber` intact. Repro: enable MIMO → add User 2 → run compute → toggle MIMO off → toggle back on → both users re-appear with stale results, the `users.size === 0` auto-add branch never fires, and stored `precoderWeights` render on an array that may have been reconfigured between sessions. **Fix**: on `setEnabled(false)`, dispose per-user body geometries and reset per-user state while preserving `arrayConfig`, `precoderType`, `focusPoint`, `showAllHeatmaps`, `showArrayPattern` so a mode toggle keeps the user's array configuration but re-enters via the auto-add User 1 flow. Added 2 regression tests to `aegis-web/src/stores/__tests__/mimo.test.ts` (per-user wipe + config preservation; toggle cycle re-adds User 1). Works cleanly against PR #676 (`precoderRequiresMgeK` + `setPrecoderType` guard), which landed on master concurrently. 105/105 → 113/113 vitest pass after auto-merge with #676's tests, `npx tsc --noEmit` clean, `npm run build` succeeds. **Notes for next agent**: (1) ECBF infeasibility fallback at `src/aegis/coherent/ecbf.py:155-163, 171-176, 186-192, 198-199` still only emits `warnings.warn` — thread through `DosimetryResult` + `/api/compute` → HUD chip remains the highest-leverage open item. (2) Coverage `complianceZone` isn't cleared on scenario change (`useScenario.ts:65-73`); low severity because `complianceZoneEnabled` gates use. (3) `osmMeshData` persists across scenario source-changes (brief visual flicker during re-fetch); `SceneRoot` gates `<EnvironmentOSM />` on `envSource === 'osm'|'3dtiles'` so source-changes to 'none' hide cleanly. (4) Open Sentry #666/#680 were already triaged as duplicates pre-deploy-7bdd91a, Sentry release tracking will auto-resolve once production catches up. 34 commits since v0.30.0 now; still PATCH-shaped (`v0.30.1`).
- [2026-04-20] code-reviewer: Focus area: **3D scene — Three.js resource lifecycle, geometry disposal, camera state, rendering**. **No bugs found.** Walked every component in `aegis-web/src/components/scene/` plus `hooks/useGltfBody.ts`, `hooks/useBodyLoader.ts`, `hooks/useMIMODosimetry.ts`, `stores/scene.ts`, `stores/mimo.ts`, `hooks/useScenario.ts`. Disposal patterns are consistent and correct: `EnvironmentOSM`/`EnvironmentTerrain`/`AntennaArray`/`HullMesh`/`LSPHeatmap`/`ComplianceRing`/`DistanceLine`/`SceneGeometry`/`ClusterPaths`/`Antenna` use the `useMemo` create → `useEffect` cleanup pattern with `geo.dispose()` (or a `prevGeoRef` pattern where the dep changes inline); `BodyMeshInstance` mutates the existing color buffer in place (correct); `VoxelField` uses count-based `instancedMesh` args. Scene store `setBodyGeometry` disposes prior, MIMO store `removeUser`/`reset`/`setUserBodyGeometry` all dispose user body geometries, `useBodyLoader` aborts + disposes on unmount, `useScenario.resetScenarioScopedStores` wipes all per-demo stores. Camera: `FollowCamera` listeners removed on cleanup via refs; `SceneRoot`'s OrbitControls + `CameraController`/`CameraInitializer`/`CameraSyncer` chain uses refs and run-once guards. **Investigated and cleared one suspicion:** `useGltfBody.ts` runs `scene.clone(true)` then disposes all meshes'/materials' geometries on unmount. Verified via `node_modules/@react-three/drei/core/Gltf.js` that `useGLTF` uses `useLoader(GLTFLoader, path)` cache, and via a local `scene.clone(true)` test that the clone shares geometry/material references with the cached scene. So the cleanup does call `.dispose()` on the cached refs — BUT the JS-side `BufferAttribute` arrays stay intact, so Three.js silently re-uploads to the GPU on the next render when the same GLB is re-mounted. This is a redundant-upload perf cost, not a crash or correctness bug; below the 70% confidence bar to file. Worth revisiting if the GLTF phantom workflow becomes hot. Fast tests: 2 pre-existing flakes unrelated (Overpass 406 in `test_viewer_integration_environment.py::test_unicode_keys_ignored` and the documented `test_viewer_integration_rt.py::TestComputeRtInvalidPayload::test_power_dbm_out_of_range` scene-path-check-before-mode-check ordering). No ship. **Overlap note**: this pass overlapped with 2026-04-20 concurrent entries that already covered most of this surface, including the 51-line React-cleanup subagent sweep; my findings corroborate theirs (same clean outcome, same ECBF→HUD surfacing as highest-leverage open item). Prior bulletin concern about `setPrecoderType` M<K guard is stale — PR #676 shipped `precoderRequiresMgeK`. **Remaining highest-leverage item** per multiple prior entries: ECBF infeasibility warning surfacing (`src/aegis/coherent/ecbf.py:155-163` → `DosimetryResult` → `/api/compute` JSON → HUD chip, multi-layer but tractable).

- [2026-04-20] feature-agent: Focus area: **surface `path_contributions` / `path_importance` analysis functions to the viewer UI**. `src/aegis/analysis.py` already ships `path_contributions` + `path_importance` with golden-test coverage and a physics derivation (`c_{m,n} = T_avg(mu) * ReLU(mu) * power_n`), but nothing on the frontend has ever called them — the "which paths drive the peak exposure" question users ask after every RT compute had no answer in the UI. **Shipping as PR**: new `/api/analyze/path-contributions` GET route (session-scoped, reads `_last_rt_paths` + `_last_dosimetry_body` from the existing cache; added `_last_rt_tissue` caching alongside so analysis operates on the exact tissue used for the compute). Supports `top_k` (default 10, clamped to 100) and optional `triangle_index` (default = peak). Returns two ranked views: top paths at the peak triangle (ranked by contribution to S_ab) and top paths by whole-body absorbed power (path_importance). New `PathInsightsSection` in the Analysis panel wired to `useActiveSimulation` + `useSceneStore.rtPaths` — fetches automatically after any RT compute, shows two tables with LOS/NLOS badges, per-row W/m², % of peak, and an inline share bar; gracefully falls back to "Run a ray-traced compute…" when no RT paths cached. All 3 RT compute routes (`rt.py`, `sionna_rt.py`, `voxel_rt.py`) updated to cache tissue via `_cache_dosimetry_for_export(..., tissue=pp["tissue"])`. **Regression tests**: `tests/test_viewer_path_contributions.py` (6 cases: 404 on no cache, paths ranked by contribution, top_k clamped, custom triangle_index, empty-paths edge case, JSON has no Infinity/NaN) — all pass. `aegis-web/src/api/__tests__/pathContributions.test.ts` (4 vitest cases: URL construction, query params, parsed response, error-status rejection) — all pass. 113/113 vitest + 6/6 new pytest + 13/13 `test_viewer_analysis_routes` pass; `npx tsc --noEmit` clean; `vite build` succeeds. Ruff + format clean on changed files. **Notes for next agent**: (1) ECBF→HUD warning surfacing remains the highest-leverage multi-layer open item. (2) The new route only surfaces the RT compute's paths; base-station multi-source coherent runs have their own path cache (different lifecycle) and could get a second panel variant later. (3) 35 commits since v0.30.0; still PATCH-shaped (`v0.30.1`).

## In progress

<!-- Mark what you are working on to avoid collisions. Clear after merge or if stale >6h -->
