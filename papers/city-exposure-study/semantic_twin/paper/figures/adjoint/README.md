# Adjoint estimator schematic

`adjoint.py` draws the forward and receiver-side sampling patterns used to
explain the first-diffuse estimator. The diagram is schematic and contains no
result data. Run it with the paper environment:

```bash
uv run --project semantic_twin --extra paper python \
  semantic_twin/paper/figures/adjoint/adjoint.py
```
