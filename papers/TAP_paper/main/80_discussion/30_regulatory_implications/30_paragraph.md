% PREV: The ICNIRP general-public reference level above $6$~GHz is
% PREV: $10$~W/m$^2$~\cite{ICNIRP2020}. Reference levels are the
% PREV: operationally measured incident-power-density limits intended to
% PREV: imply compliance with the underlying basic restriction, here
% PREV: $0.08$~W/kg whole-body SAR. Setting $\IPD_{\mathrm{max}} =
% PREV: 10$~W/m$^2$ in~\eqref{eq:Sinc-max-worst} returns the threshold
% PREV: $m/A \geq 33.9$~kg/m$^2$ at $\Tbar = 0.543$ ($28$~GHz on skin).
% PREV: \Cref{tab:anthro} lists $\IPD_{\mathrm{max}}$ values of $6.2$,
% PREV: $7.6$, and $9.9$~W/m$^2$ for the infant, the six-year-old child, and
% PREV: the adolescent under the worst-case directional bound
% PREV: $D \le 2A_{\mathrm{CH}}/\Aab$. The reference level exceeds these
% PREV: thresholds by $61\%$, $32\%$, and $1\%$ respectively.
The closed form gives a closed-form certificate that the existing
reference level fails the basic restriction for the three smaller
body sizes under worst-case directional exposure. Under realistic
plane-wave or multipath exposure the directivity is below this
worst case, and the basic restriction is met~\cite{ICNIRP2020}. The
closed form makes both the worst-case and the directional-average
evaluation explicit.

## reviews (paragraph)



_PaperMaker9000 sweep — 3 flag(s) across 4 lens(es)._

- **sentence-craft** — 2 flag(s), 12 cleared:
    - `style.prose_structure.action_in_verb` (high): `The closed form gives a closed-form certificate that the existing reference level fails the basic restriction` -> Recover the verb and drop the echoing adjective: "The closed form certifies that the existing reference level fails the basic restriction".
    - `style.positive_voice.no_passive_no_we` (high): `the basic restriction is met~\cite{ICNIRP2020}` -> Make the actor the subject: "...and exposure meets the basic restriction~\cite{ICNIRP2020}".
- **voice-tells** — pass (22 rules cleared).
- **lexical-spotcheck** — 1 flag(s), 53 cleared:
    - `BOOK_WILLIAMS_style.prose_structure.comma_after_long_intro` (unknown): `Under realistic plane-wave or multipath exposure the directivity is below this worst case` -> Add a comma after the six-word introductory phrase: 'Under realistic plane-wave or multipath exposure, the directivity is below this worst case'.
- **latex-micro** — pass (41 rules cleared).
- **incremental (2026-05-22)** — pass (2 new/edited rules cleared).

