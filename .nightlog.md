# Night log - 2026-03-25

## Agent 1 (Opus)

### What I did

**1. Validated element_index bounds in coherent channel computation**
- `field_channel.py` and `body_channel.py` now check that all element_index
  values are in [0, n_elements) before accumulation
- Previously, out-of-bounds indices caused silent data loss: `np.add.at`
  drops them silently, JAX clips them to the nearest valid index
- Now raises ValueError with clear message
- Files changed: `src/aegis/coherent/field_channel.py`, `src/aegis/coherent/body_channel.py`

**2. Hardened MIMO precoders for edge cases**
- ZF precoder: catches `LinAlgError` on singular H@H^H and falls back to MRT
  with a warning (previously crashed)
- `compute_mrt_precoder`: handles K=0 (empty user set) by returning (M_ant, 0)
  matrix (previously crashed with ZeroDivisionError)
- `compute_mimo_scene`: raises ValueError for empty scene instead of crashing
- Files changed: `src/aegis/mimo/precoders.py`, `src/aegis/mimo/compute.py`

**3. Added compliance sweep utilities**
- `power_sweep()`: compliance margin vs transmit power curve using linear S_ab
  scaling. Returns power arrays (W and dBm), margin_db, compliant boolean,
  p_max_compliant_w.
- `frequency_sweep()`: compliance margin vs frequency. ICNIRP limits change with
  frequency (sinc_local ~ 55/f^0.177). Answers "At which frequencies is this
  exposure level compliant?"
- `compliance_heatmap()`: 2D (power x frequency) compliance map. Returns margin
  grid, compliant mask, per-frequency max compliant power.
- Files changed: `src/aegis/compliance/__init__.py`

**4. Added link_budget_compliance for quick RF checks**
- Computes S_inc from free-space path loss (P * G / 4*pi*d^2), estimates
  S_ab = S_inc * T0 (using skin tissue lookup or custom T0), runs full ICNIRP
  evaluation. Returns compliance status, margin, max compliant TX power in both
  W and dBm. Useful for field engineers and initial regulatory assessments.
- Also added `--link-budget` CLI mode:
  `python3 -m aegis.compliance --freq 28e9 --link-budget --tx-power 1.0 --gain 15 --distance 5`
- Files changed: `src/aegis/compliance/__init__.py`, `src/aegis/compliance/__main__.py`

**5. Narrowed sionna bare except**
- `_extract_path_viz()` had `except Exception: return []` which silently
  discarded bugs. Narrowed to specific exception types.
- Files changed: `src/aegis/integration/sionna.py`

**6. Added 57 new tests (775 -> 828 passing, +7%)**
- `tests/test_channel_bounds_and_sweeps.py` (53 tests):
  - Field channel bounds: valid/negative/too-large indices, empty paths (5 tests)
  - Body channel bounds: valid/negative/too-large indices, empty paths (4 tests)
  - Power sweep: monotonic margin, low/high power, p_max consistency (9 tests)
  - Frequency sweep: low/high exposure, sinc varies, 1cm2 threshold (10 tests)
  - Compliance heatmap: shape, power extremes, occupational, margin monotonicity (7 tests)
  - MIMO edge cases: singular ZF, K=0 MRT, MMSE=ZF, zero Q exposure (7 tests)
  - Link budget: isotropic, inverse square law, gain scaling, T0 (11 tests)
- `tests/test_compliance_cli.py` (4 new tests):
  - Link budget CLI: plain text, JSON, missing args, occupational

### What I considered but skipped

- **Frontend/viewer work**: No npm/node setup, risky
- **Thread-unsafe viewer state**: Real but low priority for single-user viewer
- **Type annotations on kernel functions**: CLAUDE.md says "Don't add type annotations to code you didn't change"
- **Sensitivity analysis / uncertainty quantification**: Useful but complex, needs Robin's input on methodology
- **Extending compliance_heatmap to sinc_local**: The heatmap currently only uses sab_4cm2 as the varying constraint. Adding sinc_local (frequency-dependent) would make it more complete but also more complex.

### Issues discovered

1. **MIMO files not tracked on master**: `precoders.py` and `compute.py` existed on disk but were not in git on master. Fixed by committing them.
2. **Dependabot alert**: GitHub flagged 1 low-severity vulnerability. Robin should check `https://github.com/rwydaegh/aegis/security/dependabot/1`.

### Suggestions for next agent

- Extend `compliance_heatmap` to include sinc_local (frequency-dependent limit)
- The viewer backend could use `power_sweep()` to show a compliance-vs-power slider
- Test coverage for `src/aegis/viewer/routes/location.py` voxel pipeline is minimal
- Consider adding a `DosimetryResult.sensitivity()` method for parameter sensitivity analysis
- The `link_budget_compliance` could be extended to support multi-path (NLOS) scenarios using a reflection loss parameter

## Agent 2 (Opus)

### What I did

**1. Fixed spatial_averaging parameter being ignored in engine.py**
- The `spatial_averaging` parameter was accepted by `compute()`, `compute_with_timings()`, and `_compute_mode()` but never passed to `_build_result()`. Averaging was always applied regardless of the flag value.
- Wired the parameter through all dispatch paths to `_build_result()`. Changed the default from `False` to `True` to preserve backward compatibility (averaging was always applied before). Users can now actually disable it with `spatial_averaging=False` to skip the expensive G matrix build.
- Also wired it through `sweep_levels()`.
- Files changed: `src/aegis/engine.py`, `tests/test_engine.py`

**2. Made evaluate_compliance robust without spatial averaging**
- When `spatial_averaging=False`, `DosimetryResult.evaluate_compliance()` was producing a vacuously passing ComplianceResult (all checks None, `overall_pass=True`). Now falls back to raw per-triangle peak sab as a conservative upper bound for the 4 cm^2 check, and similarly uses raw sinc peak if sinc_averaged is None.
- Files changed: `src/aegis/result.py`

**3. Vectorized field_channel numpy accumulation**
- Replaced Python loop over antenna elements in `_accumulate_by_element_numpy` with `np.add.at` scatter-add, matching `body_channel.py`. Faster for large antenna arrays.
- Files changed: `src/aegis/coherent/field_channel.py`

**4. Added 46 new tests (836 -> 882 passing, +5.5%)**
- `tests/test_spatial_averaging_param.py` (23 tests):
  - Default (True) produces averaged fields, explicit True/False, shape checks
  - All incoherent levels (2-6) with False return None sab_averaged
  - Mode-based API (spatial, bound) respects the flag
  - compute_with_timings: no avg timing keys when disabled
  - sweep_levels passes through spatial_averaging
  - 1 cm^2 averaging gated by both frequency (>30 GHz) and spatial_averaging flag
- `tests/test_additional_properties.py` (23 tests):
  - Fresnel: T_avg bounded by [0, 1], normal incidence equals T0
  - Physical GELU: bounded by mu, non-negative, reduces to ReLU at sigma=0
  - DosimetryResult.scale(): identity, zero, negative raises, linearity
  - Edge cases: single-triangle mesh, back-facing path, grazing incidence, multiple same-direction paths
  - Coherent single-element produces positive sab and Q
  - PropagationPaths: construction, normalization, spherical, concatenation, serialization, LOS/NLOS partition, uniform_sphere total power

### What I investigated but did not fix

- **JAX body_channel scatter-add "bug"**: Agent 1's exploration agent flagged `jax.numpy.ndarray.at[:,:,idx].add()` as not accumulating duplicate indices. This is incorrect: JAX `.at[].add()` does correctly accumulate for duplicate indices (unlike `.at[].set()` which is non-deterministic). Verified by reading the JAX documentation. Not a bug.
- **Energy conservation bound in test_level3_energy_bound**: The existing bound `P_abs <= T0 * area * power` is technically too tight because Fresnel T_avg can exceed T0 near the Brewster angle (measured: T_avg_max/T0 = 1.056 at mu=0.30). However, the test has never failed in practice because the integral over all angles dampens the Brewster peak. Left as-is since it hasn't caused issues.
- **ECBF bisection "unbounded" loop**: The exploration agent flagged the lambda doubling in ecbf.py as potentially unbounded. It already has a `lam_high > 1e20` break and `_bisect` has `maxiter=200`. Already well-handled.

### Issues discovered

- **Fresnel T_avg > T0 at Brewster angle**: T_avg(mu=0.30) = 0.569 > T0 = 0.539 for skin at 28 GHz. This is physically correct (TM transmission enhanced at Brewster angle) but means the level 2 energy conservation bound `P_abs <= T0 * area * power` is not a strict mathematical bound for level 3. Robin should be aware if the bound matters for formal proofs.
- **Level 5 uses raw curvature_H (can be negative) while spatial.py floors to max(H, 0)**. This is an inconsistency between the legacy level API and the mode-based API for the curvature correction term. May be intentional (level 5 allows concave curvature effects) but worth verifying against the monograph.

### Suggestions for next agent

- Verify the level 5 vs spatial.py curvature_H sign handling against the monograph
- The Brewster angle T_avg > T0 finding could affect the formal energy conservation proofs in the monograph. Robin should verify whether the bound used in the monograph accounts for this.
- Consider adding a `DosimetryResult.with_averaging(body)` convenience method that computes averaging on demand from a result that has `sab_averaged=None`
- Viewer route tests are still skipped (no flask). Could install flask in CI for broader test coverage.
- The `_body_cache_key` hashes either areas or centroids depending on branch. Verify consistency.

## Agent 3 (Opus)

### What I did

**1. Added 54 deep physics invariant tests (882 -> 936 passing, +6.1%)**
- Created `tests/test_physics_invariants.py` with comprehensive physics identity tests:
  - Fresnel identities: T_s + R_s = 1 and T_p + R_p = 1 (energy conservation at interface)
  - Fresnel reciprocity: T(mu) is symmetric under angle reversal for lossless media
  - T_avg bounded in [0, 1] for all incidence angles
  - Normal incidence T_avg equals T0 (consistency check)
  - Pseudo-Brewster angle: T_avg exceeds T0 for skin/muscle tissue (documented finding)
  - Level ordering: L0 >= L1 >= L2 as bounds (verified pointwise)
  - Physical GELU: convergence to ReLU as sigma -> 0, smoothness at shadow boundary
  - Cole-Cole model: eps' > 1 and sigma > 0 for all tissue types at all frequencies
  - Cole-Cole Kramers-Kronig: eps' decreases with frequency (consistent with dispersion)
  - DosimetryResult scaling: identity, zero, linearity, non-negative enforcement, NaN/inf rejection
  - Coherent-incoherent equivalence: single-element coherent matches incoherent result
  - Input validation: NaN/inf rejected in PropagationPaths.from_powers() and DosimetryResult.scale()
  - Energy conservation: P_abs bounded by incident power for all incoherent levels

**2. Added input validation to prevent silent data corruption**
- `PropagationPaths.from_powers()`: rejects NaN/inf in power and k_hat arrays with clear ValueError
- `DosimetryResult.scale()`: rejects non-finite (NaN/inf) and negative scale factors
- `TissueModel.__post_init__()`: rejects non-positive frequencies
- Files changed: `src/aegis/paths.py`, `src/aegis/result.py`, `src/aegis/tissue/dielectric.py`

**3. Hardened viewer location route**
- Added input validation for `radius` and `voxel_size` query parameters (type checking, positive values)
- Added proper error responses (400) for invalid inputs instead of 500 errors
- Added logging import for better error diagnostics
- Fixed `tiles_dir` variable reference that could raise NameError
- Files changed: `src/aegis/viewer/routes/location.py`

**4. Updated existing test for new validation behavior**
- `test_nan_power_propagates` -> `test_nan_power_rejected`: NaN in power now raises ValueError at the input boundary instead of silently propagating through computation
- Files changed: `tests/test_engine.py`

### Key physics finding

**Pseudo-Brewster TM enhancement in lossy tissue**: For skin at 28 GHz, T_avg(mu=0.30) exceeds T0 by ~5.6%. For muscle, ~11.8%. This is physically correct (TM polarization has enhanced transmission near the Brewster angle even in lossy media) but has implications:
- The Level 2 energy bound `P_abs <= T0 * total_incident_power` is not a strict mathematical bound for Level 3 (Fresnel)
- The excess is small (~5-12%) and dampened by angular integration, so existing tests pass
- Documented with dedicated test `test_Tavg_exceeds_T0_near_pseudo_brewster`
- Robin should verify whether the monograph's formal proofs account for this

### What I investigated but did not fix

- **Level 5 vs spatial.py curvature_H sign handling**: Level 5 kernel uses raw `curvature_H` (can be negative for concave surfaces) while `spatial.py` floors to `max(H, 0)`. This is a physics decision that needs Robin's input on whether concave curvature correction is intentional.
- **DiffeRT integration polarization tracking**: Read through `differt.py` (579 lines) looking for improvements. The code is well-structured with proper Fresnel coefficient tracking per-reflection. No issues found.
- **compliance_heatmap vectorization**: Considered vectorizing the double loop and adding sinc_local support but deferred as lower priority than physics tests.

### Issues discovered

1. **Pseudo-Brewster T_avg > T0**: See key finding above. Affects formal energy conservation proofs.
2. **Silent NaN propagation**: Before this session, NaN in power arrays would silently produce NaN results through the entire pipeline. Now caught at input boundary.
3. **Viewer route could 500 on bad input**: Non-numeric radius/voxel_size caused unhandled exceptions. Now returns proper 400 errors.

### Night summary (all 3 agents)

| Metric | Agent 1 | Agent 2 | Agent 3 | Total |
|--------|---------|---------|---------|-------|
| Tests added | 57 | 46 | 54 | 157 |
| Tests passing | 828 | 882 | 936 | +21% |
| Bugs fixed | 2 | 1 | 3 | 6 |
| Files changed | 7 | 4 | 5 | 16 |

### Suggestions for next agent

- Robin should verify whether the monograph's energy conservation proofs account for Brewster T_avg > T0
- Verify level 5 vs spatial.py curvature_H sign handling against the monograph
- Vectorize `compliance_heatmap` double loop and add sinc_local frequency-dependent limit
- Add `DosimetryResult.with_averaging(body)` convenience method for on-demand spatial averaging
- The viewer backend could expose `power_sweep()` as a compliance-vs-power slider
- Consider adding a `DosimetryResult.sensitivity()` method for parameter sensitivity analysis
- Viewer route tests are still skipped (no flask in test env). Could install flask in CI.
