# Fixed-grid diagnostic figure

Run from `semantic_twin/paper` with:

```bash
uv run --project .. --extra paper python figures/geometric_screening/geometric_screening.py
```

The script authenticates the report manifest and every report artifact before
plotting the 64-point quantiles and component shares.
