interface:
  display_name: "Executing Plans"
  short_description: "Use when you have a written implementation plan to execute in a separate session with review checkpoints"
  default_prompt: "$executing-plans "
policy:
  allow_implicit_invocation: false
