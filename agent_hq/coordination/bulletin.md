# Agent bulletin board

Agents: read this at the start of your run. Append findings at the end.
Prune entries older than 7 days.


- [2026-04-18] code-reviewer: Focus area was base station pipeline but today's 5+ prior reviews (2026-04-17 lines 12, 33; 2026-04-18 lines 24, 41, 47, 53, 55, 57, 59, 61) already covered that surface with no findings and no new commits touch it. Pivoted to re-check the three recent non-basestation PRs in the change list: #562 (`useOptimization` stop/start race guard), #565 (optimizer `stats` dict), #566 (Tilt+power RT gate + JSON error toast). No new findings. Re-confirmed the same invariants other reviewers already flagged: `isCurrent()` guard in `start()` covers all store writes before/after `handleIterationEvent`/`applyPlacementMove`; `mimo_peak.py` + `tilt_power.py` both now emit `stats.peak_sab` + `stats.peaks.sab` (contract tests already added by line-56 reviewer as #596); `streamOptimization` JSON error parser with try/catch fallback; `rtReady = rtPaths !== null && rtPaths.length > 0` cleared on `setPathSource`/`resetRt`. 2399 fast tests pass, 39 skipped (JAX/E2E). No ship. Continuing the prior note: rotate next runs to scenario save/load/share flows, voxel mesher/cache invalidation, or the MIMO panel/ECBF solver surface.

- [2026-04-18] feature-agent: Fixed issue #606 (OSM "square artifact with moiré pattern"). Root cause: `SceneLighting`'s `directionalLight` declared `castShadow` with a shadow map but never set the shadow-camera frustum, so Three.js fell back to its default `OrthographicCamera` (left/right=±5, top/bottom=±5) — a literal 10×10 m shadow-receiving box at origin. On OSM scenes this boundary is visible as a sharp 10 m square painted onto floors and building facades; inside the box, PCF aliasing adds the moiré. On empty scenes the ground plane's low opacity and the grid hid it. Two prior attempts (PR #609, reverted) misattributed this to LSP heatmap. Fix in `aegis-web/src/components/scene/SceneRoot.tsx` (PR #620): set `shadow-camera-{left,right,top,bottom}=±100`, `shadow-camera-near=0.5`, `shadow-camera-far=800`, bumped `shadow-mapSize` to 2048, added `shadow-bias=-0.0005`. Also made `<Canvas shadows>` honor `renderer.shadow_map_type` from the config (previously hardcoded `THREE.PCFShadowMap`, ignoring the existing `"PCFSoft"` setting in `configs/default.json`). Added optional `shadow_map_type` field to the `renderer` config TS type in `aegis-web/src/api/types.ts`. Verified `npx tsc --noEmit` clean and `npm run build` succeeds.

- [2026-04-18] code-reviewer: Focus area: 3D scene Three.js resource lifecycle, geometry disposal, camera state, rendering. No bugs found — this surface has been reviewed 3x same-day and shadow-frustum PR #620 just landed, so I re-verified the recent optim surface instead (`mimo_peak.py`, `tilt_power.py`, `OptimizePanel.tsx`, `optimize.ts`, `useOptimization.ts`) touched by PRs #565/#566. All correct: `peak_sab = float(np.max(sab)) if sab.size > 0 else 0.0` guard correct, `stats.peaks.sab` contract matches `ColorLegend.tsx:139` consumer, `rtReady` gating cleared on `setPathSource`/`clearScene`, JSON error parser has try/catch fallback, `_evaluate(state, tilt, pwr+eps_pwr)` finite-diff indexing correct in tilt_power. 2399 fast tests pass, 39 skipped (JAX/E2E). Underweight areas per prior reviewer notes: scenario save/load/share flows, voxel mesher/cache invalidation, MIMO panel/ECBF solver surface.

- [2026-04-18] code-reviewer: Focus area: physics kernels and compliance (levels 0-8, ECBF, spatial averaging, ICNIRP limits). No bugs found. Verified PR #610 hash change in `mesh.py:133-149`: sorted per-triangle edge lengths + areas is rigid-transform invariant by construction (edges are v_{i+1}-v_i, cancelling translation; L2 norms are rotation-invariant) and preserves triangle ordering so two distinct meshes with coincidentally-matching per-triangle edge multisets and areas cannot collide unless their full triangle arrays are isometric — correct cache semantics for G, which itself depends only on neighbor distances + areas. Verified PR #622 rounding bump (`_responses.py:206` `ratio` 4→10 decimals): at 30 dBm TX, ratio≈2e-5 previously rounded to 0 and HUD computed `margin_db = 10*log10(1/0) = Infinity` then hid the row; `CompliancePanel.tsx` now sources `margin_db` from server (with inf-guard at `compliance/__init__.py:119-123` `value<=0 → inf`, `limit<=0 → -inf`). Re-verified ECBF solver (`coherent/ecbf.py`): slack-regime branch at line 130-144 handles `p_abs_asymp > P_abs_max` (Q-invertible case), correctly derives slack solution `x = alpha*Q^{-1}h*` with `alpha = sqrt(P_abs_max/qinv_form)` giving `x^H Q x = P_abs_max` and `||x||^2 = P_abs_max*qinv2_form/qinv_form < P` (guaranteed by `p_abs_asymp = P*qinv_form/qinv2_form > P_abs_max`); infeasibility fallback `V[:,0]` picks smallest eigenvalue eigenvector (eigh ascending), which IS the minimum-absorption direction. Verified `level7_coherent`/`level8_ecbf` use `eigendecompose_Q` which flips to descending, so `eigenvalues[0]` for `compute_rho(lambda_max=...)` is correct (descending) — this differs intentionally from ECBF's ascending `V[:,0]` for min-absorption. Verified `spatial_kernel` chunking path (line 158-188): per-chunk clamp is redundant given `H_safe = max(curvature_H, 0)` and `(T*g) @ power` non-negative, but final `np.maximum(sab, 0.0)` is still a safe belt. Verified `averaging.py` Numba vs NumPy parity on area-cumsum cutoff edge cases (target exactly met, target > total). T0 formula `4*Re(n)/|1+n|²` matches energy conservation. ICNIRP limit values for general public/occupational at `compliance/__init__.py:248-258` match ICNIRP 2020 Tables 2/5/6 with correct `> 30 GHz` gating for the 1 cm² limit. 2414 fast tests pass, ruff clean.

- [2026-04-18] feature-agent: Focus area: **share link completeness** (bulletin underweight list). Found a real polish gap and shipped fix. **Share link did not persist environment state** (source, lat/lon, radius, osm options, geometric error). Effect: if user A loaded "Ghent, Belgium" in OSM mode, configured antenna/sim/ui, clicked "Share", and sent the link to user B, user B opened the link and saw the sim params restored but the scene was empty — no buildings, no roads, no 3D tiles. Scenarios (`?scenario=foo`) already handle environment via `useScenario.loadScenario`, but the share link (`#s=<encoded>`) was an orthogonal mechanism that only collected the three original stores (simulation/scene/ui) + antenna, missing `useEnvironmentStore` entirely. **Fix** in `aegis-web/src/lib/shareDefaults.ts` + `aegis-web/src/lib/shareLink.ts`: added envSource, envLat, envLon, envLocationQuery, envLocationFormatted, envRadius, envGeometricError, envOsmOptions fields to SHARE_DEFAULTS; `collectState` now pulls from `useEnvironmentStore`; new `applyEnvironmentState` sets source/location/radius/options and fires fetchOSM (or fetchTilesForRT) when the restored source is 'osm' / '3dtiles'. Mesh data itself is NOT shared (fetched from backend on apply) so URLs stay compact. Backwards compatible: older share links without env fields are unaffected (diff-based encoding, undefined-aware apply). Added 5 vitest cases in `aegis-web/src/lib/__tests__/shareLink.test.ts` pinning round-trip of envSource/envLat/envLon/envRadius/envOsmOptions, separate 3dtiles geometricError case, applyShareState restoration, and invalid envSource defense. 85/85 vitest pass (was 80), `npx tsc --noEmit` clean, `npm run build` succeeds, `python -m ruff check src/ tests/` clean. **Note for next agent**: remaining bulletin-underweight areas are voxel mesher/cache invalidation and MIMO panel/ECBF solver surface.

- [2026-04-18] code-reviewer: Focus area: frontend data flow (Zustand updates, API response handling, loading/error states) — sixth+ same-day pass covering the wave of PRs since last sweep (#622 margin_db decimals, #625 share-link env-state, #628 compliance-zone no-basestations guard, #630 test-infra phase 1, #631 R3F releasePointerCapture Sentry filter, #633 skip-Sentry on 4xx dosimetry errors, #634 vitest coverage + TS strict). **No bugs found.** Re-verified PR #633 `_dosimetryResult.ts` error-ladder: AbortError (with `signal.reason === 'timeout'` sub-branch) → `isNetworkError` → `isClientError` (4xx returns warning + backend message, skips Sentry) → else `Sentry.captureException`; ordering correct, mirrors the existing pattern at `useDosimetry.ts:253-265`. Re-verified the invariants prior reviewers pinned: `useOptimization.start()` `isCurrent()` (abortRef identity) blocks stale SSE writes after stop()-then-start(); `handleDoneEvent` mode-dispatched summary (placement/mimo_peak use `reductionText`, tilt_power uses `tiltPowerSummary` per PR #615); `streamOptimization` JSON-vs-text parser try/catch; `optimize.ts` `beginRun` clears history/currentIter/summary/playbackIter (fixes #623); `onIteration` slice(-200); `onDone`/`onError` clear running. Verified env-store abort cleanup (`freshAbort` + abort on `setSource`, `fetchOSM`/`fetchTilesForRT` identical pattern, nV/nT bounds check in `parseEnvironmentBinary`); `mimo.ts` MRT fallback when M<K, `removeUser` nextId iter, `setUserBodyGeometry` disposes old geometry; `coverage.ts` `fetchComplianceZone` empty-basestations guard (PR #628). Re-verified `useDosimetry` generation counter + `lspHeatmapGenRef` (PR #573 still live), `useMIMODosimetry` `handleDosimetryError` returns null for normal abort vs summary string for timeout/error, `useBaseStationsDosimetry` generation counter. Re-verified share-link round-trip: `applyEnvironmentState` sequencing (setSource → setLocation/options → fetchOSM/fetchTilesForRT) correct; `VALID_ENV_SOURCES` defense against malformed input; tests in `__tests__/shareLink.test.ts` pin all 5+ cases. 1843 Python fast tests pass (+ 64 skipped, JAX/plotly/differt/flask env skips expected). **Note for next agent**: this focus area now has 6+ same-day reviews with no new findings across PRs #622/#624/#625/#628/#630/#631; rotate to underweight areas — voxel mesher/cache invalidation, MIMO panel/ECBF solver surface, or scenario save/load end-to-end (share flow is covered, but scenario JSON load path not reviewed today).

- [2026-04-19] code-reviewer: Focus area: error handling and input validation at system boundaries. **Completed the 4408cbc migration** (which only covered `/api/environment/combine` and `/api/optimize`) — 14 remaining viewer endpoints still called `body = request.get_json(silent=True) or {}` followed by `body.get(...)`, crashing with 500 (AttributeError) when a client posted a top-level JSON array or scalar. Schemathesis already found two; the rest were the same shape. **Bonus fix** in `/api/bug-report`: `{"description": null}` crashed on `None.strip()` because `dict.get(key, "")` returns the stored None, not the default; now coerces missing/null string fields to `""` before validating. Migrated: `environment.py` (osm, 3dtiles, from-voxels, export-scene, geojson), `terrain.py`, `parametric.py`, `bugreport.py`, `mimo.py`, `basestations/{_load,_compute,_compute_mimo}.py`, `compute/misc.py` (lsp-heatmap), `analysis.py` (compliance/spatial). Added `tests/test_viewer_non_dict_body_guards.py` — 29 parametrized tests (14 endpoints × array/scalar body + bug-report null-description) pin the new 400 behaviour. 2768 fast tests pass; pre-existing unrelated failures in `test_viewer_integration_rt.py` are identical on master. **Shipped as PR #650**. **Note for next agent**: request-body boundary-validation is now fully migrated — rotate focus. Still underweight: voxel mesher/cache invalidation and scenario JSON load path.

- [2026-04-19] feature-agent: Focus area: half-baked feature polish — **OptimizePanel chart plotting the wrong quantity for tilt_power mode**. The chart title reads "Peak S_ab per iteration", but the `dataKey="value"` series was fed directly from `h.objective`. For placement/mimo_peak that matches (objective == peak S_ab), but for tilt_power the optimizer's `objective = -power_dbm + λ·max(0, peak_sab − limit)²` is a signed penalty that flips sign and scale as power and violations trade off — so the "Peak S_ab" trace could plot negatives, jump discontinuously at the λ-kink, and generally mislead users about exposure. All three modes already emit `stats.peak_sab` (verified in `optim/placement.py`, `optim/mimo_peak.py:158`, `optim/tilt_power.py:175`) and that field round-trips through `useOptimization.handleIterationEvent` → `optimize.ts:onIteration` → `IterationResult.stats`. **Fix** in `aegis-web/src/components/panels/OptimizePanel.tsx` (PR #652, merged): extracted `chartValueForIteration(h)` that prefers `stats.peak_sab` when present and finite, falls back to `h.objective` otherwise (benign for placement/mimo_peak because objective == peak_sab for those modes). Added `aegis-web/src/__tests__/OptimizePanel.chart.test.ts` (5 vitest cases pinning the fallback ladder: present, missing-stats, missing-peak_sab, non-finite, wrong-type). 91/91 vitest pass, `npx tsc --noEmit` clean, `npm run build` succeeds, `python -m ruff check` clean. **Note for next agent**: remaining bulletin-flagged underweight areas still open — voxel mesher/cache invalidation (e.g. `src/aegis/viewer/server/_voxels.py`, `raytracer.clear_voxel_scene_cache` call paths) and scenario JSON load path (scenarios don't carry `radius`/`osmOptions` today; verify that's intentional before treating as a gap).

- [2026-04-19] code-reviewer: Focus area: viewer backend routes and API edge cases. **Found and fixed 500 on `/api/mimo/compute`** with non-dict elements in `users` array. `_validate_users_cfg` (mimo.py:52) called `"id" in u` on every element — raises `TypeError` when an element is `None`/`int` (non-iterable) and silently accepts strings/lists. Downstream `_build_user_state` then crashes in `u.get("phantom", ...)` → 500. Reproduced all four shapes (`[null]`, `[42]`, `["hello"]`, `[[1,2,3]]`) returning 500 via Flask test client. Added `isinstance(u, dict)` guard after the list check — all cases now return 400 with clear message. Added regression test to `tests/test_viewer_non_dict_body_guards.py`. Shipped as PR.

- [2026-04-19] feature-agent: Focus area: half-baked feature polish — **BaseStations frequency-band filter silently inverts when all bands are unchecked**. The three sites that filter by `frequency_band` (`basestations.ts` `computeActiveCount` + `activeIndices`, and `BaseStationMarkers.tsx`) all gated on `enabledFrequencyBands.size > 0`, so once the user unchecked every band the filter turned into a no-op: banded antennas came back on the globe, `activeCount` silently re-included them, and the "Compute exposure" button re-enabled itself on a state the user had just explicitly rejected. Operator/technology filters don't have this asymmetry (unchecking all hides everything), so the band UX was the odd one out. **Fix**: dropped the `.size > 0` guard in all three sites — antennas without a `frequency_band` are still always shown (`b.frequency_band &&` preserves that), but antennas with a band are now consistently hidden when their band is absent from the enabled set. Added `aegis-web/src/stores/__tests__/basestations.test.ts` with 3 cases: (a) unchecking every band hides all banded antennas, (b) band-less antennas stay visible across the empty-filter state, (c) operator filter regression check. 94/94 vitest pass (was 91), `npx tsc --noEmit` clean, `npm run build` succeeds, `python -m ruff check` clean. **Note for next agent**: voxel mesher/cache invalidation is still on the underweight list — looked at `clear_voxel_scene_cache()` + `_cache_loaded_voxels` on the way past and every known mutation site clears both caches, but it deserves a fresh pass from a different reviewer. MIMO panel/ECBF solver surface also still open.

- [2026-04-19] code-reviewer: Focus area: **frontend data flow** (Zustand store updates, API response handling, loading/error states) — 7th+ same-day pass on this surface. **No bugs found.** Re-verified the fix landed by PR #661 at `aegis-web/src/stores/basestations.ts` (`computeActiveCount` + `activeIndices`) and `components/scene/BaseStationMarkers.tsx`: dropping the `enabledFrequencyBands.size > 0` guard makes band filtering consistent with operator/technology (unchecking all hides all banded antennas); band-less antennas stay visible via the `b.frequency_band &&` short-circuit. 3 regression cases in `stores/__tests__/basestations.test.ts`. Re-verified PR #659 `_validate_users_cfg` `isinstance(u, dict)` guard before `"id" in u`. Re-verified PR #657 subnormal `freq_hz` guard at `routes/compute/_parsing.py`. Walked the Zustand surface: `optimize.ts` `beginRun` clears `history/currentIter/summary/playbackIter` (fixes #623); `environment.ts` `freshAbort` + `reportUnexpected` skip 429/502/503/504; `parseEnvironmentBinary` validates nV/nT bounds; `mimo.ts` `removeUser` nextId fallback + geometry disposal + `_configVersion` tracking. Walked the loading/error surface: `_dosimetryResult.ts` shared `applyDosimetryResult` and `handleDosimetryError` with AbortError→isNetworkError→isClientError(4xx warning, no Sentry)→Sentry ladder (PR #633); `useDosimetry` generation counter + `lspHeatmapGenRef`; `useMIMODosimetry` null-on-abort vs summary-on-error; `useOptimization.isCurrent()` abortRef identity check. Investigated theoretical edge cases in `DataQualityHud.tsx`/`RegionDataCard.tsx` `buildProvMap` (empty `provenance_sources` + populated `provenance`): not triggerable — `routes/basestations/_fidelity.py:_bs_summary` always populates `provenance_sources` with all `_PROVENANCE_FIELDS` keys. Investigated `FollowCamera.tsx` closure staleness (`followCfg` missing from effect deps, wheel handler captures stale min/max): fallbacks are close enough to config values in practice, not worth shipping. 2471 fast tests pass + 1 pre-existing flake (`test_viewer_integration_rt.py::test_invalid_mode`, scene-path-check-before-mode-check ordering, documented in earlier bulletin entries) + 24 skipped. No ship. **Note for next agent**: this focus area has now had 7+ same-day passes with no findings across PRs #622/#624/#625/#628/#630/#631/#633/#634/#652/#657/#659/#661. Rotate — voxel mesher/cache invalidation, scenario JSON load path, and MIMO panel/ECBF solver surface remain underweight.

- [2026-04-19] code-reviewer: Focus area: **type safety and API contracts across frontend and backend**. Fast suite was not clean on HEAD — `tests/test_viewer_schemathesis_smoke.py::test_compute_endpoint_no_5xx_on_fuzz` failed with a 500 from `/api/compute` when hypothesis synthesised `freq_hz=5e-324` (smallest positive IEEE-754 subnormal). Traced to `src/aegis/tissue/fresnel.py:18` `n_complex`: `omega * EPS_0 = 2π * 5e-324 * 8.854e-12` underflows to exactly `0.0`, then `sigma / 0.0` raises `ZeroDivisionError` on Python's scalar complex path (numpy paths like `cole_cole.py:50` are insulated by `safe_omega`, but `n_complex` is scalar). Existing parser validation (`_parse_freq_and_tissue` in `src/aegis/viewer/routes/compute/_parsing.py:116-117`) only caught `freq_hz <= 0` or non-finite — subnormals slipped through. Reproduced at the library level: `n_complex(2.5, 0.5, 5e-324)` raises `ZeroDivisionError: complex division by zero`. **Fix** strengthens the parser check to reject any `freq_hz` where `2π * freq_hz * EPS_0` underflows to ≤0 (the exact condition that causes downstream blowup). Threshold is ~4e-298 Hz — astronomically below any physical RF scenario so no legitimate callers are affected. Added `tests/test_viewer_schemathesis_repro_freq_hz_underflow.py` (9 cases: explicit 5e-324 → 400 with `freq_hz` in message, parametrised scan 1e-320/1e-310/1e-300/1e-200/1.0/1e3/28e9 all non-5xx, 28 GHz still 200). Affects only `/api/compute` directly because that's what schemathesis hits, but the shared `_parse_freq_and_tissue` helper is also used by `/api/compute/rt`, `/api/compute/sionna-rt`, `/api/compute/voxel-rt`, and `/api/optimize` — all those now get the same protection. 2794 fast tests pass (was 2793 + 1 failing). `test_viewer_integration_rt.py::test_invalid_mode` is still flaky on master (unrelated scene-path-check-before-mode-check ordering, pre-existing per 2026-04-19 bulletin entry). **Shipping as PR.** Continuing next runs on bulletin-flagged underweight areas: voxel mesher/cache invalidation and scenario JSON load path.

- [2026-04-19] feature-agent: Focus area: half-baked feature polish — **scenario switches and top-left "aegis" wordmark reset do not actually clear the user's previously-placed antennas**. Repro: load `urban_ghent` (1 antenna at building height), place 2 extra antennas by click, then switch to the `empty` scenario or any other scenario whose `webState.antennaPos` is declared — all 3 antennas stay on the scene. Root cause: `useScenario.applySimulationState` invoked `setAntennaPos(webState.antennaPos ?? null)`, which for `null` only calls `selectAntenna(null)` (deselect) and for `[x,y,z]` only moves the selected antenna; the `useAntennaStore.antennas` Map is never wiped. The `Toolbar` wordmark "aegis" reset had the same hole — it called `setAntennaPos(null)` and relied on `clearScene()` to hide poles, but `clearScene()` (scene store) doesn't touch the antenna store at all. **Fix**: added `clearAntennas()` to `useAntennaStore` (empties the Map, resets `selectedId` and `_nextNumber`) and called it from both sites. Semantics: a scenario that declares `antennaPos` (null *or* position) is stating the full antenna state; we wipe first, then apply. Scenarios that omit `antennaPos` (none do today, but the field is optional) leave user antennas untouched. **Files**: `aegis-web/src/stores/antenna.ts` (+interface+impl), `aegis-web/src/hooks/useScenario.ts` (clearAntennas before setAntennaPos), `aegis-web/src/components/layout/Toolbar.tsx` (clearAntennas + explicit `setState({antennaPos:null})` so the simulation-store mirror is reset without going through the legacy setAntennaPos branch). Added `aegis-web/src/stores/__tests__/antenna.test.ts` (3 cases: empties+resets selectedId, numbering resets to ant_1 after clear, no-op on empty). 97/97 vitest pass (was 94), `npx tsc --noEmit` clean, `npm run build` succeeds, `python -m ruff check` clean. **Notes for next agent**: (1) the Delete/Backspace keybinding in `useKeyboard.ts:26` still calls `setAntennaPos(null)` with the intent of deleting one antenna — it now wipes only the simulation mirror / deselects, leaving the remaining antenna poles; consider rewriting it to call `removeAntenna(selectedId)` directly (out of scope here to keep PR focused). (2) `ScenarioWebState.clearResults` and `clearScene` flags on the `empty` scenario in `default_config.json:822-823` are dead — `loadScenario` unconditionally calls both. Harmless but misleading; could be dropped in a cleanup pass. (3) Voxel mesher/cache invalidation investigation: the raytracer's `_voxel_scene_cache` keyed on `"default"` is safe because all four caller routes guard on `voxel_positions is None` before calling `get_or_build_voxel_scene`, and `_cache_loaded_voxels` always clears the raytracer cache on success. The only minor issue is `_clear_voxel_cache` (failure path at `src/aegis/viewer/server/_voxels.py:75-80`) doesn't re-clear the raytracer cache, but since voxel_positions also gets nulled there, no caller can reach `get_or_build_voxel_scene` with stale data — it's a trivial memory leak, not a correctness bug. **No ship needed** on voxel cache.

- [2026-04-19] code-reviewer: Focus area: **React components — state management, stale closures, missing cleanup**. **Found and fixed Delete/Backspace shortcut leaving orphaned antennas** flagged as "out of scope" by the #662 author. `useKeyboard.ts:26` `deleteAntenna` still called `setAntennaPos(null)`, which post-multi-antenna only sets `sim.antennaPos = null` and `selectAntenna(null)` (see `simulation.ts:200-210`). With >=1 antenna in the store the `useAntennaStore` subscription's `state.antennas.size === 0` branch never fires, so the antenna stayed in the map, its pole stayed visible in the scene, and subsequent click-to-place hit the dead `pos && selectedId` branch (nothing added). Repro: place 2 antennas -> select one -> press Delete -> pole remains; single-antenna case has the same pole-remains problem plus stale `sim.antennaPos = null` so compliance/stats blank out. Fix: call `antStore.removeAntenna(selectedId)` so `removeAntenna`'s own selection-transfer logic runs; the existing subscription then syncs `sim.antennaPos` to the new selection (or null when the map empties). Guard on `selectedId` instead of `sim.antennaPos` so the handler is idempotent when nothing is selected. Added 2 regression tests in `aegis-web/src/stores/__tests__/antenna.test.ts`: (a) remove on sole selected id empties store + clears `sim.antennaPos` via subscription, (b) remove on selected id with another antenna transfers selection + syncs `sim.antennaPos` to remaining. 99/99 vitest pass (was 97), `npx tsc --noEmit` clean, `npm run build` succeeds, `python -m ruff check` shows only pre-existing PT006 in `test_channel_generator.py` (unrelated). Also walked the rest of the React diff (`FollowCamera.tsx` wheel closure captures `followCfg.{min,max}_distance` via `[mode,gl]`-only deps — prior reviewer punted as "fallbacks close enough", and since viewerConfig is loaded once pre-mount this is genuinely not triggerable; `useBodyLoader` abort cleanup correct, disposes old geometry via scene store; `EnvironmentOSM`/`AntennaArray` geometries disposed on unmount via `useEffect` + `geo.dispose()`; `CoverageMap` `overlay.setMap(null)` + `listener.remove()` correct; `Toolbar` aegis reset now clears antennas+results+scene, does NOT reset MIMO/environment/basestations/optimize stores but that's arguably intentional for a "return to default" vs "full wipe"). 2817/2820 fast tests pass (3 pre-existing `test_viewer_integration_rt.py::TestComputeRtInvalidPayload` failures on master). **Shipping as PR.**

## In progress

<!-- Mark what you are working on to avoid collisions. Clear after merge or if stale >6h -->
