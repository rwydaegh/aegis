% PREV: Fig.~\ref{fig:configuration} shows how the inputs come together at Prague Old
% PREV: Town Square. A 360-degree street image is aligned with a photogrammetric city
% PREV: mesh cropped to a 250~m radius around the route. Image labels are assigned to
% PREV: the visible mesh triangles without changing their geometry. Possible
% PREV: transmitters lie along visible rooflines. Fixed points set the observation
% PREV: positions, and the anatomical body model faces the direction of travel at each
% PREV: point.
% NEXT: With this configuration fixed, Table~\ref{tab:routes} summarizes the study
% NEXT: route in each of the ten cities. Each
% NEXT: route follows a connected street corridor covered by aligned 360-degree street
% NEXT: images. Fixed observation points include positions between image locations, so
% NEXT: the number of images and points can differ. The 163 points are fixed case-study
% NEXT: observations that describe the selected routes. A different route would change
% NEXT: both position and body orientation.
\begin{figure*}[!htb]
  \centering
  \includegraphics[width=\textwidth]{figures/configuration/configuration.pdf}
  \caption{Configuration at Prague Old Town Square. (a) Object and material labels from a 360-degree street image are assigned to the city geometry. (b) Plan view of the 22 observation points and visible roofline. (c) Close-up of the route. The arrow gives the body model's direction of travel.}
  \label{fig:configuration}
\end{figure*}
